# P5 / T5-1 checkpoint — DashScope contract 与双 Profile

- 日期：2026-08-08
- 分支：`phase/p5-dashscope-contract`
- 基线：`18dd9e8`
- AC：AC-020；为 AC-015/016/021 提供 provider contract 前置
- 生命周期：`automated_verified`（A1）；未执行 live call，未产生外部费用

## 交付

- ADR-0008 与 spec delta 正式替代旧 OpenAI Responses 假设：领域层 provider-neutral port，application adapter 使用 DashScope OpenAI-compatible Chat Completions。
- `dashscope-qwen37-flash-v1` / `dashscope-qwen38-max-v1` 均为 repo-versioned `EXPERIMENTAL` Profile，冻结模型、Prompt、JSON mode、thinking/tools/remote-media policy、token/call/微元费用预算和 2026-08-08 价格快照。
- 版本化 Prompt 把图片/JSON 内容视为不可信数据，并输出 strict Candidate JSON；合法 JSON 仍由既有 codec/validator 再校验。
- provider request 只接受规范化 PNG/JPEG bytes，没有 URL 输入；DashScope adapter 固定 endpoint、Base64 Data URL、`response_format=json_object`、`enable_thinking=false`，不发送 tools。
- API Key 支持 `DASHSCOPE_API_KEY` 或 `DASHSCOPE_API_KEY_FILE` 二选一；对象 `toString` 永远脱敏。基础 Compose 保持无 Key 可用，`compose.live.yaml` 通过 environment-backed secret 只读挂载。
- HTTP/provider 异常只暴露 stable code/status/retryability/Retry-After，不保存或传播 response body、Key、完整 prompt/output。

## 证据

| Gate | 保证等级 | Revision/input | 命令/来源 | 结果 | 原始证据 | 新鲜度 |
|---|---|---|---|---|---|---|
| TDD red | A0 | dirty diff | `mvn ... test-compile` | expected compile failure：Profile 字段/provider contract 尚不存在 | 当前会话工具输出 | exact dirty diff |
| provider local | A0 | dirty diff | quoted Maven targeted test | 4 DashScope adapter tests passed | 当前会话工具输出 | exact dirty diff |
| Compose live overlay | A0 | dirty diff + placeholder secret | `docker compose -f compose.yaml -f compose.live.yaml config --quiet` | exit 0；无配置输出 | 当前会话工具输出 | exact dirty diff |
| server affected | A1 | `18dd9e8` + metadata-bound dirty tree | `tools/run-gate.ps1 -Gate server`；子进程移除真实 Key | exit 0；Java 全 reactor、53 app tests、PostgreSQL migrations V001–V007 green | `.sdlc/evidence/20260808-185129-server/metadata.json` | exact dirty diff |

## 安全与恢复

- 本节点没有读取/输出真实 Key、没有 provider network call、没有真实数据、没有数据库迁移或外部费用。
- 源码恢复：revert 本节点独立 commit。
- 数据恢复：N/A，无数据结构或持久化写变化。
- 外部副作用恢复：N/A；live side effect 仍未发生。
- A2 尚未满足；待 T5-2/T5-3 后由独立上下文在同一 commit 重跑关键 gate/review。

## 再锚定

- 当前 diff 只触及已批准 P5 provider/Profile/secret/预算边界，没有实现 Template、Workspace、Renderer 或 AI publish/update/delete/tool capability。
- 下一恢复入口：提交本节点，基于该提交创建 `phase/p5-live-inference`，先以 fake provider 失败测试冻结真实上传与 durable worker 行为。
