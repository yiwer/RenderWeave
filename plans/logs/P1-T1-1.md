# Checkpoint：P1-T1-1

- 结果：完成
- AC：AC-001, AC-002, AC-008（P1 text subset）
- 生命周期状态：`automated_verified`
- 摘要：以失败测试冻结 strict definition envelope、identity key、字段唯一性和无 `fieldId` 契约；实现 Jackson 3 duplicate detection、封闭 member 检查、稳定 code + JSON Pointer 诊断，以及 immutable key/value objects。

## 验证证据

| Gate | 保证等级 | Revision | 命令/来源 | Exit/结论 | 原始证据指针 | 验证者 captured_by | 新鲜度 |
|---|---|---|---|---|---|---|---|
| T1-1 red baseline | A1 | `UNBORN` | `mvn -B -ntp -pl renderweave-schema test` | 1；28 个 missing-symbol 编译错误，证明测试先于实现 | 当前会话 tool capture | Codex shell tool | exact input；临时红证据未落盘 |
| schema local | A1 | `UNBORN` | `mvn -B -ntp -pl renderweave-schema clean test -Dmaven.compiler.showDeprecation=true` | 0；8 tests；0 warnings | 当前会话 tool capture | Codex shell tool | exact input |
| affected server reactor | A1 | `UNBORN` | `tools/run-gate.ps1 -Gate server` | 0；schema 8 + PG canary 1，全 reactor success | `.sdlc/evidence/20260808-002421-server/metadata.json` | project gate | exact manifest |

- 证据复用理由：server gate 在 T1-1 最后一次代码变化后生成，input manifest 包含当前所有 tracked/untracked 输入。
- 人工 gate：本任务无新的产品判断；沿用已记录的原型 J1，不以 A1 替代后续 P3 体验验收。

## 重要偏差

- P1 parser 暂只允许可持久化的 `text` value，并实现 min/max/pattern 数据形状；完整七类型、enum/const、regex safety 在已计划的 T2-1 完成。当前 API 尚未暴露 Draft 写入，因此没有把部分语义伪装成完整 v1。
- 是否需要 spec delta：否。
- 再锚定是否发现漂移：否；没有新增 Template/Workspace/Renderer、fieldId 或外部能力。

## 多 Agent

- 未启用；single writer，`atomic_claim:none`。

## 下一步

- 下一任务或恢复入口：P1/T1-2，先以 Testcontainers 失败测试冻结 revision 0→1、stale `expectedRevision` 零覆盖及完整 JSONB snapshot。
- 需要用户决定：无。
