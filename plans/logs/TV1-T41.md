# TV1-T41：definite Grid independent multi-AUTO constraints

- 结果：完成
- 生命周期状态：`automated_verified`
- 起始 revision：`409734fdf2a4289116f68d3da9b1ec76729154d2`
- 验证时 revision：`409734f` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\\Yiwer\\code\\RenderWeave-template-v1-implementation`
- single-writer：Codex `/root`
- 外部副作用：禁止 provider、API Key、真实数据、付费调用与 push/tag/PR

## 决策与边界

1. T40 verified commit 后重读原始 Ticket 10/16/19。一般 HUG/resource、multiple Stack FILL 与 multiple
   FRACTION 仍依赖未物化的 measurement 或 residual tolerance；Group transformed union 会引入尚未冻结的
   transform conformance。多个 AUTO 中每条 span constraint 至多覆盖一个 AUTO 时则无需平均或 tolerance。
2. 本票只深化既有 `layout_definite_resource_free`：允许多 AUTO 独立 constraints；跨两个以上 AUTO 的 span
   继续 `GRID_AUTO_TRACK`，HUG contribution 继续 `HUG_CONTENT`，不接线 daemon output 或注册 Profile。
3. TDD 先升级 shared vector/verifier `/7` 并捕获 Rust/Python RED，再实现 Rust primary 与 Python stdlib
   independent mirror；fixture bytes 未变时保持 `/3`。

## TDD、审查与证据

- [x] RED vectors：Rust positive case 在 grid occurrence 返回旧 `GRID_AUTO_TRACK`，stage case 未推进到 row
  `GRID_FRACTION_TRACK`；Python independent 同样停在旧 multi-AUTO 分支
- [x] GREEN：Rust primary 与 Python independent mirror 均实现独立多 AUTO constraint 求解；共同通过
  34 laid-out + 13 unsupported，47/47、142 checks
- [x] focused Rust exact vectors 3/3、Python stdlib independent replay、workspace fmt、clippy
  `-D warnings`、workspace tests、py_compile、JSON inventory 与 `git diff --check`
- [x] `render`：Windows Rust 与 `rust:1.89-slim` network-none Linux UDS 均通过，Profile NOT_REGISTERED、
  certification NOT_CERTIFIED、raster ABSENT、daemon output UNWIRED
- [x] `server`：8/8 reactor BUILD SUCCESS；Application 344 tests、0 failure、0 error、15 controlled skips
- [x] 受影响 `fast`：repository diff、八模块 package 与 Node 24 Web typecheck 全过
- [ ] governance-final `fast`（exact 目录只在 commit handoff 报告）
- [ ] final `full`（exact 目录只在 commit handoff 报告）
- [ ] verified local commit 与 clean worktree

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1/A2 | Rust primary + Python independent 47/47、142 checks；fmt/clippy/test/py_compile/diff-check 通过 | 当前命令输出 |
| Render | A1；exact replay A2 | gate 1.5；Java 26；Rust Windows + Linux network-none UDS；Python 110+14+32+47+16 | `.sdlc/evidence/20260822-001001-render/` |
| Server | A1 | 8/8 reactor；App 344 tests、0 failure、15 controlled skips | `.sdlc/evidence/20260822-001028-server/` |
| Affected Fast | A1 | repository diff、八模块 package 与 Node 24 Web typecheck 全过 | `.sdlc/evidence/20260822-002942-fast/` |
| Final Fast | A1 | resolution governance 后执行，exact 目录只在 commit handoff 报告 | `.sdlc/evidence/<final-passing-fast>/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在 commit handoff 报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |

## 合同 identity 与保证上限

- definite vector/verifier identity 为 `/7`，fixture identity 为 `/3`；vector SHA-256
  `f1e02f5286fcad390b2c62a9e2899dfb9ac94668e9e1e73245217dfb8b3ccbfe`，fixture SHA-256
  `a11475bcebad7e1c35cb0acd7018419d94afcb4b37d7f1df7346a055ad1df669`。
- independent layout implementation 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_SINGLE_FRACTION_INDEPENDENT_MULTI_AUTO_GRID_BOX_KERNEL`；
  render summary 的 `layoutKernel` 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_SINGLE_FRACTION_INDEPENDENT_MULTI_AUTO_GRID_BOX_AUTOMATED_VERIFIED_UNWIRED`。
- Profile 仍 NOT_REGISTERED、certification NOT_CERTIFIED、world scene/raster ABSENT、daemon output UNWIRED、
  Renderer READY=false。
- T41 不证明跨多个 AUTO 的平均 deficit、一般 intrinsic/HUG、multiple FRACTION、multiple Stack FILL water
  filling、完整 Layout Profile、resource/shaping、world transform/scene、pixel/JPEG、daemon RESULT、physical
  Linux certification、formal Case/Oracle、A3、新 J1、E6 或 Template/Renderer READY。
- rollback 边界为本票单一 verified commit；无 migration 或外部持久状态需逆转。未经授权不 push/tag/PR。
