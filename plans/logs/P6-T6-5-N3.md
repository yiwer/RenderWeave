# P6-T6-5 N3：确定性 Candidate Materializer 与能力矩阵

- 结果：完成
- 生命周期状态：`automated_verified`
- AC：AC-VR-003、AC-VR-008
- 实现 revision：`3d56c51dc329c2e0362f265e434d2db038dad05c`
- checkpoint revision：`3bee395c140446c04f9911a7c473128da1576e17`
- 分支：`phase/p6-visual-recognition-vnext`
- 决策：ADR-0024
- 外部副作用：0 Provider attempts、0 reservations、0 live ledger mutation；全部 live ledger 保持 `CLOSED`

## 可验证增量

1. 新增不可变 `renderweave-inference-pipeline/4.0`。IMAGE_ONLY 的 OBSERVE、HIERARCHY、
   ELEMENT_BINDING 继续使用三个严格视觉合同，STRUCTURE 改为本地 Java materializer；通用 Provider 入口
   对 pipeline 4 STRUCTURE/REPAIR fail-closed。
2. materializer 从已验证 inventory/hierarchy/binding 唯一生成 Schema、field、ONE/MANY reference、array、
   evidence 和 Candidate 局部 UUID。Schema 使用根开始的规范 BFS；field/evidence 固定排序与去重；等价计划
   的输入列表排列不影响 Candidate JSON 字节。
3. 所有本地物化 AI 项使用阈值减一的 confidence、`UNRESOLVED`、`required=false`，进入逐项人工审核；
   本节点没有把尚未校准的视觉推断伪装成已确认事实，也没有生成 constraint。
4. 新增 Flash、Plus、Max 三个 v5 Profile 与独立严格 capability 资源。启动时逐模型核对 exact model、
   JSON Object、non-streaming、thinking/tools/remote 边界、10 images、4096 edge、16,000,000 pixels、8192
   output tokens 与 240 秒 timeout。
5. Plus/Flash capability 绑定官方视觉/结构化输出文档与 N2 live evidence；精确 `qwen3.8-max` 只标记为
   `N2_EXACT_ALIAS_LIVE_ONLY`，不借用其他型号的 advertised 上限。模型响应名仍必须精确匹配。
6. 规范化图片现在同时受最长边 4096 与 16,000,000 总像素约束；4096² 会确定性归一为 4000²。费用预留
   仍按更大的 4096² 保守估算，不降低成本安全边界。
7. 三个 v5 Profile 保持 `EXPERIMENTAL`，只从内部 `visualNextProfiles()` 暴露；产品 capability API、
   OpenAPI、Web 选择器继续使用四个 v4 Profile。历史 v1..v4 Profile/Prompt 资源未修改，旧 snapshot 继续
   按原 pipeline 恢复。

## 验证结果

| 验证 | 结果 | 证明范围 |
|---|---|---|
| focused materializer/profile/input tests | 23/23 green | byte golden、排列不变性、三层站牌、严格 capability、图片像素边界 |
| inference module | 132/132 green | Candidate、visual contracts、Profile、Provider contract 与历史路径无回归 |
| focused PostgreSQL workflow | 26/26 green | 正常路径恰好 3 attempts/reservations；STRUCTURE 恢复零重复调用 |
| clean server gate | PASS，330 tests，5 gated skip | exact revision、fresh PostgreSQL migrations、全部服务端回归 |
| clean fast gate | PASS | checkpoint revision 的 repository diff、服务端打包与 Web typecheck |

A1 evidence：`.sdlc/evidence/20260810-213856-server/metadata.json`；其中
`revision=3d56c51dc329c2e0362f265e434d2db038dad05c`、`workingTreeDirty=false`、`result=passed`。
checkpoint 的 exact-clean fast A1 为 `.sdlc/evidence/20260810-214150-fast/metadata.json`，绑定
`3bee395c140446c04f9911a7c473128da1576e17`。这些证据是工具捕获的 A1，不夸大为独立 A2；N3 没有运行
网络或读取 API Key。

## 完成信号与残余

- AC-VR-003：同一 validated plan 的 Candidate byte golden 固定；pipeline 4 STRUCTURE Provider attempt=0；
  PostgreSQL 租约恢复不重复前三个成功 stage。
- AC-VR-008：三模型各有独立 capability/Profile/pricing/timeout/output 合同，启动时严格配对；Max 文档缺口
  被显式保留，不静默降级或外推。
- 仍未证明 perception/grounding 质量；v5 不可从产品 API 创建，也不晋级默认。这是 N4–N7 的质量门，
  不是 N3 失败。
- 下一节点 N4：多尺度 view、region/grounding 2.0、空间不变量，以及 GENERIC 与 TRANSIT_BOARD Prompt 分离；
  节点内仍为零 Provider。
