# TV1-T38：definite Stack singleton main-axis FILL

- 结果：完成
- 生命周期状态：`automated_verified`
- 验证时 revision：`df28550` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\\Yiwer\\code\\RenderWeave-template-v1-implementation`
- 外部副作用：Provider attempts=0、API Key reads=0、open authorization=0、paid external calls=0；未发送真实
  数据；只运行仓库 gate、offline Cargo/Python 与 network-none Linux UDS replay；未 push/tag/PR

## 产品增量

1. workspace-internal `renderweave-renderer-layout` 的唯一 arrange 入口继续消费 T23/T25 同一 admitted/preflight
   tree；在 T33 Stack 与 T34 Grid 闭包上，仅新增每个 definite Stack 至多一个 main-axis FILL，不新增 parser、
   crate、route、Profile 或 daemon success path。
2. Stack 按 authored order 从 definite ContentBox 扣除 materialized-adjacent gap、全部 signed main margins 与非
   FILL definite sizes；唯一 FILL 获得 `max(0, available - usedWithoutFill)`，再按既有顺序应用 min/max clamp。
   singleton 下 `fillWeight` 在冻结比例公式中相消，不引入 residual tolerance 或额外量化。
3. sole FILL 的 max clamp 留余进入六种 `justifyContent`；min clamp 超出可用空间时允许 overflow，free 为正零。
   cross-axis FIXED/FILL、margin、alignment、ContentBox 与 Frame/Stack/Grid 递归语义保持不变。
4. 同一 Stack 出现多个 main-axis FILL 时继续以第一个 authored FILL occurrence 返回 `STACK_MAIN_FILL`；sibling
   预量不改变实际 DFS first error，任何失败不返回 partial layout。
5. HUG、Grid AUTO/FRACTION、Group/compositionViewport、Text/Image、resource fetch/decode/shaping、world scene、
   paint/raster/JPEG、daemon RESULT、Profile registration 与公开 render/preview 均未实现。

## TDD、审查与证据

- RED：先把 vector identity 升级为 `/4` 并把 singleton cases 改成 exact laid-out expectations；Rust primary 与
  Python independent 都先在首个 singleton case 返回既有 `STACK_MAIN_FILL`。首次 GREEN 后自审再加入“首个 main
  FILL 同时 cross-axis HUG”的 multiple-FILL 组合，两端均先错误返回 `HUG_CONTENT`；把 fill 发现与 cross-axis
  measure 解耦后最终共同通过 28 laid-out + 11 unsupported，39/39、120 checks。
- 覆盖 row/column、remainder consumption、max-clamp justify release、min-clamp overflow、nested singleton、
  multiple-FILL first occurrence 与 sibling/premeasure 下的 authored DFS first error。
- focused Rust/Python、workspace fmt、clippy `-D warnings`、workspace tests、PowerShell gate、JSON hash/inventory 与
  `git diff --check` 均通过。自审补齐 top-level render summary 的 singleton `layoutKernel` machine identity。
- 首次 `render` `.sdlc/evidence/20260821-204919-render/` 只因 harness 仍断言旧 105 checks 而失败；product/replay
  report 当时已绿色。修正 118 后的中间 pass 又经自审发现旧 T34 summary 标签；21:10 pass 后再以组合向量抓到
  cross-HUG/multiple-FILL 首错问题。最终一致证据以 21:17 目录为准。
- Server 最终 BUILD SUCCESS；Application 344 tests、0 failure、0 error、15 controlled skips。测试上下文销毁后
  遗留 scheduler/Hikari pool 曾打印旧 Testcontainer 端口连接拒绝并触发 Surefire 30 秒强制 fork 退出，但未形成
  test/gate failure，且与本票 renderer-only diff 无关；如实保留在原始 server log。
- Java/OpenAPI/generated SDK/migration/API version/Web/route 零产品差异；API contract 保持 `0.15.0`，runtime
  migration count 保持 25。

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1/A2 | Rust primary + Python independent 39/39、120 checks；fmt/clippy/test/diff-check 通过 | 当前命令输出 |
| Render | A1；exact replay A2 | gate 1.5；Java 26；Rust Windows + Linux network-none UDS；Python 110+14+32+39+16 | `.sdlc/evidence/20260821-211731-render/` |
| Server | A1 | 8/8 reactor；App 344 tests、0 failure、15 controlled skips | `.sdlc/evidence/20260821-205133-server/` |
| Affected Fast | A1 | repository diff、八模块 package 与 Web typecheck 全过 | `.sdlc/evidence/20260821-210938-fast/` |
| Final Fast | A1 | resolution governance 后执行，exact 目录只在 commit handoff 报告 | `.sdlc/evidence/<final-passing-fast>/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在 commit handoff 报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |

## 合同 identity 与保证上限

- definite vector/verifier identity 为 `/4`，fixture identity 为 `/3`；vector SHA-256
  `640c53afb1a19605ce7318fd40d991c5934de32dae6699c79306eee1e05227d4`，fixture SHA-256
  `a11475bcebad7e1c35cb0acd7018419d94afcb4b37d7f1df7346a055ad1df669`。
- independent layout implementation 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_GRID_BOX_KERNEL`；render summary 的
  `layoutKernel` 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_GRID_BOX_AUTOMATED_VERIFIED_UNWIRED`。
- Profile 仍 NOT_REGISTERED、certification NOT_CERTIFIED、world scene/raster ABSENT、daemon output UNWIRED、
  Renderer READY=false。
- T38 不证明 multiple-FILL water filling/tolerance、HUG、Grid AUTO/FRACTION、完整 Layout Profile、resource/shaping、
  world transform/scene、pixel/JPEG、daemon RESULT、physical Linux certification、formal Case/Oracle、A3、新 J1、
  E6 或 Template/Renderer READY。
- rollback 边界为本票单一 verified commit；无 migration 或外部持久状态需逆转。未经授权不 push/tag/PR。
