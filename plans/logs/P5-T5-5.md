# P5 / T5-5 checkpoint — 安全复核与 Profile 认证决定

- 日期：2026-08-08
- 分支：`phase/p5-a2-hardening`
- 基线：`916a9f3`
- AC：AC-021
- 生命周期：`safety_independently_reviewed`；`certification_incomplete`
- 决定：两个 DashScope Profile 均保持 `EXPERIMENTAL`、默认关闭，不具备认证或生产启用资格

## 独立复核结论

- 独立只读 reviewer `/root/p5_independent_review` 首轮给出 FAIL，并定位了预算账本可随 run 删除、请求前费用边界不足、live upload 与 worker 授权未分离、retry/recovery 绕过门、`Retry-After` 日期格式、multipart 容器边界、provider 响应无上限以及 OpenAPI/generated drift 等问题。
- 当前分支逐项修复后，reviewer 第二轮复核为 **PASS**，未遗留 blocker/high/medium finding。该 A2 只覆盖本次 P5 live safety hardening，不等价于质量评测 A2、外部 CI 或平台强制的 A3。
- 仍保留的 residual：没有数据库超级用户级/A3 策略；没有 crash injection 或 reserve/delete 并发专项测试；评测器当前偏结构化，不能把“结构指标 100% 但仍有 review blocker”的 2 条结果解释为模型质量认证。

## 已闭环的安全边界

- V009 仅追加迁移，移除 reservation ledger 的破坏性 run 外键；消费记录在 run 删除后仍不可被抹除，未知 run 不能预留预算。
- worker 在 provider 调用前按 UTF-8 输入、图片上界、输出上界及 qwen3.7-flash 分档价格计算最坏费用；实际费用不能超过 reservation。
- live worker 与 live upload 分设 fail-closed gate；新建、retry、kick 与 lease recovery 均要求两门同时开启。已关闭授权账本和 Compose overlay 均保持两门为 false。
- 任意非空 `Retry-After`（秒数、HTTP-date 或不可解析值）都会安全停止即时重试；lease 为 600 秒。
- HTTP 层接受领域允许的 10 MiB 单图与 30 MiB 批次，并为 Servlet 超限稳定返回 RFC 9457 `413 / INFERENCE_PAYLOAD_TOO_LARGE`；provider 响应流上限为 3 MiB。
- OpenAPI、生成 TypeScript client、系统版本与 UI 已同步到 `0.9.0`。

## 证据

| Gate | 保证等级 | 结果 | 原始证据 |
|---|---|---|---|
| 限定 synthetic live canary | J1 / A1 | 2 次真实调用，¥0.054017；`certificationComplete=false` | `.sdlc/evidence/20260808-114257-p5-live-canary/summary.json` |
| P5 safety independent review | A2 | 最终 PASS；0 blocker/high/medium | reviewer `/root/p5_independent_review` 的独立只读复核记录 |
| server full gate | A1 | 151 tests；150 passed，1 个已关闭付费 canary skipped；V001–V009 green | `.sdlc/evidence/20260808-204315-server/metadata.json` |
| web full gate | A1 | 27 tests + typecheck + lint + production build green | `.sdlc/evidence/20260808-203911-web/metadata.json` |
| Compose config | A1 | base 与 live overlay 均可解析；live worker/upload 均为 false | 当前节点命令记录 |

## 认证决定

- 真实通路与本轮安全闭环已证明，但只完成 2/60 个 synthetic case；没有完成按 mode/holdout 的独立全量评测。
- `dashscope-qwen37-flash-v1` 与 `dashscope-qwen38-max-v1` 均保持 `EXPERIMENTAL`，不会自动选为默认 Profile。
- 认证状态保持 `certificationComplete=false`。后续若要运行剩余 58 条或任何新增 provider call，必须创建新的显式 J1 授权、预算与数据范围账本；本节点剩余额度不延续。
