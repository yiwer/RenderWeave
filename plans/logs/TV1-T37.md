# TV1-T37：Editor E9 accessibility 与问题定位

- 结果：完成
- 生命周期状态：`automated_verified`
- 验证时 revision：`8406f85` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\\Yiwer\\code\\RenderWeave-template-v1-implementation`
- 外部副作用：Provider attempts=0、API Key reads=0、open authorization=0、paid external calls=0；未发送真实
  数据；未 push/tag/PR

## 已冻结边界

1. Web-only 实现 strict RFC 6901 problem projection、invalid summary focus、roving tree keyboard、central live delta、
   1024 inspector drawer 与 unsupported-width 行为；不修改服务端保存/确认语义。
2. 浏览器证据使用无产品 route 的 Vite/Playwright fixture；1024/1280/1440、等效 200% 有效视口与 `<1024`
   状态均需覆盖，axe serious/critical 必须为 0。
3. 不实现 E6 preview、不开放 `/templates/:templateId`，不修改 Java/OpenAPI/generated SDK/migration/Renderer/
   Profile/formal records；人工 keyboard/zoom J1 保持 pending，不以自动 gate 冒充 accepted。

## 产品增量与审查

1. `template-problem-locator` 对 RFC 6901 pointer 执行 2048 字符/256 segment 预算、严格 `~0/~1` 解码、
   canonical 数组索引与 own-property exact walk；closed target 仅为 Template display name、definitions、稳定 authored
   node（含如实标记 owning-node 精度）或 unavailable，不从输入构造 selector。
2. invalid-save 摘要自动聚焦；可定位问题经显式按钮打开所需面板、扩展有界树、选择/滚动/聚焦等价目标。Structure
   tree 采用 roving tab 与 ArrowUp/ArrowDown/Home/End，中央 polite/atomic announcer 只播报短 delta；整个 entry panel
   不再是 live region。
3. 1024 drawer、1280/1440 完整操作、44px 可见按钮、skip/focus/tree/locator、1024 CSS px + 2x device scale 与
   900px truthful unsupported 均由无产品 route 的 Vite/Playwright fixture 覆盖；axe serious/critical、横向溢出、
   console/page errors 均为 0。
4. pure locator 14/14；新增 DOM E9 3/3；完整 Editor 12 files/136 tests。正式 Node 24 Web 为 26 files/212 tests、
   typecheck/lint 与 2144-module build 全绿。完整 E2E 为 23 passed/1 明确 live-provider skip；既有资源列表 axe
   audit 在 reduced-motion 用户偏好下等待卡片入场结束，避免扫描动画过渡帧而不放宽 WCAG 规则。
5. 无 Java/OpenAPI/generated SDK/migration/API version/`App.tsx`/产品 route/Renderer 差异；没有 E6 preview、
   provider/API Key/真实数据/付费调用。最终 full 在 resolution governance 落盘后执行，exact evidence 路径仅在
   commit handoff 报告，避免把 gate 运行后生成的目录反写进其输入。

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1 | locator 14/14；DOM E9 3/3；Editor 136/136 | 当前命令输出 |
| Browser/axe + E2E | A1 | E9 4/4；整套 23 passed/1 gated skip | `.sdlc/evidence/20260821-200647-e2e/` |
| Web | A1 | Node 24；26 files/212 tests；SDK/typecheck/lint/build 全绿 | `.sdlc/evidence/20260821-200839-web/` |
| Fast | A1 | resolution governance 后执行 | `.sdlc/evidence/<final-passing-fast>/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在提交交接报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |

## 保证上限

- T37 只验证当前未发布 Product component 的自动可访问性行为；真实浏览器 200% zoom、人工完整键盘走查与体验
  判断仍需 J1。
- 不证明 E6 authoritative preview、公开产品 route、Renderer output/raster/Profile/certification、A3 或 READY。
