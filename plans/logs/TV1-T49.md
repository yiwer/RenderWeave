# TV1-T49：zero-rotation affine 非空 Frame HUG

- 结果：完成
- 生命周期状态：`automated_verified`
- 起始 revision：`162dcd855dc2ca4e4bad894585c3af31750d19af`
- 验证时 revision：`162dcd8` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\\Yiwer\\code\\RenderWeave-template-v1-implementation`
- single-writer：Codex `/root`
- 外部副作用：禁止 provider、API Key、真实数据、付费调用与 push/tag/PR；实际计数均为 0

## 决策与边界

1. T48 verified commit 后重读当前 layout seam、Ticket 10/16/19 与冻结 Renderer 语义。multiple
   FILL/FRACTION、跨多 AUTO 平均仍依赖未物化 residual tolerance；任意 rotation AABB 依赖未冻结的跨平台
   三角函数 tolerance；actual resource fetch 仍缺 deployment identity。`rotationDeg == 0` 的 axis-local affine
   Frame HUG 可只用 binary64 加乘与 min/max 精确闭合，因此成为下一条安全 frontier。
2. 只深化 workspace-internal `renderweave-renderer-layout` 与唯一入口
   `layout_definite_resource_free(&AdmittedRenderDocument)`。非空 Frame HUG 只测 direct ABSOLUTE child 的
   resource-free definite/HUG size，复用 admission/preflight、authored preorder、ContentBox 与 arrange writer；
   未新增 crate、parser、route、Profile 或 daemon success path。
3. 每轴固定执行 `originRatio×size`、`position+originOffset`、transformed near/far、`max(near, far)`；Frame
   extent 从 0 开始，负端只 overflow，再按 padding、inward stroke、min→max 处理。scale 可为任意非零值，
   包括 flip；所有中间值 finite-check，不做 FMA、epsilon、tolerance 或量化。
4. 实际参与 Frame HUG 的 direct child 若 `rotationDeg != 0`，返回 closed internal `CHILD_ROTATION`。Group、
   Text/Image/Vector intrinsic、resource fetch/decode、multiple Stack FILL、跨多 AUTO 平均、multiple FRACTION、
   world scene/paint/raster/JPEG 与 daemon RESULT 保持 fail closed。

## TDD、审查与证据

- [x] RED：shared vector `/12` 先把 Frame cases 转 positive，并加入 signed position、origin、正/负 scale、
  单轴/递归/min-max 与 nonzero rotation；Rust primary 与 Python independent verifier 同时失败。
- [x] GREEN：Rust 与 Python 分别实现冻结公式；随后以 authored-first negative vector 发现两端都会在更晚的
  resource dependency 上失败，再把 kind/role boundary 前移，稳定返回 `CHILD_ROTATION`。
- [x] Focused：`cargo fmt --all`、layout crate tests、workspace clippy `-D warnings`、workspace tests、Python
  independent replay、`py_compile`、JSON inventory 与 `git diff --check` 全绿。
- [x] 受影响与 Goal：`render`、`server`、`fast`、`full` 全绿。
- [x] 最终 Fast：治理落盘后执行并通过，证据 `.sdlc/evidence/20260822-121536-fast/`。
- [x] 提交前 exact staged-diff 审计；verified local commit 与 clean worktree 由本次 handoff 给出。

## 合同 identity 与保证上限

- definite-layout vector/verifier `/12`：57 laid-out + 12 unsupported，共 69/69、209 checks；vector SHA-256
  `b77d4428d346af37d8f16974e522a507f07d38b092391d232c5e5a79e270a973`；fixture `/3` SHA-256
  `a11475bcebad7e1c35cb0acd7018419d94afcb4b37d7f1df7346a055ad1df669`。
- layout implementation identity：
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_SINGLE_FRACTION_INDEPENDENT_MULTI_AUTO_GRID_EMPTY_CONTAINER_STACK_HUG_GRID_AUTO_HUG_CONTRIBUTION_GRID_HUG_ZERO_ROTATION_AFFINE_FRAME_HUG_BOX_KERNEL`。
- render gate version `renderweave-renderer-process-gate/2.0`；gate summary identity 在上述 identity 后追加
  `_AUTOMATED_VERIFIED_UNWIRED`。Profile NOT_REGISTERED、certification NOT_CERTIFIED、resource bytes UNFETCHED、
  world scene/raster ABSENT、daemon output UNWIRED。
- 本票只证明 zero-rotation affine resource-free Frame HUG exact-bit 子闭包，不证明完整 Layout/Renderer、任意
  rotation tolerance、scene/pixel、daemon output、A3/J1/READY。rollback 边界为本票单一 verified commit；
  无 migration 或外部持久状态需逆转。

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1/A2 | Rust primary + Python independent 69/69、209 checks；fmt/clippy/test/py_compile/JSON inventory/diff-check 通过 | 当前命令输出 |
| Render | A1；exact replay A2 | gate 2.0；Java 26；Rust Windows + Linux network-none UDS；Python process 110 + document 83 + resource body 15 + preflight 32 + layout 69 + PNG 16 | `.sdlc/evidence/20260822-112051-render/` |
| Server | A1 | 8/8 reactor；Schema 20、Validation 13、Inference 361、Template 78、Asset 90、Rendering 109、App 344，0 failure/error | `.sdlc/evidence/20260822-112132-server/` |
| Affected Fast | A1 | repository diff、server package 与 Node 24 Web typecheck 全过 | `.sdlc/evidence/20260822-114137-fast/` |
| Goal Full | A1；各 exact replay step 按自身合同为 A2 | renderer、server、Node 24 Web、offline/R0/R1/P0、runtime 与 E2E 全绿；provider attempts/key reads/cost 0 | `.sdlc/evidence/20260822-114421-full/` |
| Final Fast | A1 | resolution governance 后 repository diff、server package 与 Node 24 Web typecheck 全过 | `.sdlc/evidence/20260822-121536-fast/` |
