# TV1-T33：资源无关 definite Stack layout kernel

- 结果：完成
- 生命周期状态：`automated_verified`
- 验证时 revision：`7e5f3df` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\Yiwer\code\RenderWeave-template-v1-implementation`
- 外部副作用：Provider attempts=0、API Key reads=0、open authorization=0、paid external calls=0；未发送真实
  数据；只运行仓库 gate、offline Cargo/Python 与 network-none Linux UDS replay；未 push/tag/PR

## 产品增量

1. workspace-internal `renderweave-renderer-layout` 的唯一 arrange 入口演进为
   `layout_definite_resource_free(&AdmittedRenderDocument)`；继续消费 T23/T25 同一 admitted/preflight tree，输出
   immutable authored-preorder `DefiniteLayout`，失败时不暴露 partial layout。
2. T26 的 Canvas/Frame/8 种资源无关视觉叶子 + ABSOLUTE FIXED/FILL 闭包扩展到 definite Stack 与 STACK child；
   Frame/Stack/叶子可递归组合，不新增 parser、crate、route、Profile 或 daemon success path。
3. ROW 左到右、COLUMN 上到下；signed leading/size/trailing margin、仅 materialized 相邻 child 的 gap、occupied/free
   `max(0, …)` 与 START/CENTER/END/SPACE_BETWEEN/SPACE_AROUND/SPACE_EVENLY 均按固定 binary64 顺序执行。
   distributed slots 的最后一项接收 remainder，single-child 行为与冻结规格一致。
4. cross axis 以原始 signed-margin interval 计算 FIXED/FILL；FILL 先 `max(0, interval)` 再 min/max clamp，随后按已
   展开的 `alignSelf`（含 parent `alignItems` 继承）START/CENTER/END 定位。Stack main-axis FILL 稳定返回 closed
   internal `STACK_MAIN_FILL`，不猜测 water-fill tolerance。
5. Stack sibling 预量只服务主轴分布；实际 traversal 仍先分类当前 node 并递归其 subtree，因此 deferred unsupported
   不会越过更早 authored DFS error。失败结果全有或全无。

## TDD、审查与证据

- RED：升级后的 shared Stack vectors 先令 Rust 缺少 `layout_definite_resource_free`/`StackMainFill`，Python 仍把
  Stack 归为 unsupported；两端均在实现前失败。GREEN：Rust primary 与 Python stdlib independent algorithm 同时
  通过 21 laid-out + 10 unsupported，31/31、97 checks。
- 覆盖 ROW/COLUMN、signed margins、全部六种 justify、empty/single/nested Stack、non-divisible remainder、overflow
  free=0、cross FILL max clamp + inherited alignment、main FILL fail-closed 与 sibling premeasure/DFS error order。
- `cargo fmt --all`、workspace clippy `-D warnings`、workspace test、Python compile/replay、PowerShell gate parse、JSON
  inventory/hash 与 `git diff --check` 均通过；自审修正顶层 render summary 的旧 T26 `layoutKernel` 标签。
- Java/OpenAPI/generated SDK/migration/API version/Web/route 零差异；API contract 保持 `0.15.0`，runtime migration
  count 保持 25。

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1/A2 | Rust primary + Python independent 31/31、97 checks；fmt/clippy/test/diff-check 通过 | 当前命令输出 |
| Render | A1；exact replay A2 | gate 1.4；Java 26；Rust Windows + Linux network-none UDS；Python 110+14+32+31+16 | `.sdlc/evidence/20260821-160137-render/` |
| Server | A1 | 8/8 reactor；App 344 tests、0 failure、15 controlled skips | `.sdlc/evidence/20260821-154146-server/` |
| Fast | A1 | repository diff、八模块 package 与 Web typecheck 全过 | `.sdlc/evidence/20260821-154123-fast/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在提交交接报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |

## 合同 identity 与保证上限

- definite fixture/vector/verifier identity 均为 `/2`；vector SHA-256
  `0cca763c421ad19baf8d662db68f652cb0f0deead842b3df19075a38f8d3d891`，fixture SHA-256
  `25772e296b81a14bd4cdc1e08481097de18ecc655e8b70b2e5b60ccfb7195c8f`。
- render summary 的 `layoutKernel` 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_AND_STACK_BOX_AUTOMATED_VERIFIED_UNWIRED`；Profile 仍 NOT_REGISTERED、
  certification NOT_CERTIFIED、world scene/raster ABSENT、daemon output UNWIRED、Renderer READY=false。
- T33 不证明 HUG、Stack water filling、Grid、resource/shaping、world transform/scene、pixel/JPEG、daemon RESULT、
  physical Linux certification、formal Case/Oracle、A3、J1 或 Template/Renderer READY。
- rollback 边界为本票单一 verified commit；无 migration 或外部持久状态需逆转。未经授权不 push/tag/PR。
