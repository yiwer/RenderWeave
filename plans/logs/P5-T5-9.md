# P5 / T5-9 checkpoint — Payload-free IMAGE_ONLY diagnostics

- 日期：2026-08-09
- 分支：`phase/p5-image-only-diagnostics-v1`
- 基线：`f2c0341`
- AC：AC-016、AC-021
- 生命周期：`implementation_complete`（clean A1 与独立 A2 待完成）
- 外部副作用：0 Provider attempt、0 token、¥0；未读取 API key，所有 live gate 均关闭

## 目标与边界

- 解释 Grounded v2 中 IMAGE_ONLY 20/20、60 attempts 全部 `REJECTED` 的系统性合同失败类别。
- 只持久化系统定义的稳定问题 code 与次数，不保存 Candidate、Prompt、图片、RootDocument、字段名/值、pointer、problem args、异常消息或 Provider request id。
- 不修改 Prompt/Profile、repair policy 或认证阈值；不重写 CLOSED evidence，不申请或执行 live。

## 红绿实现

- 红：先增加 strict decode 八分类、bounded taxonomy、真实 PostgreSQL attempt round-trip、journal persistence/隐私与 V010 migration tests；实现前因缺少 diagnostic code、taxonomy 类型与 attempt Map 而按预期编译失败。
- 绿：Candidate codec 保持外部错误码不变，同时产生低基数 diagnostic code；attempt 在解析成功时使用与确定性校验共享的 validator helper生成计数。
- 存储：`V010__attempt_problem_taxonomy.sql` 追加 JSONB `{}` 默认值、object 与 16 KiB 检查；Java strict codec 在写入和读取端重验。
- 评测：journal/report/canary schema 分别升级为 1.2/1.2/1.1；journal 1.1 缺失字段按空 Map 读取，不修改历史文件。

## 当前验证

- 定向离线 Maven：Candidate contract + taxonomy 19/19；Environment/Flyway + journal + PostgreSQL live workflow 27/27。
- fresh PostgreSQL 从 V001 顺序迁移到 V010；workflow 已验证空 Map、decode value-invalid、`AI_REQUIRED_UNCONFIRMED` 与 `JSON_EVIDENCE_ITEM_MISSING` 的精确持久化。
- journal 1.2 reload 保留计数且敏感词扫描为零；合成 1.1 state 移除新字段后可读为空 Map。
- 命令环境显式清除了 `DASHSCOPE_API_KEY`、key file 与所有 live/certification gate；测试 Provider 均为本地 fake。

## 待完成门禁

- 在实现节点提交上执行 clean `tools/run-gate.ps1 -Gate server` 并记录 A1 evidence。
- 使用现有独立 reviewer 做 payload 泄露、分类上限、迁移/兼容、worker一致性与 default-off A2。
- 修复任何 Blocker/High/Medium 后重跑 clean A1，再把生命周期提升为 `automated_verified` / `independently_reviewed`。

## 恢复点

当前可恢复到基线 `f2c0341`；新迁移只追加，未执行生产迁移或外部写入。所有历史 authorization 仍为 CLOSED，本节点无任何可复用 live 权限。
