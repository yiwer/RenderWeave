# P5 / T5-9 checkpoint — Payload-free IMAGE_ONLY diagnostics

- 日期：2026-08-09
- 分支：`phase/p5-image-only-diagnostics-v1`
- 基线：`f2c0341`
- AC：AC-016、AC-021
- 生命周期：`independently_reviewed`（clean A1 + 独立 A2 PASS）
- 外部副作用：0 Provider attempt、0 token、¥0；未读取 API key，所有 live gate 均关闭

## 目标与边界

- 解释 Grounded v2 中 IMAGE_ONLY 20/20、60 attempts 全部 `REJECTED` 的系统性合同失败类别。
- 只持久化系统定义的稳定问题 code 与次数，不保存 Candidate、Prompt、图片、RootDocument、字段名/值、pointer、problem args、异常消息或 Provider request id。
- 不修改 Prompt/Profile、repair policy 或认证阈值；不重写 CLOSED evidence，不申请或执行 live。

## 红绿实现

- 红：先增加 strict decode 八分类、bounded taxonomy、真实 PostgreSQL attempt round-trip、journal persistence/隐私与 V010 migration tests；实现前因缺少 diagnostic code、taxonomy 类型与 attempt Map 而按预期编译失败。
- 绿：Candidate codec 保持外部错误码不变，同时产生低基数 diagnostic code；attempt 在解析成功时使用与确定性校验共享的 validator helper生成计数。
- 存储：`V010__attempt_problem_taxonomy.sql` 追加 JSONB `{}` 默认值、object 与 16 KiB 检查；Java strict codec 在写入和读取端重验。
- 评测：journal/report/canary schema 分别升级为 1.2/1.2/1.1；journal 1.1 缺失字段按空 Map 读取，不修改历史文件。

## 验证与证据

- 定向离线 Maven：Candidate contract + taxonomy 19/19；最终 journal/payload guard 独立重放 15/15；含 fresh PostgreSQL 的本地定向回归全部通过。
- fresh PostgreSQL 从 V001 顺序迁移到 V010；workflow 已验证空 Map、decode value-invalid、`AI_REQUIRED_UNCONFIRMED` 与 `JSON_EVIDENCE_ITEM_MISSING` 的精确持久化。
- journal 1.2 严格拒绝重复/未知字段、标量与浮点强制转换、缺失/null taxonomy；1.1 仅允许 CLOSED 只读兼容，OPEN 恢复 fail-closed。
- canary 1.1 已移除 Provider request id，并用 scalar metrics 替代完整 mismatch 列表；canary、certification summary、journal 共用 payload-free 写边界，journal reload 同样执行 guard。
- 命令环境显式清除了 `DASHSCOPE_API_KEY`、key file 与所有 live/certification gate；测试 Provider 均为本地 fake。
- 实现节点：`7316e2f`；输入操纵修复：`65ad369`；证据边界硬化：`ec53b3d`。
- clean server A1：`.sdlc/evidence/20260809-174556-server/metadata.json`，精确 revision `ec53b3d5408f8f083abfeb95c2fca10b9924ec1b`、`workingTreeDirty=false`、manifest SHA-256 `52B6D84B29E1FDB819E0116E2AA87331B40D1D68EEB3B2ADD18E42525F8ED3F7`；Schema 20、Validation 13、Inference 90、Application 111，2 个 paid live tests 默认跳过。
- `.sdlc/evidence/20260809-174501-server` 因 Docker Desktop 瞬时不可用失败，只作为非结论环境记录；daemon 恢复后同一 clean revision 重跑通过。
- 独立 A2：`ec53b3d` PASS，0 Blocker / 0 High / 0 Medium；复核了分类不可被输入文本操纵、strict taxonomy、worker 单一路径、V010/store、journal 兼容与 payload-free evidence 边界。

## 低风险残余

- 尚无专门的 V009 既有 attempt 行升级到 V010 的集成 fixture；当前由 PostgreSQL 非空默认、fresh V001–V010 migration 与严格 round-trip 共同覆盖。
- 外部手工构造 taxonomy 可令 `TRUNCATED_CODE` 计数不为 1；正常 Builder/worker 路径始终 canonical 为 1。
- 本节点只完成 payload-free 诊断能力，不构成新的 IMAGE_ONLY 失败归因或 Profile 认证；真实归因仍必须使用新 Profile、精确 `PROPOSED` ledger、新 J1 与 live evidence。

## 恢复点

当前实现恢复点为 `ec53b3d`，节点基线为 `f2c0341`；新迁移只追加，未执行生产迁移或外部写入。所有历史 authorization 仍为 CLOSED，本节点无任何可复用 live 权限。
