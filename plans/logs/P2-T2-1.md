# Checkpoint：P2-T2-1

- 结果：完成
- AC：AC-001, AC-008, AC-009, AC-011（DSL/parser subset）
- 生命周期状态：`automated_verified`
- 摘要：closed DSL 扩展为 text/decimal/date/time/boolean/reference/array 七类型，覆盖类型专属 constraints、typed enum/const、精确 decimal/date/time、非嵌套数组和保守 regex safety；normalized writer 可稳定 round-trip。

## 验证证据

| Gate | 保证等级 | Revision | 命令/来源 | Exit/结论 | 原始证据指针 | 验证者 captured_by | 新鲜度 |
|---|---|---|---|---|---|---|---|
| T2-1 red baseline | A1 | `UNBORN` | schema test compile | 1；七类 descriptor 尚不存在 | 当前 Goal tool capture | Codex shell tool | superseded by green |
| schema affected tests | A1 | `UNBORN` | `mvn -pl renderweave-schema test` | 0；16 tests，含 100-case seeded decimal round-trip | 当前 Goal tool capture | Codex shell tool | superseded by server gate |
| affected server gate | A1 | `UNBORN` | `tools/run-gate.ps1 -Gate server` | 0；16 schema + 9 app tests | `.sdlc/evidence/20260808-011728-server/metadata.json` | project gate | exact manifest |

- 证据复用理由：server gate 在所有 T2-1 生产与测试变化后运行，且通过真实 PostgreSQL 验证 invalid definition 零写。
- 人工 gate：无；类型/约束语义来自已批准 spec。

## 重要偏差

- `reference` descriptor 已进入 domain parser，但在 T2-2 transactional graph adapter 完成前，Draft persistence 明确返回 `REFERENCE_RESOLUTION_REQUIRED`；不会出现悬空引用暂存态。
- Regex policy 采用保守安全交集：拒绝 backreference/lookaround/inline flag/Java-only escape/possessive quantifier、嵌套或明显歧义的无界重复；匹配验证使用 substring 语义。
- Decimal 从 JSON number 直接进入 BigDecimal，normalized precision≤128、scale -64..64，writer 强制 plain notation、零为 `0`。
- object/reference array 只要出现 `uniqueItems`（包括 false）即拒绝，符合“不支持 uniqueItems”而非只禁止 true 的解释。
- 是否需要 spec delta：否。
- 再锚定是否发现漂移：否；尚未实现引用解析、生命周期或发布。

## 多 Agent

- 未启用；single writer，`atomic_claim:none`。

## 下一步

- 下一任务或恢复入口：P2/T2-2，以 Testcontainers 失败测试冻结 advisory-lock 图事务、DAG/depth、soft delete/restore/history/copy。
- 需要用户决定：无。
