# P5 / T5-3 checkpoint — 60-case live 金标与结构质量评测合同

- 日期：2026-08-08
- 分支：`phase/p5-live-evaluation`
- 基线：`af1e199`
- AC：AC-016、AC-021（部分）
- 生命周期：`automated_verified`（A1，评测合同）；Profile certification 仍为 `not_eligible`

## 交付

- 新增版本化 `renderweave-live-eval/1.0` 金标：完整覆盖已有 60 条仓库合成 fixture，IMAGE_ONLY / JSON_ONLY / COMBINED 各 20 条，固定 45 DEV + 15 HOLDOUT。
- 每条金标冻结 fixture、mode、partition、期望 Schema 数、root field 集及其 scalar/reference/array/unresolved/conflict shape；字段 UUID 与 display wording 不参与评分。
- corpus loader 与 60-case replay manifest 交叉校验 fixture 集、mode、Schema 数和 root field envelope，重复、遗漏或 partition 漂移会 fail fast。
- case evaluator 输出 root precision/recall、shape accuracy、AI evidence coverage、未证明 required 的安全率、blocker 数和可审计差异。
- reporter 支持 global / mode / DEV-HOLDOUT slice，并把缺少的 case id 明确写入报告；任一子集结果的 `complete=false`，不能被误作 60-case 认证。

## 证据

| Gate | 保证等级 | Revision/input | 命令/来源 | 结果 | 原始证据 | 新鲜度 |
|---|---|---|---|---|---|---|
| gold projection | A0 | repository synthetic replay corpus | 临时只读 projection test | 60 条稳定 root shape projection；临时测试随后删除 | 当前会话工具输出 | exact corpus |
| evaluator | A0 | dirty diff | targeted `LiveCandidateEvaluatorTest` | 4 tests green：60/20×3/45+15、exact pass、shape/required regression、partial-report deny | 当前会话工具输出 | exact dirty diff |
| paid-test default deny | A0 | dirty diff；子进程移除 canary gate 与 Key | evaluator + `DashScopeLiveCanaryTest` targeted compile | evaluator 4 green；paid canary 1 skipped；0 provider call | 当前会话工具输出 | exact dirty diff |

## 认证边界

- 本节点建立 60-case 数据集与可重复的第一层结构指标，但尚未执行 60 条真实 provider 输出；当前 J1 只允许最多 6 次调用，因此不能完成 full/mode/holdout live report。
- 当前 scorer 尚不声称覆盖规格中的完整 schema/entity F1、全图 field micro-F1、parent-child edge F1、DAG 与 critical-hallucination 发布判定。已有 strict Candidate codec/validator 提供合同与图约束，但升级为 certification report 前仍需独立 metric review。
- 因此两个 DashScope Profile 必须继续保持 `EXPERIMENTAL`，AI 默认关闭；即使小样本 canary 全绿也不得变更该状态。

## 再锚定

- 下一节点 T5-4 只在两条 DEV JSON 合成 fixture 上各调用一个模型，用于证明真实传输、provider、Candidate、持久预算与小样本 scorer 已闭环。
- T5-4 不修改 Prompt/Profile，不扩大数据、次数或费用；任何失败都保留安全 telemetry 并停止自动扩张。
