# P5 / T5-4 checkpoint — 限定预算的 DashScope 双模型 synthetic canary

- 日期：2026-08-08
- 分支：`phase/p5-live-evaluation`
- 基线：`41be49b`
- AC：AC-015、AC-020；为 AC-021 提供真实通路证据，不构成认证
- 生命周期：`live_canary_verified`（J1 + 本机 A1）；`certificationComplete=false`

## 授权与执行边界

- 用户显式授权 Provider=DashScope，模型=`qwen3.7-flash` / `qwen3.8-max`，输入仅仓库合成数据，最多 6 次 provider attempt、累计费用不超过 ¥1。
- 执行前 fail-closed 预检：两 Profile 最坏 3+3 次，最大预留 900,000 微元（¥0.90）；Key 只检查“已配置”，未读取或输出值。
- 输入仅 `json-01-scalars` 与 `json-02-nested-object` 的 value-free structural profile；未发送客户或真实业务数据。
- 只执行了一次 gated Maven canary run；没有重跑。实际 2 次 attempt 后成功，其余 4 次/预算未使用且不构成后续调用授权。`plans/live-canary-authorizations/p5-20260808.json` 已标记 `CLOSED`，canary 测试在任何数据库清理或网络动作前强制检查该状态。

## 结果

| Profile / model | Case | Attempts | Tokens in/out | 估算费用 | 状态 | 结构评测 |
|---|---|---:|---:|---:|---|---|
| `dashscope-qwen37-flash-v1` / `qwen3.7-flash` | `live-json-01-scalars` | 1 | 820 / 941 | ¥0.000917 | `REVIEW_REQUIRED` | schema 1/1；root precision/recall/type/evidence/optionality 100% |
| `dashscope-qwen38-max-v1` / `qwen3.8-max` | `live-json-02-nested-object` | 1 | 822 / 1201 | ¥0.053100 | `REVIEW_REQUIRED` | schema 2/2；root precision/recall/type/evidence/optionality 100% |

- 合计：2 attempts，1,642 input tokens，2,142 output tokens，54,017 微元 = **¥0.054017**。
- 两条结果分别保留 3 / 2 个 review blocker；这是 Candidate 必须由人确认的预期安全行为，不影响小样本结构 scorer 通过。
- harness failure 为 null；没有 retry/repair、模型不匹配或 provider failure。

## 证据与判定

| Gate | 保证等级 | 命令/来源 | 结果 | 原始证据 |
|---|---|---|---|---|
| live preflight | J1 / A0 | gated shell；不输出 Key | configured；6 attempts / 900,000 微元最坏预留在授权内 | 当前会话工具输出 |
| DashScope canary | J1 / A1 | `RENDERWEAVE_RUN_LIVE_CANARY=true` + targeted Maven test | 1 test passed；2 real attempts；¥0.054017 | `.sdlc/evidence/20260808-114257-p5-live-canary/summary.json` |
| safe summary audit | A1 | 只读取 summary telemetry | synthetic-only、2 results、0 harness failure、`certificationComplete=false` | 同上 |
| final server regression | A1 | 子进程显式移除 canary gate 与 Key；`tools/run-gate.ps1 -Gate server` | 140 tests，0 failure/error，1 paid test skipped；V001–V008 green；0 additional provider call | `.sdlc/evidence/20260808-194427-server/metadata.json` |

## 安全、恢复与下一步

- 证据不含 Key、prompt、输入值、provider 原始响应或 Candidate JSON，只含 Profile/case、有限 telemetry 与结构指标。
- 外部调用和费用不可撤销；恢复策略是停止后续 attempt。该节点已关闭，不会为“用完剩余额度”而追加调用；Compose overlay 的 worker 与 upload 两个门也均保持 false。
- 两条成功样本只证明上传→归一化→DashScope→strict Candidate→deterministic validate→review→eval 的真实闭环。
- 60-case corpus 仅有 2 条真实输出，且 certification metric surface 仍待独立完善；`qwen3.7-flash` 与 `qwen3.8-max` 必须继续为 `EXPERIMENTAL`，不能成为默认生产 Profile。
