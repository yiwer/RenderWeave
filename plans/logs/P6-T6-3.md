# P6 / T6-3 checkpoint — 产品 live 运行与恢复切片

- 结果：T6-3a 已完成 clean full A1；T6-3b 恢复/存储故障演练仍为 `pending`
- AC：AC-015、AC-019、AC-020、AC-024
- 生命周期状态：T6-3a `automated_verified`；T6-3 整体 `in_progress`
- 分支：`phase/p6-inference-product-acceptance-v1`
- 实现 revision：`a3c54d21a23aee049a55456382099fa641e3e316`
- 门控修正 revision：`cbd9bdfe0eee2fe633cb1cf3fee3dbcadcaa8ba8`
- evidence repair / Product v2 revision：`ad68481b4c15b7d7882d6bed42cb9b4c25666f8c`
- upload gateway revision：`eae3d10`
- oversized image normalization revision：`9008e9dd55fc804d887184a8b2e540a1f0175a10`
- 用户授权：2026-08-10 明确要求开放产品运行、允许成本限额可填或不填，并固定四个产品模型
- Provider 外部副作用：本节点实现、部署与验证均未发起 DashScope 请求；product reservations=0、费用 ¥0

## T6-3a 产品运行合同

- 产品目录只展示 `qwen3.7-flash`、`qwen3.7-plus`、`qwen3.8-max`、
  `qwen3.7-max-2026-06-08`；旧的评测 Profile 和不可变 CLOSED 账本继续保留，但不进入产品选择器。
- live availability 不再依赖已消耗的历史 canary budget；必须同时满足 worker、上传门和凭据可用。
  基础 Compose 仍 fail-closed，只有显式叠加 `compose.live.yaml` 才开启产品运行。
- 每个任务可选择 1..100,000,000 micros CNY 的累计成本限额，也可不设置。不设置只是不增加
  per-run 累计门；Profile 的保守预留、输出上限和最多三次调用仍生效。
- 所有真实上传均为 `USER_PROVIDED`，创建任务前要求用户分别确认数据外发与模型处理；retry 继承原任务限额。
- V013 增加 nullable `cost_limit_micros_cny`，并将产品 reservation 放入独立、append-only 的
  `product-live` 命名空间，不能借用任何 P5 CLOSED ledger 的额度。

## 验证证据

| Gate / 检查 | 保证等级 | Revision | 结论 | 原始证据 |
|---|---|---|---|---|
| full | A1 | `cbd9bdfe0eee2fe633cb1cf3fee3dbcadcaa8ba8` | PASS；Server 129 passed / 3 gated skip；Web 54 passed + production build；offline eval 37 passed；runtime、prototype、Draft 与 inference 浏览器旅程通过 | `.sdlc/evidence/20260810-103851-full/metadata.json` |
| product-live readiness | A0 tool-observed | `cbd9bdfe0eee2fe633cb1cf3fee3dbcadcaa8ba8` | 本机 overlay 启动；enabled/configured/uploadEnabled=true；四个模型精确匹配；runCostLimitRequired=false | 本会话只读 readiness probe |
| side-effect audit | A0 tool-observed | `cbd9bdfe0eee2fe633cb1cf3fee3dbcadcaa8ba8` | active runs=0；`product-live` reservations=0；schema version=013；Agent 未调用 Provider | 本会话只读 PostgreSQL probe |

完整 gate 在子进程内强制清空 Key/key-file/canary/cert 环境变量，并关闭 live AI/upload，因而不会把
自动测试误当成真实模型验收。该结果是本地 A1，不是独立 A2 或外部强制 A3。

## 恢复、残余与下一切片

- 源码恢复可 revert `a3c54d2` 与 `cbd9bdf`；V013 是 forward-only nullable 迁移，不通过破坏性 down
  migration 回滚。要关闭外发，用基础 Compose 重新创建 API，不删除数据库或 Blob。
- 当前官方视觉/价格资料明确覆盖 Qwen3.7 Flash/Plus/Max dated；精确别名 `qwen3.8-max` 按用户要求保留，
  但尚未在当前官方模型目录得到同等明确确认，故保持 `EXPERIMENTAL`。若供应商拒绝该 model id，任务应
  稳定失败并进入可读重试路径，不静默降级到其他模型。
- T6-3b 仍需执行数据库与 Blob 备份/恢复、missing artifact、storage full 和操作员观测演练；在完成前
  T6-3 仍是 `in_progress`，不能据 T6-3a 宣称发布候选已完成。

## T6-3a.1 assessment evidence repair 与 Product Profile v2

### 用户触发失败的有界诊断

- 用户触发的 IMAGE_ONLY 任务完成了 3 次 DashScope 调用，但三次 Provider 输出都在 Candidate contract
  解码边界被拒绝；有限 taxonomy 均为
  `CANDIDATE_DECODE_CONSTRUCTOR_INVALID_ASSESSMENT_EVIDENCE`，最终状态为
  `FAILED / LIVE_REPAIR_BUDGET_EXHAUSTED`，没有生成 Candidate。
- 三次实际费用分别为 123,792、134,472、137,244 micros CNY，合计 ¥0.395508；对应保守预留分别为
  448,920、449,256、449,256 micros CNY，均低于原 Profile 上界。因此原始失败不是排队、网络、模型别名
  或成本门造成，而是模型持续返回缺失或 `null` 的 `assessment.evidence`。
- Worker 虽已把精确 decoder code 写入 payload-free attempt taxonomy，但旧实现随后把 parse-invalid checkpoint
  退化为通用 `LIVE_STRUCTURE_OUTPUT_INVALID`；REPAIR 看不到真实槽位，三次输出重复同一结构错误。
- 本记录不保存 run id、Provider request id、原始图片、Prompt、完整模型输入输出或 Candidate payload。

### 版本化修复

- `LiveInferenceWorker` 在 contract decode 失败时把精确、有限、长度受控的 BLOCKER 写入 checkpoint；
  deterministic validation 优先复用该问题，只有旧 checkpoint 无诊断时才回退通用 code。
- Prompt 4 明确 `assessment` 只有 `confidenceBps`、`inferred`、`resolution`、`evidence` 四个成员，
  `assessment.evidence` 必须始终存在且为 JSON array，不允许省略或为 `null`，并为上述精确 code 提供 repair 指令。
- 新建任务只展示四个不可变 Product Profile v2，分别绑定 `qwen3.7-flash`、`qwen3.7-plus`、
  `qwen3.8-max`、`qwen3.7-max-2026-06-08`；旧 product-v1 资源保留用于历史快照解析，但不再接受新建。
- 四个 Product v2 Profile 的 `maximumEstimatedCostMicrosCny` 统一为 2,000,000（¥2）。这只是单次调用的
  保守预留硬门，不替代用户可选的任务累计成本限额；极端高分辨率请求若保守估值超过 ¥2，会在调用前拒绝。
- 历史失败任务重试仍继承其 product-v1 Profile 快照和原任务成本限额，但会使用新的 Worker 精确反馈；
  新建任务才使用 Prompt 4 / Product v2。Agent 未替用户自动重试，因此真实模型修复效果尚未声明为 live-proven。

### 验证证据

| Gate / 检查 | 保证等级 | Revision | 结论 | 原始证据 |
|---|---|---|---|---|
| server | A1 | `ad68481b4c15b7d7882d6bed42cb9b4c25666f8c` | PASS；包含真实 PostgreSQL workflow 回归，证明 `evidence=null` 后 REPAIR 收到精确 decoder code 并可进入审核；paid/live tests gated skip | `.sdlc/evidence/20260810-110522-server/metadata.json` |
| web | A1 | `ad68481b4c15b7d7882d6bed42cb9b4c25666f8c` | PASS；generated client、四个 Product v2 选项与 Node 24 production build 绿色 | `.sdlc/evidence/20260810-110622-web/metadata.json` |
| e2e | A1 | `ad68481b4c15b7d7882d6bed42cb9b4c25666f8c` | PASS；16 passed，1 个真实 paid/replay 场景按门控 skip | `.sdlc/evidence/20260810-110658-e2e/metadata.json` |
| redeploy readiness | A0 tool-observed | `ad68481b4c15b7d7882d6bed42cb9b4c25666f8c` | enabled/configured/uploadEnabled=true；四个可见 Profile 均为 Product v2 且上界 2,000,000 micros CNY | 本会话只读 readiness probe |
| post-deploy side-effect audit | A0 tool-observed | `ad68481b4c15b7d7882d6bed42cb9b4c25666f8c` | active runs=0；历史 reservation=3；部署时间之后新增 reservation=0 | 本会话只读 PostgreSQL probe |

当前结论为 `automated_verified`：结构错误已由确定性回归覆盖且新版本已部署，但没有把零 Provider A1
表述成真实 DashScope 质量认证。T6-3b 的恢复/存储故障演练仍保持 `pending`。

## T6-3a.2 multipart 网关边界

- 用户上传的 multipart 请求体为 1,309,592 bytes；Nginx 在 Java 之前按默认 1 MiB
  `client_max_body_size` 返回 HTML 413。前端对响应直接调用 `response.json()`，因此二次表现为
  `Unexpected token '<'`。API 日志没有对应请求，排除了 Spring 34 MiB 与领域 10/30 MiB 边界。
- Nginx `/api/` 上限改为 35 MiB，略高于 Spring `max-request-size: 34MB`，使应用保持稳定的
  `INFERENCE_PAYLOAD_TOO_LARGE` 合同；超过网关硬边界时，named error location 也返回
  `application/problem+json`。
- live multipart 客户端改为先读取 text 再有界解析 JSON；HTML/纯文本 413 被转换为可读的
  `StudioRequestError`，不再向用户泄漏 JSON parser TypeError 或网关 HTML。
- clean Web A1：revision `eae3d10`，55/55 tests、typecheck、lint 与 Node 24 production build PASS；
  evidence 为 `.sdlc/evidence/20260810-114334-web/metadata.json`。
- 真实部署探针：`nginx -t` PASS；1.25 MiB 合成 multipart 穿过网关并由 Java 返回 JSON 400；
  36 MiB 合成 multipart 返回 JSON 413 / `INFERENCE_PAYLOAD_TOO_LARGE`。探针前后数据库均为
  5 个 inference run、3 个 reservation，因此新增任务=0、新增 Provider 调用=0。

当前修复已部署，生命周期为 `automated_verified`；用户仍需在浏览器中重新提交原文件，才能形成真实
Product v2 运行与 Candidate 审核结果。

## T6-3a.3 超大设计图有界规范化

- 用户原图通过 Nginx 后由 Java 返回 422 / `INFERENCE_IMAGE_DIMENSIONS_INVALID`；数据库保持
  runs=5、active=0、reservations=3，证明它尚未创建任务或调用 Provider。
- 4096 从源图拒绝门改为持久化/Provider 规范化目标。源图新增最长边 65535、总像素 268435456
  的独立安全硬门；预计完整解码超过 20000000 pixels 时先应用等比 source subsampling，再以 bicubic
  缩放到最长边 ≤4096。PNG/JPEG magic、动画拒绝、10/30 MiB、EXIF、sRGB 与去 metadata 合同不变。
- artifact width/height、Provider 输入、费用最坏估值与 Candidate evidence 坐标继续统一绑定规范化产物；
  不保存原始图片，也不扩大单次 ¥2 或三次调用边界。
- unit/targeted：InputNormalizer 13/13；真实 PostgreSQL Live API 4/4，4097×1 源图创建后数据库 artifact
  为 4096×1，并进入合成 Provider 的 REVIEW_REQUIRED；这些测试清除 live Key，未访问 DashScope。
- exact clean A1 revision `9008e9dd55fc804d887184a8b2e540a1f0175a10`：Server 131 passed / 3 gated
  skip，证据 `.sdlc/evidence/20260810-115533-server/metadata.json`；Web 55/55 + Node 24 build，证据
  `.sdlc/evidence/20260810-115533-web/metadata.json`。
- 部署后使用已有幂等键提交合成 4097×1 PNG：返回 409 / `INFERENCE_IDEMPOTENCY_CONFLICT`，而不再返回
  dimensions 422，证明请求已越过归一化边界；探针前后 run/artifact/reservation 均为 5/7/3，零任务、
  零 Provider、零费用。

当前生命周期为 `automated_verified`，不是原始用户图片的 live 结果。用户重新提交后才会产生新的 Product v2
任务，并按现有真实调用授权进入审核或可读失败状态。
