# TV1-T31：Editor E4b StaticSchema 与 child-fill 语义依赖校验

- 结果：完成
- 生命周期状态：`automated_verified`
- 验证时 revision：`8d517b2` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\Yiwer\code\RenderWeave-template-v1-implementation`
- 外部副作用：Provider attempts=0、API Key reads=0、open authorization=0；未发送真实数据，未调用付费外部模型；
  只运行仓库 gate、Testcontainers PostgreSQL 与 pinned Node 24；未 push/tag/PR

## 产品增量

1. Template 新增唯一 E4b semantic walker，只消费 admitted canonical DesignDSL、exact immutable
   `SchemaDefinition` 与 child current facts。StaticSchema path 大小写敏感并执行 RFC 6901 解码；允许逐层穿越 exact
   Static reference，拒绝 array 数字下标/wildcard，不使用通用 JSON Schema validator。
2. 每个 context source 形成 closed scalar/list/reference/type/presence proof。definitions、Expression inputs、Mapping
   input/result、node bindings、Repeat items、Conditional condition 与 TemplateUse selector/fills 共用同一解释；合法
   optional ancestor 形成 `MAY_BE_ABSENT`，不存在路径不会伪装成 runtime ABSENT。
3. Repeat items 在父 lexical scope 求型，child 只可见自身和祖先 loop。五种 scalar list 映射到 exact
   `system-basic-{text|decimal|date|time|boolean}@v1`，reference array item 保留 exact StaticSchemaRef，不做 shape 推断。
4. Binding bindability 仍由 `BindingPolicyCatalog` 持有，永久 Node Property ValueType 由 `NodeContractCatalog` 持有。
   Schema 驱动的 path/type/presence drift 是可确认 dependency ERROR；纯 authored type/domain 错误是 hard。
5. TemplateUse 重新 admission child current canonical bytes 并核对 contentHash；empty/context selector 必须精确匹配
   child 永久 StaticSchema，每个 fill 必须命中 child current PUBLIC CustomDefinition 且 source 同型。省略 fill 与合法
   runtime ABSENT 仍使用 child default。
6. 新问题复用 T30 的 bounded report、五分钟 opaque confirmation、fresh revalidation 与 SERIALIZABLE snapshot fence。
   App authority 解析 StaticSchema 不可变 DSL 并读取 child canonical bytes；无 migration、token wire、OpenAPI、SDK、
   route 或 Web placeholder。

## TDD、审查与证据

- Template pure scenarios 覆盖 scalar/reference/array/optional/nested path、非法 array traversal、Repeat scalar/reference
  item context、ancestor/self/out-of-scope lexical domain、Mapping/Conditional/binding type/presence、selector exact schema、
  child missing/PRIVATE/type drift、child integrity，以及 UTF-8 stable ordering/201-item budget；Template 最终 78 tests。
- App PostgreSQL focused 10/10 覆盖真实 system StaticSchema type drift 的 confirmation→INVALID 与 child current PUBLIC
  definition drift 的 parent readiness recheck；完整 server 为 App 344 tests、0 failures、15 controlled skips。
- Web generic confirmation problem 使用 E4b code/pointer 回归；正式 Node 24 为 19 files/127 tests，SDK generation、
  typecheck、lint 与 2144-module production build 全绿。`git diff --check` 通过。

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Template | A1；kernel exact replay 为 A2 | Schema 20、Template 78；Java/Python kernel 211/211；static registry replay byte-identical | `.sdlc/evidence/20260821-140538-template/` |
| Server | A1 | 8/8 reactor；App 344 tests、0 failures、15 controlled skips | `.sdlc/evidence/20260821-134445-server/` |
| Web | A1 | pinned Node 24；19 files/127 tests；SDK/typecheck/lint/2144-module build 通过 | `.sdlc/evidence/20260821-135643-web/` |
| Fast | A1 | repository diff、八模块 package 与 Web typecheck 全过 | `.sdlc/evidence/20260821-135722-fast/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在提交交接报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |

## 合同 identity 与保证上限

- API contract 保持 `0.15.0`，runtime migration count 保持 25；StaticSchema 内容与已编译 JSON Schema 未修改、删除或
  重编译，正式 Schema 不含 fieldId，Candidate 局部 ID 未进入 Draft/Static/compiled artifact。
- T30+T31 只证明 Editor E4 dependency-save semantics；没有 E5 reconciliation、E6 preview、E7 recovery、E8 import、
  E9 完整 locator/a11y、copy/restore confirmation 或公开产品 route。
- 没有 Renderer/Profile/formal Case/Oracle、物理 Linux、J1/A3 或 READY 升级；Profile 仍 NOT_REGISTERED，raster/output
  仍不从本票推导。
- rollback 边界为本票单一 verified commit；无 migration 或外部持久状态需逆转。下一 frontier 必须按真实依赖另票
  登记；未经授权不 push/tag/PR。
