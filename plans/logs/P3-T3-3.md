# Checkpoint：P3-T3-3 / G-P3-STUDIO

- 结果：完成
- AC：AC-003、AC-005、AC-007、AC-012、AC-014
- 生命周期状态：`automated_verified`
- 摘要：交付正式 Draft/StaticSchema/RootDocument 四类资源页面与完整生命周期操作；409 冲突保留本地并展示结构化差异；真实 PostgreSQL 浏览器旅程覆盖历史恢复、软删除撤销、发布、复制与 exact Static 验证。

## 验证证据

| Gate | 保证等级 | Revision | 命令/来源 | Exit/结论 | 原始证据指针 | captured_by | 新鲜度 |
|---|---|---|---|---|---|---|---|
| server | A1 | `UNBORN` | `tools/run-gate.ps1 -Gate server` | 0；Java 58 tests，含 Draft list/历史/删除恢复/Static/validation PG 契约 | `.sdlc/evidence/20260808-033313-server/metadata.json` | project gate | exact manifest |
| Web production | A1 | `UNBORN` | `tools/run-gate.ps1 -Gate web` | 0；OpenAPI 0.6.0 regenerate、typecheck、lint、14 tests、production build | `.sdlc/evidence/20260808-033537-web/metadata.json` | project gate | exact manifest |
| browser + axe | A1 | `UNBORN` | `tools/run-gate.ps1 -Gate e2e` | 0；7 Playwright tests；生命周期、冲突、lossless artifact、1024/1280、axe serious/critical 0 | `.sdlc/evidence/20260808-033128-e2e/metadata.json` | project gate | exact manifest |
| G-P3 real PG/API/browser | A1 | `UNBORN` | `tools/run-gate.ps1 -Gate draft-e2e` | 0；Java 58 + Web 14；revision 0→1→restore 2→delete/restore 3→publish→copy→Static validation | `.sdlc/evidence/20260808-033646-draft-e2e/metadata.json` | project gate | independent fresh DB per run |

- `.sdlc/evidence/20260808-033008-e2e` 首次捕获两个问题：测试定位器歧义，以及删除恢复后旧 Draft cache 可先初始化编辑器；通过 exact dialog locator、mutation 成功时写入 exact query cache、按 schemaKey remount workspace 修正，随后两次 E2E 绿色。
- 视觉 J1：人工查看 mocked 与真实 PostgreSQL 的 Draft list、Static detail、validator、1024 editor screenshots；延续用户已接受的暖色 editorial 高密度方向，无未来 Template/Render 占位入口。

## 关键结果

- 新增 active Draft 分页合同与 PostgreSQL 查询；删除 Draft 默认从列表隐藏，删除成功后提供即时“撤销删除”，恢复复制旧 snapshot 为新 revision。
- Lifecycle bar 提供 history preview/restore、Draft copy、发布 readiness、soft delete；所有 graph-changing 操作携带 expected revision，成功后同步 React Query exact cache。
- 409 `REVISION_CONFLICT` 异步读取 authoritative server snapshot，本地会话不覆盖；按 metadata、field add/remove/change/reorder 展示结构化 local/server diff，并提供导出本地或显式载入服务端。
- StaticSchema list/detail 展示系统与用户发布物、不可变元数据、release note、精确 definition/compiled artifact、复制与下载；使用 `lossless-json` 做可读格式化，128 位 decimal 不经原生浮点解析。
- RootDocument validator 支持 Draft current / Static exact、1..20 raw JSON documents、稳定 diagnostics 与 resolved graph；请求体用字符串 envelope 构造，保留原始 number token。
- `/schemas`、`/schemas/new`、`/schemas/:key`、`/static-schemas`、exact detail、`/validator` 全部 lazy route 可达；低于 1024 显示明确 desktop-only 状态。
- 真实浏览器/PG 审计验证 unknown field 在默认 `additionalProperties=true` 下通过，且 Static copy 生成独立 revision 0 Draft。

## 重要取舍

- 精确 Definition/compiled artifact 继续走 raw text boundary；generated SDK 只承载不会因 JS number 丢精度的 summary/history 合同。
- 软删除恢复入口通过一次性 router state 呈现，但 authoritative restore 仍由服务端 tombstone/revision/graph transaction 决定。
- 不增加数据库 UI 偏好或坐标状态；不进入 Template、Workspace、Renderer、live AI 或付费调用。
- 本地仓库仍为 unborn/record-only，故不宣称 A2/A3；P4 计划中的 A2 target 在当前 harness 下只能诚实收敛为 A1，除非后续引入独立 verifier。
- 是否需要 spec delta：否。
- 再锚定是否发现漂移：否。

## 多 Agent

- 未启用；single writer，`atomic_claim:none`。

## 下一步

- 下一任务或恢复入口：P4/T4-1，建立零网络 replay inference 的输入归一化、BlobStore、durable run、lease/checkpoint/cancel/retry 基线。
- 需要用户决定：无；P5 live provider 仍在未授权边界外。
