# P6-T6-5 N6：有界视觉语义验证、阶段修复与可审核 telemetry

- 结果：实现与自动门控完成，live 诊断未晋级
- 生命周期状态：`automated_verified`
- AC：AC-VR-007、AC-VR-009
- 主要实现 revisions：`4290227`、`f0ebe77`、`d5afadf`
- N7 实证驱动的 verifier/observation 延伸：`195894b`、`bb15096`、`31a8c6f`、`4d2cc46`、`214fff9`、`391bd52`、`dda763c`、`edc0c28`、`e13bf0c`、`061101f`、`f8f09b4`、`2b6eb9c`、`6cb2624`、`d3b0292`、`5ef25bd`、`676180a`、`e1f1a9d`、`3a56af9`
- full audit selector 修复：`de97131`
- 分支：`phase/p6-visual-recognition-vnext`
- 决策：ADR-0028
- 终态：全部 Flash/Plus/Max visual ledger `CLOSED`；所有 vNext Profile 继续隐藏 `EXPERIMENTAL`；v27
  source-ancestor verifier、真实 PostgreSQL tracer、监控/审核 UI 与三模型 single-case live 已分别 A1/A2，
  但新增 telemetry 未获 live 命中，final eval 与验收门未满足

## 恢复与治理再锚定

接管时仓库位于用户指定分支和 clean revision `b9ef21d4803bd567f09a03964dba39ecfcd6114d`，三份 visual ledger
均为 `CLOSED`，且没有遗留 live/Maven 进程。编排层 `get_goal` 返回空，与 handoff 的 paused Goal 描述漂移；
按用户要求没有创建 replacement Goal。后续以本计划、ledger/evidence、git history 与 checkpoint 为权威恢复事实，
每个可验证节点独立提交，不 squash、不改写历史、不 push、不 tag。

## 可验证增量

1. `renderweave-visual-semantic-verifier/1.0` 在 OBSERVE、HIERARCHY、ELEMENT_BINDING 边界执行有限语义检查：
   repeated-group/cardinality/ownership、GROUP 与 relationship 一一对应、字段绑定到覆盖其区域的最近实体。
2. verifier 只输出固定 problem code 和最早失败阶段；已通过 checkpoint 保留，repair 只重做最早失败阶段。
   retry diagnostics 按 stage 去重、排序并最多保留 16 个，防止后一次修复遗忘已有约束。
3. selected crop 只从已验证 grounding checkpoint 派生，单次最多 4 个；派生图片、局部 ID、坐标、OCR、
   Prompt、Provider response 与 Candidate 不进入常规日志或 evidence。
4. PostgreSQL 回归覆盖同阶段修复、checkpoint 保留、crash/retry/cancel；immutable v10/v11/v12 Profile/Prompt
   资源新增而不改写历史 snapshot。
5. 监控与审核复用 payload-free execution telemetry，展示阶段/checkpoint、受控区域类别、固定 issue code、
   token/费用/延迟与恢复状态。1024 宽度下通过 keyboard、focus、overflow 和 axe serious/critical 检查；
   real-PG E2E 保持 apply 为唯一 create-only 写边界。

## Flash 单 case smoke

数据仅为仓库合成 `transit-board-v3`。每轮都在精确 clean evaluation identity 与 Profile snapshot 下执行
PROPOSED → 负探针 → OPEN → CLOSED，单 case、最多 5 attempts；没有调用 Plus 或 Max。

| 授权 / Profile | attempts | input + output tokens | 到达阶段 | 最终固定问题码 | A2 |
|---|---:|---:|---|---|---|
| `visual-v10-semantic-smoke-qwen37-flash-transit-20260811` / product-v10 | 5 | 8,542 + 23,544 = 32,086 | OBSERVE | `VISUAL_GROUNDING_REGION_FOREST_INVALID` | PASS |
| `visual-v11-stage-local-smoke-qwen37-flash-transit-20260811` / product-v11 | 5 | 10,236 + 22,661 = 32,897 | OBSERVE | `VISUAL_SEMANTIC_REPEATED_GROUP_ELEMENT_MISSING` | PASS |
| `visual-v12-accumulated-repair-qwen37-flash-transit-20260811` / product-v12 | 5 | 11,712 + 24,555 = 36,267 | OBSERVE | `VISUAL_GROUNDING_PARENT_KIND_INVALID` | PASS |

- 每个 PROPOSED 负探针均以 `NOT_OPEN` 退出，没有创建 evidence，Goal state/hash 和 provider attempts 不变。
- 三次独立 verifier 均为 PASS、0 abandoned、payload scan PASS；每次 live 后先 CLOSED 再检查 evidence。
- 15 次 attempt 全部为 OBSERVE rejection。v12 证明 bounded accumulated diagnostics 能从 enum 继续暴露
  parent-kind/cardinality，但最后仍未越过 OBSERVE；A2 证明授权、用量和 evidence 闭环，不证明模型质量。
- 因三阶段合同不可达，Max 的条件授权入口没有成立；在该 N6 freeze 时 Plus 遵守 N5 决策未调用。后续用户
  J1 delta 已重新允许 Plus，并两次给每个稳定槽位追加 500,000 tokens，当前累计 cap 为 1,500,000；详见
  N7 checkpoint。历史 N6 证据与 ledger 没有改写。

N6 freeze 时累计：Flash 71 attempts、393,034/500,000 exposed tokens、¥0.169035；Plus 86、
485,886/500,000、¥2.040696；Max 73、428,816/500,000、¥9.204720。三份模型 ledger 全部 `CLOSED`。

## N7 驱动的 bounded verifier 延伸

Plus v14 A2 实证发现 OBSERVE 可在 0 GROUP 时合法通过，而 HIERARCHY 随后提出 relationship，造成只能由上游
omission 解释的跨阶段矛盾。`195894b` 新增固定码 `VISUAL_SEMANTIC_OBSERVE_RELATIONSHIP_GROUP_MISSING`，并把
最早失败阶段路由到 OBSERVE；只有 REJECTED HIERARCHY + 该精确单码允许事务性写入 NORMALIZE checkpoint，
保留 provider call 计数并清空下游 plan/Candidate。通用状态机仍严格前向，合法的零关系扁平 hierarchy 仍被接受。

immutable element prompt v7/product-v15、监控问题标签与 1024 E2E 同步完成。`195894b` exact-clean fast
`.sdlc/evidence/20260811-035530-fast`、server `.sdlc/evidence/20260811-035542-server`、web
`.sdlc/evidence/20260811-035542-web`、E2E `.sdlc/evidence/20260811-035710-e2e` 均 PASS；该延伸没有 Provider
调用。后续 Plus v15 smoke 仍未触达 BINDING；当前 Goal 累计为 Flash 428,373、Plus 579,967、Max 428,816
exposed tokens，三份 ledger 仍 CLOSED。

Plus v15 连续两次命中 relationship support cardinality mismatch 后，`bb15096` 新增 pipeline 4.3/product-v16：
每条 relationship 必须恰好由一个已知 GROUP 支撑，内部 cardinality 从该 GROUP 的已验证 multiplicity 派生；
多支撑、未知或非 GROUP 仍固定码拒绝。旧 4.1/v15 保持 model-asserted cardinality，不改写历史 snapshot。
semantic verifier 仍只拒绝；派生发生在确定性 codec，并仅向成功 attempt 写 payload-free 计数。真实 PostgreSQL
脚本 provider 已完成 OBSERVE→HIERARCHY→BINDING/Candidate，证明本地合同可达，但不代表 live 模型可达。

后续 Plus product-v16 live A2 中，OBSERVE 得到 10 SLOT/1 GROUP，HIERARCHY 三次在 support group reused 与
support IDs invalid 间失败，仍未触达 BINDING。cardinality mismatch 已不再出现。数量差不足以证明 OBSERVE
遗漏，因为多余 relationship 也可能由 HIERARCHY 删除；该初步 capacity 假设已否决。`31a8c6f` 随后用 pipeline
4.4/product-v17 收窄为 exact relationship region 已是 GROUP/REPEATED_GROUP、却没有 GROUP element owner 时才
回到 OBSERVE；已有 owner 的 GROUP reuse 仍留在 HIERARCHY，且从不复制 GROUP。Plus Goal 保持 106 attempts、
607,449 tokens、¥2.517864，三份 ledger CLOSED。

后续 Plus product-v17 live A2 中，OBSERVE 首次接受并记录 20 SLOT/1 GROUP；四次 HIERARCHY 依次为三次
region ownership invalid 与一次 support not group，exact-owner rewind 没有触发，仍未进入 BINDING。独立 verifier
重建 5 attempts、27,498 input + 7,733 output tokens、142,447 ms 与 payload scan PASS。Plus Goal 现为
111 attempts、642,680 tokens、¥2.634724；三份 ledger CLOSED，Max 未调用。

`4d2cc46` 随后新增 pipeline 4.5/product-v18，把 v17 generic region ownership failure 拆为六类固定原因，并在
hierarchy prompt v6 中为这些 code 与 support-not-group/reuse 提供 stage-local repair。旧 Profile 继续
`LEGACY_GENERIC`；新结构码不触发 crop，不自动补 GROUP 或改写已验证 OBSERVE。真实 PostgreSQL 测试证明
checkpoint 保留 OBSERVE/grounding 与 provider-call 计数，HIERARCHY 原地拒绝，并在 lease-expiry 后只重做
HIERARCHY 再进入 BINDING/Candidate。

后续 Plus product-v18 live A2 中，OBSERVE 首次接受并记录 9 SLOT/0 GROUP；四次 HIERARCHY 均为
relationship support IDs invalid，未命中新的 region taxonomy，也未进入 BINDING。独立 verifier 重建 5 attempts、
20,274 input + 4,920 output tokens、89,402 ms 与 payload scan PASS。Plus Goal 现为 116 attempts、667,874 tokens、
¥2.714632；三份 ledger CLOSED，Max 未调用。

`214fff9` 随后新增 pipeline 4.6/product-v19。旧 Profile 保持 strict；新 Profile 只对已逐项验证的同一
relationship support ID 做 stable exact dedupe，并将 missing/empty/limit/invalid 拆成固定码。不同有效 ID、缺失
GROUP、owner/结构冲突仍 fail-closed，代码不择一、不补元素、不删边；结构问题不触发 crop。真实 PostgreSQL
纵切证明精确重复可归一化、HIERARCHY 接受后进入 BINDING，并在 lease-expiry 后只重做 BINDING。

后续 Plus product-v19 live A2 中，OBSERVE 首次接受并记录 13 SLOT/1 GROUP；四次 HIERARCHY 均为
relationship region/cardinality invalid，未命中 support-ID normalization telemetry，也未进入 BINDING。独立
verifier 重建 5 attempts、24,956 input + 6,103 output tokens、109,700 ms 与 payload scan PASS。Plus Goal 现为
121 attempts、698,933 tokens、¥2.813368；三份 ledger CLOSED，Max 未调用。

`391bd52` 随后新增 pipeline 4.7/product-v20。旧 Profile 保持 strict；新 policy 仅在 relationship 当前引用已知但
cardinality-incompatible region 时，从其唯一有效支撑 GROUP 的已验证 owned regions 中过滤 compatible region。
恰有一个才确定性归一化并记录 `VISUAL_HIERARCHY_RELATIONSHIP_REGION_NORMALIZED`；零个输出
`VISUAL_SEMANTIC_GROUP_REGION_INVALID` 并回到 OBSERVE；多个继续由既有 cardinality fixed code fail-closed。
代码不处理未知 region，不跨 GROUP 选择，不修改 endpoint/topology，不补元素或删边；结构问题保持 no-crop。
真实 PostgreSQL 纵切证明 support-ID 与 region 同时归一化后进入 BINDING，lease-expiry 恢复只重做 BINDING。

后续 Plus product-v20 live A2 中，OBSERVE 首次接受并记录 12 SLOT/1 GROUP；四次 HIERARCHY 依次为
support-not-group、两次 relationship-region-connection-invalid、support-IDs-empty，未命中 region normalization
telemetry，也未进入 BINDING。独立 verifier 重建 5 attempts、24,251 input + 6,086 output tokens、109,414 ms 与
payload scan PASS。Plus Goal 现为 126 attempts、729,270 tokens、¥2.910558；三份 ledger CLOSED，Max 未调用。

`dda763c` 随后新增 pipeline 4.8/product-v21。旧 Profile 保持原语义；新 policy 在当前已知 relationship region 的
cardinality 或 parent/child connection 不成立时，只检查唯一支撑 GROUP 已验证的 owned regions，并要求候选同时
满足 cardinality 与 connection。恰有一个才确定性归一化并复用 payload-free telemetry；零/多 combined 候选
保留既有 detailed HIERARCHY fixed code，零 cardinality 候选仍回 OBSERVE。代码不跨 GROUP、不排序、不改
endpoint/topology，也不补删结构。真实 PostgreSQL 纵切证明归一化后进入 BINDING，lease-expiry 恢复只重做
BINDING；这仍是 repository synthetic 合同证据，未调用 Provider。

后续 Plus product-v21 live A2 中，前两次 OBSERVE 分别因 parent-kind/element fixed code 被拒，第三次接受；
两次 HIERARCHY 均为 support-not-group，未命中 region normalization telemetry，也未进入 BINDING。独立 verifier
重建 5 attempts、18,715 input + 8,810 output tokens、164,553 ms 与 payload scan PASS。Plus Goal 现为
131 attempts、756,795 tokens、¥3.018468；三份 ledger CLOSED，Max 未调用。

`edc0c28` 随后新增 pipeline 4.9/product-v22。旧 Profile 保持原语义；新 policy 只在 relationship support
经验证、去重后恰有一个已知非 GROUP element，且 exact relationship region 是已验证 GROUP/REPEATED_GROUP、
恰有一个 observed GROUP 精确拥有该 region 时，才确定性替换 support owner 并派生 cardinality。未知 support、
非容器/未知 region、零 owner 或多个 owner 均保留既有 fixed code fail-closed。代码不跨 GROUP、不按距离/顺序
排名、不改 endpoint/topology/entity/relationship，也不补删结构；成功只记录 payload-free
`VISUAL_HIERARCHY_RELATIONSHIP_SUPPORT_OWNER_NORMALIZED`。

真实 PostgreSQL 纵切证明归一化后进入 BINDING，lease-expiry 恢复只重做 BINDING；监控/审核 UI 展示固定说明，
不显示局部 ID、模型文本、图片、完整 prompt/Candidate。该结果仍是 repository synthetic 合同证据，Provider
attempts=0，三份 ledger CLOSED，product-v22 保持隐藏 `EXPERIMENTAL`。

后续 Plus product-v22 按 `6f65516` PROPOSED → `2d396e7` OPEN → `4f86456` CLOSED 完成单 case lifecycle；
负探针精确 NOT_OPEN 且 Goal/guard、280 reservations、target evidence 零变化。唯一 wrapper exit 0、138,611 ms，
先 CLOSED 后由独立 verifier A2 重建 5 attempts、19,659 input + 7,284 output tokens、128,862 ms、0 abandoned
与 payload scan PASS。第二次 OBSERVE accepted；一次 HIERARCHY region/cardinality 拒绝后，下一次 HIERARCHY
accepted 并只命中 cardinality-derived telemetry；随后 ELEMENT_BINDING accepted。该 live 首次证明三阶段可达，
但未命中 support-owner normalization，report 仍 `complete=false`，stage-gold 匹配远未达标。Plus Goal 现为
136 attempts、783,738/1,000,000 tokens、¥3.116058；285 reservations 中 280 SETTLED、5 历史 RESERVED。

Max product-v22 随后在三阶段前置成立后，按 `e0b1d67` PROPOSED → `740d28f` OPEN → `99efc6b` CLOSED
完成同一 synthetic case。唯一 Provider wrapper 的测试主体写出 3 个 SETTLED attempts；外层 PowerShell 因
Mockito stderr warning 未取得 Maven 摘要，检查确认无存活子进程、evidence 已完整写出后立即 CLOSED，未重跑。
独立 verifier A2 PASS：11,318 input + 3,163 output tokens、61,032 ms、¥0.249684、payload scan PASS。三个
阶段都一次 accepted，但得到 11 SLOT/0 GROUP、1 entity/0 relationship、11 bindings，只有 1 slot/1 entity
匹配，bindings 0/10、tree edit 30/32。它证明合同可执行，但不证明嵌套结构质量；v22 normalization 未命中。
Max Goal 现为 76 attempts、443,297/1,000,000 tokens、¥9.454404；三份 ledger CLOSED。

Max 的 0 GROUP/relationship 说明：当两个上游 stage 都完全省略重复结构时，bounded verifier 没有合法证据可补造
GROUP。`e13bf0c` 形成新的零 Provider 假设：pipeline 4.10/product-v23 完整复用 4.9 support-owner policy，并绑定
既有精确 RapidOCR/OpenVINO capability；一次 ephemeral observation 由 OBSERVE、HIERARCHY、BINDING 共享。
OCR text、line ID、bbox 不进入 checkpoint、Candidate、problem、attempt、journal 或 report，本地代码也不据此直接
合成结构。三个 v23 Profile 都隐藏 `EXPERIMENTAL`；该节点只建立组合合同，不宣称 repeated-group recall 提升。

Flash v23 随后按 `0c1506f` PROPOSED → `9652837` OPEN → `1185890` CLOSED 完成单 case。精确 Document
Vision canary 通过后，唯一 wrapper 写出 5 个 SETTLED attempts；外层 PowerShell 因 Mockito stderr warning
失败，恢复检查确认无存活 Java/Maven/OCR 子进程、state/report 已完成后立即 CLOSED，未重跑。独立 verifier
A2 PASS：20,110 input + 24,522 output tokens、178,163 ms、¥0.023643、payload scan PASS。五次均停在
OBSERVE，三次 region-kind、两次 parent-kind，实际 slot/group/entity/relationship/binding 均为 0。Flash Goal
累计变为 81 attempts、473,005/1,000,000 tokens、¥0.213929；Goal 共 293 reservations，其中 288 SETTLED、
5 个历史 Plus RESERVED。

`061101f` 新增 pipeline 4.11/product-v24，把下一步限制为本地可证明的 bounded normalization：仅接受
`DOCUMENT→ROOT`、`CONTAINER→GROUP` 与允许枚举的大小写差异；ITEM 只有在同 artifact、bbox 包含、精确
repeatGroupId 的 REPEATED_GROUP 候选恰好一个时才改 parent，受影响 readingOrder 只按几何重算。未知 alias、
零/多候选、未知 parent 与结构增删继续 fail-closed。成功只记录三个固定 telemetry count，不保存被替换值或
任何 payload。三个 v24 Profile 仍隐藏 `EXPERIMENTAL`；该节点只证明合同与恢复可达，不证明 live 质量。

## 门控与证据

- `.sdlc/evidence/20260811-012556-server`：370 Java tests、6 gated skip，semantic verifier 与 stage-local repair A1。
- `.sdlc/evidence/20260811-014731-web`：Node 24、73 Web tests、type/lint/build A1。
- `.sdlc/evidence/20260811-015021-inference-e2e`：370 Java + 73 Web + real-PG inference journey A1。
- `.sdlc/evidence/20260811-015325-runtime`：runtime canary A1。
- 首轮 full `.sdlc/evidence/20260811-015430-full` 只在历史树视图 selector 失败；诊断确认 T6-2e 已把
  默认 `<pre>` 改为 `.readonly-schema-tree`。`de97131` 修复 audit，targeted draft E2E
  `.sdlc/evidence/20260811-015955-draft-e2e` 通过。
- `de97131` exact-clean full `.sdlc/evidence/20260811-020246-full` 全绿，包含 server、Web、offline eval、
  compose、runtime、prototype、draft 与 inference browser journey。所有 gate 均清除 live 环境，没有新增 Provider 调用。
- `bb15096` targeted codec/Profile 12/12、real-PG 1/1；server
  `.sdlc/evidence/20260811-043007-server`（379 tests、6 gated skip）、web
  `.sdlc/evidence/20260811-043128-web`（73 tests + build）、E2E
  `.sdlc/evidence/20260811-043158-e2e`（18 passed、1 gated skip）与 exact-clean fast
  `.sdlc/evidence/20260811-043334-fast` 均 PASS，Provider attempts=0。
- `31a8c6f` 合同/Profile 29/29、独立 verifier 2/2、relationship-region rewind real-PG 1/1；server
  `.sdlc/evidence/20260811-045814-server`（383 tests、6 gated skip）、web
  `.sdlc/evidence/20260811-045937-web`（73 tests + build）、E2E
  `.sdlc/evidence/20260811-050010-e2e`（18 passed、1 gated skip）与 clean fast
  `.sdlc/evidence/20260811-050115-fast` 均 PASS，Provider attempts=0。
- v17 lifecycle `178bafb` PROPOSED → `8e0c31e` OPEN → `7107303` CLOSED；负探针零调用，live evidence A2 PASS，
  CLOSED clean fast `.sdlc/evidence/20260811-051247-fast` PASS。
- `4d2cc46` 合同/Profile/prompt 31/31、独立 verifier 2/2、real-PG 1/1；server
  `.sdlc/evidence/20260811-052610-server`、web `.sdlc/evidence/20260811-052610-web`、E2E
  `.sdlc/evidence/20260811-052745-e2e` 与 clean fast `.sdlc/evidence/20260811-052853-fast` 均 PASS，
  Provider attempts=0。
- v18 lifecycle `df166df` PROPOSED → `dca738c` OPEN → `2ee5691` CLOSED；负探针零调用，live evidence A2 PASS，
  CLOSED clean fast `.sdlc/evidence/20260811-053811-fast` PASS。
- `214fff9` 合同/Profile/prompt 33/33、独立 verifier 2/2、real-PG 1/1；server
  `.sdlc/evidence/20260811-055056-server`、web `.sdlc/evidence/20260811-055228-web`、E2E
  `.sdlc/evidence/20260811-055259-e2e` 与提交后 clean fast `.sdlc/evidence/20260811-055541-fast` 均 PASS，
  Provider attempts=0。
- v19 lifecycle `09c3c16` PROPOSED → `7e1b98b` OPEN → `aa92ae2` CLOSED；负探针零调用，live evidence A2 PASS，
  CLOSED clean fast `.sdlc/evidence/20260811-060633-fast` PASS。
- `391bd52` inference 定向 34/34、独立 verifier 2/2、real-PG 1/1；server
  `.sdlc/evidence/20260811-062224-server`、Node 24 web `.sdlc/evidence/20260811-062433-web`、E2E
  `.sdlc/evidence/20260811-062513-e2e` 与 clean fast `.sdlc/evidence/20260811-062623-fast` 均 PASS，
  Provider attempts=0、三份 ledger CLOSED。
- v20 lifecycle `7afda44` PROPOSED → `191cf63` OPEN → `85b2000` CLOSED；负探针零调用，live evidence A2 PASS，
  CLOSED clean fast `.sdlc/evidence/20260811-063705-fast` PASS。
- `dda763c` inference 定向 35/35、独立 verifier 2/2、real-PG 1/1；server
  `.sdlc/evidence/20260811-064835-server`、Node 24 web `.sdlc/evidence/20260811-065005-web`、E2E
  `.sdlc/evidence/20260811-065038-e2e` 与 clean fast `.sdlc/evidence/20260811-065134-fast` 均 PASS，
  Provider attempts=0、三份 ledger CLOSED。
- v21 lifecycle `405fa9e` PROPOSED → `d793c92` OPEN → `02872c5` CLOSED；负探针零调用，live evidence A2 PASS，
  CLOSED clean fast `.sdlc/evidence/20260811-070414-fast` PASS。
- `edc0c28` inference 定向 36/36、独立 verifier 2/2、real-PG lease-expiry 1/1；server
  `.sdlc/evidence/20260811-071714-server`（191 tests、6 gated skip）、Node 24 web
  `.sdlc/evidence/20260811-071856-web`（73 tests + type/lint/build）、E2E
  `.sdlc/evidence/20260811-071927-e2e`（18 passed、1 gated skip、无 console/page error）与 clean fast
  `.sdlc/evidence/20260811-072030-fast` 均为 A1 PASS；Provider attempts=0、三份 ledger CLOSED。
- v22 lifecycle `6f65516` PROPOSED → `2d396e7` OPEN → `4f86456` CLOSED；负探针零调用，live evidence A2 PASS，
  CLOSED clean fast `.sdlc/evidence/20260811-073017-fast` PASS。
- Max v22 lifecycle `e0b1d67` PROPOSED → `740d28f` OPEN → `99efc6b` CLOSED；负探针零调用，live
  evidence A2 PASS，外壳摘要失败后按 process/evidence lease 恢复且未重跑；CLOSED clean fast
  `.sdlc/evidence/20260811-074402-fast` PASS。
- `e13bf0c` 的 Profile/能力合同 1/1、real-PG hybrid/payload/support-owner 与独立 verifier 2/2、clean fast
  `.sdlc/evidence/20260811-075518-fast`、server `.sdlc/evidence/20260811-075612-server`（192 tests、6 gated
  skip）均为 A1 PASS；Provider attempts=0、三份 ledger CLOSED。
- v23 lifecycle `0c1506f` PROPOSED → `9652837` OPEN → `1185890` CLOSED；Document Vision canary
  `.sdlc/evidence/20260811-080747-document-vision` 与独立 live verifier A2 PASS，外壳失败后按 process/evidence
  lease 恢复且未重跑。
- `061101f` 的 contract/Profile 20/20、real-PG + independent verifier 2/2、clean server
  `.sdlc/evidence/20260811-082418-server`（193 tests、6 gated skip）均为 A1 PASS；Provider attempts=0、
  三份 ledger CLOSED。
- `2b23617` 将 Goal guard 升为 v3；v1/v2 exact guard 迁移、state/reservation 不变、tamper fail-closed 与独立
  verifier 共 12/12 PASS，exact-clean fast `.sdlc/evidence/20260811-083559-fast` PASS；没有 Provider 调用。
- v24 lifecycle `9d2dfa3` PROPOSED → `ded9e78` OPEN → `ac17e0e` CLOSED；精确 Document Vision canary
  `.sdlc/evidence/20260811-084304-document-vision`、独立 live verifier A2 与 CLOSED clean fast
  `.sdlc/evidence/20260811-085018-fast` PASS。五次仍停在 OBSERVE，normalization telemetry 未命中。
- Plus v24 lifecycle `3598c12` PROPOSED → `963d1e6` OPEN → `4747947` CLOSED；独立 verifier A2 与 CLOSED
  clean fast `.sdlc/evidence/20260811-085833-fast` PASS。三阶段可达，但 slot/binding 0 matched、9 critical
  hallucinations、12 blockers，质量未达门。
- Max v24 lifecycle `a04691e` PROPOSED → `b8ac358` OPEN → `57b1502` CLOSED；负探针、复用 Document Vision
  canary、独立 verifier A2 与 CLOSED clean fast `.sdlc/evidence/20260811-091152-fast` PASS。三阶段首次尝试均
  accepted，但 slot/group/binding/field 均 0 matched、16 critical hallucinations、17 blockers，质量未达门。

## 结论与残余

N6 的代码、可审核 UI 和自动门控已完成，因此记录为 `automated_verified`；真实 Flash/Plus/Max 结果只作为未晋级
A2 诊断。Plus/Max v22 live 完成三阶段但质量未达门；Flash v23 的 OCR secondary evidence 没有突破 OBSERVE。
v24 bounded observation normalization 已有 clean local A1；Flash v24 A2 未突破 OBSERVE，Plus/Max v24 A2
虽完成三阶段但质量未达门，三模型均未命中 observation normalization。相同 v22/v23/v24 不再重复；下一步是
零 Provider 的 stage-gold replay 与 OBSERVE bounded semantic verifier，随后验证 earliest-stage repair、checkpoint、
审核 UI 与 E2E。exact-clean gate 不能替代 final eval、独立 verifier 或用户业务/视觉 J1。Goal 保持未完成，
Profile 保持 `EXPERIMENTAL`。

## v25 leaf-evidence verifier checkpoint

`f8f09b4` 新增 opt-in `SLOT_LEAF_EVIDENCE_REQUIRED`：只检查已验证本地 inventory，同 artifact 的 SLOT 证据框
不得严格包含另一 element 的证据框；GROUP 容器允许包含字段。固定码
`VISUAL_SEMANTIC_SLOT_EVIDENCE_CONTAINS_ELEMENT` 最早路由到 OBSERVE，verifier 不修改模型计划。60 个
stage-gold scene 全部接受，历史 pipeline 继续 `LEGACY`。

`2b6eb9c` 用 pipeline 4.12/product-v25 接入真实 workflow 和 Flash/Plus/Max 三份 immutable Profile。real-PG
tracer 证明失败 OBSERVE 不写 checkpoint、只重做 OBSERVE，随后复用成功阶段进入 HIERARCHY/BINDING；OCR
只预处理一次且不进入 checkpoint/Candidate/problem。三模型 Profile registry 与独立 snapshot verifier 通过，
全部隐藏 `EXPERIMENTAL`。`6cb2624` 在监控/审核共用 telemetry 中显示固定码、中文解释、证据范围和最早修复
阶段，不显示 OCR/provider payload。

- server `.sdlc/evidence/20260811-093552-server`：196 tests、6 gated skip，PASS。
- Node 24 web `.sdlc/evidence/20260811-093552-web`：73 tests + type/lint/build，PASS。
- E2E `.sdlc/evidence/20260811-093741-e2e`：18 passed、1 gated skip，PASS。
- runtime `.sdlc/evidence/20260811-093824-runtime`：PostgreSQL ephemeral canary，PASS。

本增量 Provider attempts=0，三份 visual ledger 均保持 CLOSED。N6 仍为 `automated_verified`；这不是 final live
质量结论，不能把 v25 Profile 晋级。

## v26 enclosing-connected support-owner checkpoint

Plus v25 的 payload-free 固定码证明 exact-region support-owner 与 connection-aware relationship-region 两条规则
存在可局部求解的次序死锁。`d3b0292` 新增 pipeline 4.13/product-v26 policy
`CANONICALIZE_EXACT_DUPLICATES_AND_UNIQUE_ENCLOSING_CONNECTED_GROUP_OWNER`：已知非 GROUP support 与已知 source
region 只有在恰好一个 GROUP/容器 region 配对同时满足 cardinality、包围全部 support element regions、连接
parent/child entity regions 时才被归一化。零/多配对保持原失败；不读取 OCR/模型文字，不使用 gold、距离或排序，
不增删结构。

真实 PostgreSQL tracer 的 Provider stage 序列精确为 OBSERVE、HIERARCHY、ELEMENT_BINDING；telemetry 为
cardinality derived=3、generic support owner=1、enclosing support owner=1、relationship region=1。ambiguity 合同
保持 `SUPPORT_NOT_GROUP`，OCR sentinel 未进入 checkpoint/current/problems。合同/Profile 定向 22/22 与独立
snapshot verifier PASS。`5ef25bd` 在监控/审核共用面板展示
`VISUAL_HIERARCHY_RELATIONSHIP_ENCLOSING_SUPPORT_OWNER_NORMALIZED`、中文解释、`层级边` 范围和最早 HIERARCHY
修复阶段，不显示 payload。

- server `.sdlc/evidence/20260811-101733-server`：197 tests、6 gated skip，PASS。
- Node 24 web `.sdlc/evidence/20260811-101734-web`：73 tests + generate/type/lint/build，PASS。
- E2E `.sdlc/evidence/20260811-101948-e2e`：18 passed、1 gated skip、无 console/page error，PASS。
- runtime `.sdlc/evidence/20260811-102032-runtime`：PASS。
- revision `371505b` 隔离 clean full `.sdlc/evidence/20260811-102845-full`：9/9 steps、
  `workingTreeDirty=false`，A1 PASS。

该切片 Provider attempts=0，三份 ledger CLOSED。N6 继续为 `automated_verified`；v26 Profile 仍隐藏
`EXPERIMENTAL`，其 live 质量、final eval 与用户验收属于 N7。

后续 N7 Flash/Plus v26 live 已独立 A2，但 Flash 仍停在 OBSERVE，Plus 在 HIERARCHY 的 region-cardinality 与
support-not-group 间停止；两者都没有命中 v26 enclosing-owner telemetry，也没有建立同版本三阶段可达性，故
Max v26 未调用。这不改变 N6 的 `automated_verified`：离线合同、真实 PostgreSQL checkpoint 与 UI telemetry 仍
有效，但没有升级为 live 质量结论。下一安全切片仍属于 bounded verifier 工作：仅离线调查已知非 GROUP support
是否恰有一个 validated ancestor GROUP，并继续要求 owner/region 满足既有 cardinality、enclosure、connection；
禁止读取模型原文/OCR/gold、排序选择或补删结构。

## v27 source-ancestor support-owner checkpoint

`676180a` 新增 pipeline 4.14 所需的 opt-in support policy。它只在 v26 的 enclosing candidate 集合为零时，
消费已经通过合同校验的原始 `relationship.regionId` 与 region forest：沿 source region 的祖先链寻找同时由
GROUP element 唯一拥有、cardinality-compatible 且连接 parent/child entity ownership 的容器。恰有一个候选
才替换 support owner；v26 候选为多个、source-ancestor 为零或多个时均保持原 fixed code fail-closed。规则不
读取 OCR/model text/gold，不使用距离、reading order 或任意排名，不合成、删除或改写 topology。

`e1f1a9d` 发布三份 immutable product-v27 Profile，并把 pipeline 4.14 接入 worker、checkpoint telemetry 与
独立 evidence verifier；Flash 精确模型为 `qwen3.7-flash-2026-07-15`，Plus/Max ID 不变，全部隐藏
`EXPERIMENTAL`、maximumTotalCalls=5、maximumRepairRounds=0。真实 PostgreSQL synthetic tracer 完成
OBSERVE→HIERARCHY→ELEMENT_BINDING→本地 materialize/verifier→`REVIEW_REQUIRED`，并精确记录 cardinality、
generic owner、source-ancestor owner 与 relationship-region 四个计数；checkpoint/Candidate/problems 均不含
OCR sentinel。codec 22/22、registry 1/1、real-PG 1/1、独立 verifier 2/2 组合回归通过。

`3a56af9` 在监控与审核共用面板显示
`VISUAL_HIERARCHY_RELATIONSHIP_SOURCE_ANCESTOR_SUPPORT_OWNER_NORMALIZED`、固定中文解释、`层级边` 范围与最早
HIERARCHY 修复阶段。Node 24 Web 73/73 与 Playwright telemetry 场景通过，后者覆盖 1024px、键盘折叠、
WCAG 与 OCR/Provider payload 不可见。

server gate 首轮 `.sdlc/evidence/20260811-113055-server` 暴露既有预算硬门竞争：接口 5 参数 default reserve
入口可能绕过实现方法事务，使 `FOR UPDATE` 不能覆盖完整 reservation。`5ada0fa` 显式覆写该入口并建立事务，
同一 PostgreSQL 容器并发回归 10/10 通过；随后 server `.sdlc/evidence/20260811-113412-server`（schema 20、
validation 13、inference 176、app 207，6 gated skip）、Node 24 web
`.sdlc/evidence/20260811-113607-web`、E2E `.sdlc/evidence/20260811-113652-e2e`（18 passed、1 gated skip）与
runtime `.sdlc/evidence/20260811-113726-runtime` 全部 A1 PASS。

本切片 Provider attempts=0，Goal 用量不变，三份 visual ledger 仍 `CLOSED`。N6 保持
`automated_verified`；随后完成的隔离 full 与 v27 live 结果见下一节。该 full 仍不能替代 final 20/60、最终
independent verifier 或用户业务/视觉 J1。

## v27 live 诊断 checkpoint

revision `47f622b` 的隔离 clean full 9/9 A1 PASS 后，三模型均在 fresh identity `…960c965` 与精确 product-v27
snapshot 下完成 bounded lifecycle。Flash 的首个 v27 ledger 因 wrapper 漏传 Document Vision Spring 属性，在
Provider 前以 `DOCUMENT_VISION_DISABLED` 结束，0 attempts、Goal 零变化；该 ledger 已 CLOSED 且未重开。
当前 revision 的 RapidOCR/OpenVINO/model manifest 随后由 `20260811-115701-document-vision` 重验 PASS。

- Flash v27b：`6185570` PROPOSED → `ea9cb87` OPEN → `a473d2f` CLOSED；5 attempts、42,913 tokens，全部
  停在 OBSERVE，source-ancestor 规则无机会命中。
- Plus v27：`ccfce3b` → `7f49117` → `854d652`；3 attempts，OBSERVE/HIERARCHY/BINDING accepted，但
  slot/binding 0 matched、7 blockers、4 critical hallucinations；只命中既有 cardinality-derived。
- Max v27：在 Plus 建立同版本三阶段门后按 `1fa1ccf` → `705ccff` → `95be8fa` 执行；3 attempts，三阶段
  accepted，但 slot/binding 0 matched、26 blockers、27 critical hallucinations；新增 telemetry 仍未命中。

三份 current evidence 在最终 Goal state 下交叉独立 verifier A2 PASS，0 abandoned、payload scan PASS。Goal
累计 335 reservations（330 SETTLED、5 历史 Plus RESERVED、0 BREACHED）；Flash/Plus/Max 分别为
641,256 / 900,566 / 491,919 exposed tokens。CLOSED clean fast `20260811-121335-fast` PASS；前一环境红灯
`20260811-121310-fast` 只因隔离 worktree 缺 `web/node_modules`，保留不抹除。

这组 live 结果没有否定 v27 verifier 的离线安全性，却否定了“单凭该规则即可提升当前 case 质量”的晋级解释。
N6 因而仍是 `automated_verified`，不是 accepted；下一步只能从 payload-free fixed code/metrics 形成新的 bounded
假设，或在最终质量门明确满足前保持现状，不能放宽合同、读取模型原文或直接扩大 final eval。

## Git-blob canonical identity `/2` checkpoint

`cded69e` 关闭了 N2 遗留的 Windows checkout 字节可移植性债务。新 `/2` 从 Git index 读取 regular blob OID，
按 UTF-8 path bytes、`100644`/`100755` mode 与 `git cat-file --batch` canonical blob bytes 做确定性 framing；
clean status 之外拒绝 assume-unchanged/skip-worktree、non-regular mode、missing tracked input 与双捕获不稳定。
Java live executor 只允许 `/2` OPEN，历史 `/1` 仅 CLOSED/read-only；独立 Python verifier 按 ledger identity
version 重算两种合同。

clean LF/CRLF 双 checkout、mode drift、dirty/untracked 与 hidden-index 负例均进入 server 回归。revision
`cded69e` 的 Java/Python `/2` 一致为 `fc46a428…b5a7bf`；clean server
`.sdlc/evidence/20260811-123055-server` 和 fast `.sdlc/evidence/20260811-123245-fast` A1 PASS。新 verifier 又对
v27 Flashb/Plus/Max 的真实 CLOSED `/1` evidence 回放 PASS、0 abandoned、payload scan PASS。该治理节点
Provider attempts=0，Goal reservation/token/CNY 均不变，三份 ledger 仍 CLOSED。

这不会改变 v27 质量结论或 N6 生命周期：N6 继续 `automated_verified`，Profile 继续 `EXPERIMENTAL`。任何新
live 必须在最终 pre-live revision 重新计算 `/2` 与 Profile snapshot；`cded69e` 摘要不能跨后续提交复用。

## v28 minimal entity ownership checkpoint

`76a0635` 为新 pipeline 增加 opt-in hierarchy/binding semantic policy。HIERARCHY 拒绝非根 entity 拥有 ROOT
region，也拒绝同一 entity 同时拥有严格祖先和后代 region；BINDING 要求覆盖字段证据的空间 entity owner 存在
唯一最小者。zero/multiple 候选继续 fail-closed，legacy overload/Profile 保持原语义。定向 verifier/codec
27/27 通过。

`a96fec1` 发布 pipeline 4.15 与三份 immutable product-v28 Profile，并接入 worker、checkpoint、真实
PostgreSQL repair tracer 与独立 snapshot verifier。binding 检出的同等最小 owner 歧义固定路由到最早
HIERARCHY；`rehierarchizing` 保留已验证 OBSERVE inventory/grounding、清空 hierarchy/binding。持久层只对白名单
单码 `VISUAL_SEMANTIC_HIERARCHY_BINDING_OWNER_AMBIGUOUS` 允许 `REJECTED ELEMENT_BINDING` 回到 HIERARCHY，
其余逆向 transition 仍 fail-closed。inference 回归 180/180、真实 PostgreSQL 两条 tracer 2/2、独立 Profile
snapshot 1/1 PASS；五次 scripted provider stage 能按 HIERARCHY→BINDING→HIERARCHY→BINDING 路径恢复到
`REVIEW_REQUIRED`，OCR sentinel 未持久化。

`6a8a36f` 把三个 payload-free issue code、固定中文解释与最早 HIERARCHY repair stage 接入 monitor/review。
Node 24 Web gate 14 files / 73 tests、typecheck/lint/build PASS，evidence 为
`.sdlc/evidence/20260811-125512-web`；隔离 4174 的针对性 Playwright 1/1 PASS，覆盖 1024、keyboard、axe 与
payload sentinel 不可见。

本节点 Provider attempts=0，三份 ledger CLOSED，Goal aggregate 仍为 Flash 641,256、Plus 900,566、Max
491,919 / 各 1,500,000 exposed tokens。N6 继续 `automated_verified`、product-v28 继续 `EXPERIMENTAL`；它只
证明 bounded verifier、stage-local repair、selected crop/checkpoint 与审核 telemetry 可达，不证明真实模型质量。

## v28 live 对 N6 合同的反馈

`0a3b90b` clean full 与 current-revision Document Vision canary 均 A1 PASS。Flash LF replacement v28b 经独立
A2 后仍在 OBSERVE 停止；Plus 首个 v28 因调用侧 120 秒 timeout 超过实现 60 秒上限，于 Provider 前固定码失败，
0 attempts，历史 evidence 与 CLOSED ledger 保留。使用合法 60 秒的独立 Plus v28b 为 5 attempts / 36,204
tokens，OBSERVE 可达，但 HIERARCHY 连续三次命中
`VISUAL_HIERARCHY_V2_RELATIONSHIP_REGION_CARDINALITY_INVALID`，BINDING/ownership verifier 未被 live 触发。

这不否定 N6 已有 deterministic verifier/checkpoint/repair tracer，却证明当前 stage-local repair 对相同
HIERARCHY cardinality fixed code 没有取得进展。下一 bounded 节点应在不读取 Provider 原文、不放宽 verifier、
不合成 topology 的前提下，为该 fixed code 建立可测试的 repair/no-progress 合同；先用 scripted provider 与真实
PostgreSQL 验证 checkpoint 和调用上界。N6 仍为 `automated_verified`，不是 accepted。

## v29 group-region cardinality 前移 checkpoint

`70da862` 将 v28 live 暴露的 bounded 缺口固定为 opt-in OBSERVE semantic policy：除了既有
REPEATED_GROUP→MANY GROUP 条件，每个 MANY GROUP 也必须拥有至少一个 REPEATED_GROUP；否则用
`VISUAL_SEMANTIC_REPEATED_GROUP_CARDINALITY_INVALID` 在最早 OBSERVE fail-closed。legacy Profile 不变，
校验只读取已验证 region/element ownership，不读取 OCR/model text 或补造结构。

`dd920cc` 发布 pipeline 4.16、visual-elements v9 与三模型 product-v29 Profile。真实 PostgreSQL tracer 以四次
scripted call 从 OBSERVE 拒绝只重做 OBSERVE，随后通过 HIERARCHY/BINDING 到达 `REVIEW_REQUIRED`；本地
Document Vision 仅执行一次，OCR sentinel 未进入 checkpoint。`70e0f2c` 把双向归属解释接入监控，并以组件/
Playwright 验证固定码、最早修复阶段与 payload-free 展示。

clean `70e0f2c` 的 fast/server/web/inference-e2e 全部 A1 PASS：Inference 182、App 213（6 gated skip）、Web
73、真实 replay→review→atomic Draft Apply 1/1。本节点 Provider attempts=0、345 reservations 和三模型
用量不变、三 ledger CLOSED。N6 继续 `automated_verified`，因为 deterministic verifier/recovery/UI 已由机器
证明，但真实质量与用户视觉 J1 尚未满足。

## v29 bounded live A2 checkpoint

clean full、Document Vision canary、双实现 identity/snapshot 与预算 preflight 通过。首次 Flash 因缺少 runtime
enable flag 在 Provider 前 fail-closed，0 attempts；replacement Flash v29b 的 5 次和 Plus v29 的 3 次真实调用
均停在最早 OBSERVE，独立 verifier/payload scan/CLOSED probe 全部 PASS，无 abandoned 或 Goal/evidence 漂移。
Flash 消耗 43,203 tokens/¥0.022207，Plus 消耗 21,000/¥0.093918；Max 因三阶段合同未达未调用。

这验证了 N6 的 bounded verifier、checkpoint、最早阶段停止、immutable ledger 与可审计 telemetry 在真实失败上
按设计工作，但没有形成 REVIEW_REQUIRED Candidate 或用户视觉 J1。N6 继续 `automated_verified`；下一安全任务是
用 scripted provider/真实 PostgreSQL 对 enum、sibling overlap、evidence-region 三类 OBSERVE fixed code 建立不
放宽语义的 stage-local repair/no-progress 测试，再决定新 Profile。

## v30 unique evidence-owner normalization checkpoint

`71ccbdf` 将 evidence-outside-region 收窄为 opt-in 的原子 bounded repair。它只消费已经 canonicalize 的 element
evidence 与通过校验的 region forest：保留覆盖任一 evidence 的既有 owner，并为每块未覆盖 evidence 选择唯一
最具体、非 ROOT 且与 SLOT/ONE GROUP/MANY GROUP 兼容的 region。零或多候选、unknown region、ID coverage
不全、空结果或超过 8 owners 时返回原 plan，由原固定码 fail-closed；不更改 topology/evidence，不猜 enum、
sibling overlap 或 rejected-plan crop。

`d3fedf3` 以 pipeline 4.17/product-v30 接入 stage-local worker，成功只产生数量型
`VISUAL_GROUNDING_ELEMENT_REGION_NORMALIZED`。真实 PostgreSQL tracer 三次 stage call 依次为 OBSERVE、
HIERARCHY、BINDING 并进入 `REVIEW_REQUIRED`；Document Vision 一次，OCR sentinel 未进入 checkpoint、Candidate
或 validation。独立 Python verifier 覆盖三份 Profile snapshot。`837c015` 将固定码接入监控/审核说明。

验证为 Inference 183/183、real-PG 1/1、Node 24 Web 14 files/73 tests + build、真实 replay 浏览器 Apply 1/1、
1024px payload-free diagnostics Playwright 1/1 PASS；证据见 `20260811-150327-web`、
`20260811-150428-inference-v30-ui`、`20260811-150534-v30-diagnostics-e2e-results`。本节点 Provider attempts=0，
三 ledger CLOSED，Goal/预算不变。N6 继续 `automated_verified` 而非 accepted；真实 v30 质量、final eval 与用户
视觉 J1 仍属于 N7。

## v30 bounded live 对 N6 合同的反馈

clean full、Document Vision 与 fresh identity/Profile/Goal preflight 后，Flash/Plus v30 分别按独立
PROPOSED→负探针→OPEN→CLOSED lifecycle 完成 5 次和 1 次真实调用；两份 evidence 均由独立 verifier A2
重建为 PASS、0 abandoned、payload scan PASS。所有调用都在最早 OBSERVE fail-closed，stage-local worker 没有
错误进入 HIERARCHY/BINDING，也没有把 rejected plan、OCR/model text 或 Candidate 写入 evidence；费用守卫在
Plus 下一次预留前正确停止。

`VISUAL_GROUNDING_ELEMENT_REGION_NORMALIZED` 没有命中，因此 live 没有验证该 normalization 的质量收益，
也没有否定 real-PG scripted contract。新的可操作信号是 Plus 的
`VISUAL_SEMANTIC_REPEATED_ITEM_FIELD_MISSING`，以及 Flash 的 parent-kind、non-repeated cardinality、
region-kind 与 parent-containment fixed codes。N6 仍为 `automated_verified`，不是 accepted；下一实现切片必须
先证明其中某个错误存在唯一、本地可验证且不合成 topology 的 repair/no-progress 条件，否则保持 fail-closed。

## v31 repeated-item SLOT owner repair checkpoint

`7e464df` 已证明 Plus v30 的 `VISUAL_SEMANTIC_REPEATED_ITEM_FIELD_MISSING` 存在一个更窄的本地可判定子集：只有
已有 SLOT 的所有 canonical evidence 都位于 ITEM 内、每块 evidence 对应唯一最具体非 ROOT region，并且每个当前
缺字段 ITEM 都有 eligible evidence 时，才原子重建该 SLOT 的 owner。inventory/ownership 不全、unknown、root-only、
零/多候选、缺任一 ITEM 或超过 8 owners 都返回原输入与同一 fixed code；不新建字段/region，不修改 topology/
evidence，不读文字，不从 rejected OBSERVE 派生 crop。v30 与更早行为保持不变。

`791d4e9` 通过 pipeline 4.18/product-v31 opt-in，并把成功次数写为 payload-free
`VISUAL_GROUNDING_REPEATED_ITEM_SLOT_OWNER_NORMALIZED`。真实 PostgreSQL tracer 只做一次 Document Vision，随后
OBSERVE→HIERARCHY→BINDING 三次 scripted call 到达 `REVIEW_REQUIRED`；checkpoint 中 owner 精确为两个 ITEM，OCR
sentinel 未进入 checkpoint/Candidate/problems。`f6cc529` 的独立 Python verifier 覆盖三份 immutable snapshot；
`eea8b3f` 完成 monitor/review 中文说明与最早 OBSERVE repair scope。

Inference 184/184、real-PG 1/1、snapshot verifier 1/1、Node 24 Web 73/73/build、1024px Playwright 1/1、Axe
serious/critical=0、payload sentinel=0，证据为 `20260811-155052-web` 与
`20260811-155200-v31-diagnostics-e2e-results`。本节点 Provider attempts=0，359 reservations 与三 ledger CLOSED
状态未变。N6 继续 `automated_verified`：scripted 可达性和 payload-free 可观测性已验证，但真实 v31 质量、final
eval、最终独立 verifier 与用户视觉 J1 仍属于 N7。

## v31 bounded live 对 N6 合同的反馈

clean full、Document Vision、双实现 identity/Profile 与逐模型 Goal preflight 后，Flash/Plus v31 均按独立
PROPOSED→负探针→OPEN→唯一 wrapper→CLOSED lifecycle 完成 5 次真实调用；两份 evidence 的独立 Python verifier、
Java verifier 2/2 与 payload scan 全部 PASS，0 abandoned、0 BREACHED。Flash 的 5 次调用都在最早 OBSERVE
fail-closed；Plus 首个 OBSERVE accepted 后，4 次 HIERARCHY 都以
`VISUAL_HIERARCHY_V2_RELATIONSHIP_SUPPORT_IDS_EMPTY` fail-closed。没有错误进入 BINDING/Candidate，也没有把
OCR、模型原文、图片、完整 prompt/Candidate 写入 evidence。

该结果没有真实命中 `VISUAL_GROUNDING_REPEATED_ITEM_SLOT_OWNER_NORMALIZED`，因此不证明 v31 repair 的质量收益；
但它给出了新的最早阶段信号：Plus 已形成可信 OBSERVE checkpoint，缺口位于 HIERARCHY relationship support IDs。
下一 N6 安全切片应只消费已验证 hierarchy relationships、OBSERVE inventory/region/evidence，证明 support IDs 是否
能在唯一且完整的本地约束下恢复；zero/multiple/unknown 必须保留原 fixed code，不能补造 relationship、topology、
OCR 文本或从 rejected HIERARCHY 派生不可信 crop。N6 继续 `automated_verified`，不是 accepted。

## v32 empty relationship support owner repair checkpoint

`212f468` 证明 `VISUAL_HIERARCHY_V2_RELATIONSHIP_SUPPORT_IDS_EMPTY` 存在一个更窄、可本地证明的子集：只消费
已验证 relationship 自己的 region、父子 entity region ownership 与 OBSERVE 的 region/element inventory。
relationship region 必须是已知 GROUP/REPEATED_GROUP、处于 parent→relationship→child 的连接上，且只存在一个
kind=GROUP、multiplicity/cardinality 兼容的既有 owner；此时才能原子补入该 element ID。v31、null/missing、
unknown/non-container、zero/multiple owner、断连与 validation failure 全部保留原 fixed code；不创建或修改
relationship/entity/region/evidence/文字/Candidate，不读取 Provider 原文，也不选择 rejected-stage crop。

`7e4e70c` 以 pipeline 4.19/product-v32 显式 opt-in，继承 v31 的 bounded OBSERVE、minimal hierarchy ownership、
unique binding owner、stage-local repair 与 deterministic materializer。成功同时记录通用 support-owner 计数和
payload-free `VISUAL_HIERARCHY_RELATIONSHIP_EMPTY_SUPPORT_OWNER_NORMALIZED`。独立 Python verifier 覆盖三份
immutable Profile snapshot。`b892503` 的真实 PostgreSQL tracer 只运行一次 Document Vision，三次 stage attempt
全 SUCCEEDED 并进入 `REVIEW_REQUIRED`，OCR sentinel 未进入 checkpoint/Candidate/problems。`7404c7a` 完成
monitor/review 中文解释与 E2E。

验证为 inference 185/185、Profile/capability 3/3、independent verifier 2/2、real-PG 1/1、Web 73/73/build、
1024px Playwright 1/1、payload sentinel=0。本节点 Provider attempts=0，Goal/ledger/累计用量不变，三 ledger
CLOSED。N6 继续 `automated_verified`，不是 accepted：scripted 可达性已验证，但真实 v32 质量、final eval、最终
独立 verifier 与业务/视觉 J1 仍属于 N7；clean full/Document Vision 与 fresh pre-live 治理门通过前不得调用。

## v32 bounded live 对 N6 合同的反馈

v32 在 clean full/Document Vision、双实现 identity/Profile 与 Goal preflight 后只运行 Plus；Flash 在
reservation 前因 Goal CNY 不足 fail-closed。Plus 唯一 wrapper 完成 3 个 Provider attempts，独立
verifier/payload scan PASS，0 abandoned/0 BREACHED。OBSERVE accepted，两次 HIERARCHY 均为
`VISUAL_HIERARCHY_V2_SUPPORT_ELEMENT_UNKNOWN`，第四次在 Provider 前因 ledger cost 预留不足停止，
未进入 BINDING/Candidate。empty-support telemetry 未命中。

该结果再次证明最早失败阶段停止、checkpoint 保留、immutable ledger 与费用守卫按设计工作，
但不证明 v32 repair 改善了 live 质量，也不允许从通用 unknown fixed code 推断 owner payload。
N6 仍为 `automated_verified`，不是 accepted。

## v33 unknown relationship support owner repair checkpoint

`5951047` 新增独立 opt-in policy，只处理一条 relationship 的唯一 unknown support ID。已知
relationship region 必须为 container、连接 parent/child entity ownership，并且恰有一个
multiplicity-compatible 的已有 GROUP owner；否则原子保留原 plan。多 unknown ID、non-container、
ambiguous、disconnected 均保留原 fixed code。实现不创建元素、关系、region、evidence、文字、
crop 或 Candidate。

`7ac4259` 以 pipeline 4.20/product-v33 显式启用，成功只记录通用 owner 与
`VISUAL_HIERARCHY_RELATIONSHIP_UNKNOWN_SUPPORT_OWNER_NORMALIZED` 数量。`edd310d` 真实
PostgreSQL tracer 只执行一次 Document Vision，三个 stage attempt 全 SUCCEEDED 并到达
`REVIEW_REQUIRED`；telemetry 只出现在 HIERARCHY，OCR sentinel 未进入 checkpoint/Candidate/
problems。`94060a0` 完成 monitor/review 中文说明与 1024px diagnostics E2E。

验证为 contract 28/28、inference 186/186、Profile/capability 3/3、independent verifier 2/2、
real-PG 1/1、Web 73/73 + lint/typecheck/build、Playwright 1/1。本节点 Provider attempts=0，Goal/ledger
用量不变，三 ledger CLOSED。N6 继续 `automated_verified`；由于 live fixed code 未证明其 ID 属于
relationship，该状态仍不是 live 质量 accepted，final eval 与 J1 属于 N7。

## v4 cost guard 影响说明

用户后续批准 Flash/Plus 各 ¥10 Goal 总 cap 与 24h 窗口。该 delta 只改变 N7 的 guarded live
入口，不改变 N6 的 bounded verifier、repair 或自动证据；N6 继续 `automated_verified`。guard v4
必须保留 v1–v3 evidence 可重放，且在本 checkpoint 提交前 Provider attempts=0、ledger 全 CLOSED。
