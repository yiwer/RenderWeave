# Checkpoint：P1-T1-3 / G-P1-DRAFT

- 结果：完成
- AC：AC-001, AC-002, AC-003, AC-013, AC-022, AC-023（P1 thin slice）
- 生命周期状态：`automated_verified`
- 摘要：冻结 Draft create/get/save OpenAPI 与 RFC 9457 错误合同，生成 React SDK，并将已确认的 A 表单 + B 一层树状图实现为共享 `EditorSession` 的生产 Schema Studio；真实浏览器完成 revision 0→1、PostgreSQL 回读与刷新恢复。

## 验证证据

| Gate | 保证等级 | Revision | 命令/来源 | Exit/结论 | 原始证据指针 | 验证者 captured_by | 新鲜度 |
|---|---|---|---|---|---|---|---|
| HTTP contract red baseline | A1 | `UNBORN` | targeted MockMvc | 1；新 Draft routes 尚不存在（404） | 当前 Goal tool capture | Codex shell tool | superseded by green gate |
| G-P1 full | A1 | `UNBORN` | `tools/run-gate.ps1 -Gate full` | 0；server/web/contract/compose/runtime/prototype/production Draft E2E 全绿 | `.sdlc/evidence/20260808-005748-full/metadata.json` | project gate | impact-analyzed；之后仅调整 Map edge ports |
| final affected gate | A1 | `UNBORN` | `tools/run-gate.ps1 -Gate draft-e2e` | 0；9 schema tests、8 app tests、5 web tests、SDK regeneration/build、create/save/reload browser journey | `.sdlc/evidence/20260808-010032-draft-e2e/metadata.json` | project gate | exact final input manifest |
| production visual audit | A1 | `UNBORN` | headless Chromium 1440/1024/1000 | Form/Map screenshot、无 console/page error、1024 无水平溢出、1000 明确阻断 | `.sdlc/evidence/draft-e2e-150264/` | `tools/draft_journey_audit.py` | exact final UI |
| prototype direction | J1 | `UNBORN` | 用户确认 A+B 并吸收 C 可读性能力 | accepted | `docs/adr/0005-shared-editor-session-form-and-map.md` | 用户 | 当前产品方向；非生产 UI 最终验收 |

- 证据复用理由：full gate 覆盖所有仓库基础门，之后唯一生产变化是 React Flow 节点连接方向；final affected gate 在该变化后重跑 server、generated SDK、web 与真实全栈浏览器旅程。
- 当前没有 independent verifier/CI，不能声称 A2/A3；生产 UI 的最终 J1 保留到 G-P3-STUDIO。

## 重要偏差

- 首次 full gate 因已有项目 Vite dev 进程锁住 Windows native `lightningcss` 文件而失败；确认命令行属于本项目后只终止该进程，随后 full gate 通过。失败证据保留于 `.sdlc/evidence/20260808-005640-full/metadata.json`。
- P1 UI 只暴露已实现的 `text` 字段；未用禁用控件伪装其余类型。空 definition 可原样读取/保存，内存 `rowKey` 永不序列化。
- 生产模式只有 Form/Map；C 的搜索、密度摘要与只读 DSL preview 被吸收，不保留第三模式。
- 视觉复核发现横向树节点使用默认上下 ports 会产生回环，已改为 root-right → field-left，并在最终门禁重拍证据。
- Vite 报告主 chunk 约 508 kB；这是 P3 路由/Map lazy-loading 的已知优化项，不影响 P1 正确性。
- 是否需要 spec delta：否。
- 再锚定是否发现漂移：否；route/table/tool inventory 未引入 Template、Workspace、Renderer、live AI、真实数据或付费调用。

## 多 Agent

- 未启用；single writer，`atomic_claim:none`。

## 下一步

- 下一任务或恢复入口：P2/T2-1，以失败测试扩展完整七类型、类型专属约束、array 非嵌套和 regex safety；随后才进入引用图与不可变发布。
- 需要用户决定：无。
