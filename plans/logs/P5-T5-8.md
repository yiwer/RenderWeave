# P5 / T5-8 checkpoint — Grounded Pipeline v2

- 日期：2026-08-09
- 分支：`phase/p5-grounded-pipeline-v2`
- 实现基线：`6077bf0`
- AC：AC-016、AC-020、AC-021
- 生命周期：`live_independently_reviewed`（J1 + A1 + 独立 A2 PASS；authorization `CLOSED`）
- 外部副作用：80 provider attempts / 278,740 tokens / ¥0.908984；仅使用仓库 synthetic corpus，未使用真实业务数据

## 目标与边界

- 以 Prompt v2 的 CLOSED 60-case evidence 为归因基线，不降低认证阈值。
- JSON_ONLY 由 value-free structural profile 确定性生成 Candidate，任何阶段均为零 provider、零 reservation。
- COMBINED 以 JSON truth 为 base，只接收有直接图片证据的受限视觉语义；Provider UUID、SchemaKey、JSON evidence 与非法图拓扑不受信任。
- 新增 pipeline 2.0、Prompt 3.0 与 pinned Grounded Profile；旧 pipeline/prompt/profile 字节保持不变。

## Pre-live 验证

- clean server A1：`.sdlc/evidence/20260809-152534-server/metadata.json`，revision `6077bf0`，Schema 20、Validation 13、Inference 82、App 107（2 个 paid tests 默认关闭而 skipped）。
- clean web A1：`.sdlc/evidence/20260809-152655-web/metadata.json`，revision `6077bf0`，OpenAPI client 重生成无 drift、typecheck、lint、27 tests 与 production build 全绿。
- 独立 A2：0 Blocker / 0 High / 0 Medium；独立离线 inference 26/26、真实 PostgreSQL live workflow 14/14，全程清除 key/live gates，provider attempt 0。
- A2 覆盖 JSON uncertainty→REVIEW、20-case JSON_ONLY 零副作用、异常 REPAIR 恢复、SchemaKey digest collision、1.1→1.2 `*` 路径规范化、JSON evidence 对齐、歧义/非法 visual reference graph 与 Provider UUID 污染。

## 授权信封与 Git 生命周期

- 精确 Profile：`dashscope-qwen37-plus-20260526-grounded-v1` / `qwen3.7-plus-2026-05-26`。
- 数据仅限 `renderweave-live-eval/2.0` 的 60 个仓库 synthetic case；不发送真实业务数据。
- 最多 120 provider attempts；JSON_ONLY 的 20 case 设计上为 0 次，IMAGE_ONLY/COMBINED 各最多 20×3 次。
- 费用硬上限 2,000,000 micros CNY（¥2），每批最多 5 case；OPEN 窗口进一步限制为最多 4 小时。
- Plus 最低输入单价为 ¥2 / 1M tokens，结合每次调用前最坏费用预留与 ¥2 总上限，确定性收窄为不超过用户批准的 1,000,000 tokens。
- evaluation identity 以该 tracked ledger 之外的完整 staged repository tree 计算并写入 ledger；只有 ledger 可在不改变 identity 的前提下 `PROPOSED → OPEN → CLOSED`。
- identity：`renderweave-repository-tree-sha256/1:67328db2cfe66b8753398108452d5e22715ab942c558fe47db5117ea4bc499a8`。
- Git 生命周期：`b454b14`（PROPOSED）→ `7ee817c`（OPEN）→ `6733260`（CLOSED）；OPEN 账本批准窗口为 `2026-08-09T07:39:25Z–11:39:25Z`。
- PROPOSED 负向探针以 `LIVE_CERTIFICATION_AUTHORIZATION_NOT_OPEN` fail-closed，零 journal、零 Provider；CLOSED 后同一入口再次以该 code 拒绝，最终 `state.json` SHA-256 不变。

## Live J1、执行与结果

- 实际 Provider 窗口为 `2026-08-09T07:41:58.640Z–08:15:15.611Z`，约 33 分 17 秒；完成后约 23 秒提交 CLOSED。
- 28 个批次完成 60 个唯一 assignment/run：前 20 个 IMAGE_ONLY 因每例 repair budget exhausted 而逐例停批，随后 JSON_ONLY 与 COMBINED 各 4 批×5 例；每批均不超过 5。
- IMAGE_ONLY 20 例全部 `FAILED/LIVE_REPAIR_BUDGET_EXHAUSTED`，每例 1 次 STRUCTURE + 2 次 REPAIR；JSON_ONLY 20 例全部 `REVIEW_REQUIRED` 且严格 0 attempt / reservation / token / cost；COMBINED 20 例全部 `REVIEW_REQUIRED` 且每例仅 1 次 STRUCTURE。
- 80 个 reservation 全部唯一且 `SETTLED`，40 次 STRUCTURE + 40 次 REPAIR；60 次 attempt 为 `REJECTED`、20 次为 `SUCCEEDED`，每条 actual cost 均不超过 reserved。
- 输入 220,156、输出 58,584，合计 278,740 tokens；账本按 pinned Plus Profile 记录总费用 908,984 micros CNY（¥0.908984），低于 120 attempts、1,000,000 tokens 与 ¥2 的收窄上限。
- payload-free evidence：`.sdlc/evidence/p5-certification-20260809-plus-grounded/`；`state.json` SHA-256 `7d82703214f7698a0726e47656e86661e41250023016072ef5b929106576c4c3`，`summary.json` `5b88aa2b67492785def36c59f29e72bb6756d4fb7e94f5b344ebd6ab77d60fcb`，`state.guard.json` `08221ef183fb471bcb37fd91c5b7c921c19a6bf29dc7d8f24eb423db74f205d1`。

### 质量结论

| Slice | 通过 | Contract | Entity F1 | Field F1 | Type | Edge F1 | Evidence | DAG | Critical / Blocker |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Global | 40/60 | 66.66% | 80.00% | 81.57% | 67.08% | 80.00% | 67.90% | 66.66% | 0 / 23 |
| IMAGE_ONLY | 0/20 | 0% | 0% | 0% | 0% | 0% | 0% | 0% | 0 / 0 |
| JSON_ONLY | 20/20 | 100% | 100% | 100% | 100% | 100% | 100% | 100% | 0 / 13 |
| COMBINED | 20/20 | 100% | 100% | 100% | 100% | 100% | 100% | 100% | 0 / 10 |
| HOLDOUT | 10/15 | 66.66% | 81.08% | 81.81% | 70.83% | 83.33% | 68.75% | 66.66% | 0 / 3 |

完整报告得到 17 项 policy violation，决定为 `EXPERIMENTAL`。Grounded v2 把 Prompt v2 的 JSON_ONLY 从 18/20 提升为 20/20，并以零调用完成；COMBINED 从 15/20 提升为 20/20，20 例均只需一次受限视觉调用，且没有 unexpected entity/field/edge 或 unsupported assertion。与此同时 IMAGE_ONLY 从 14/20 退化为 0/20，全部在两次修复后仍无法满足严格 Candidate contract；因此全局通过数从 47/60 降为 40/60，不能将局部 Grounding 收益描述为整体质量提升。

当前 Profile 保持 `EXPERIMENTAL`、默认关闭。下一迭代应先增加不含 payload 的 IMAGE_ONLY validation-problem taxonomy，定位 Prompt/Profile v3 与 Candidate envelope 的系统性不匹配；不能降低 contract 门槛，也不能继承本账本的预算或重新打开 CLOSED authorization。

## 最终独立复核

独立 reviewer 从 CLOSED journal 重新聚合并得到 PASS，0 Blocker / 0 High / 0 Medium：

- 60 个 assignment/run 均唯一，mode `20/20/20`、partition `DEV 45 / HOLDOUT 15`，没有缺失或重复；20 `FAILED` 与 40 `REVIEW_REQUIRED` 全部保留。
- 80 个 reservation 全部唯一、`SETTLED` 且逐条 `actual ≤ reserved`；逐条重算 token 与 pinned Plus 阶梯价格得到 278,740 tokens / 908,984 micros CNY，与 state/summary 精确一致。时间序列最大预算负债为 983,596 micros，低于 ¥2。
- 独立重建 global、三个 mode、两个 partition 的全部 scalar/diagnostics，以及 17 项 policy violation，结论仍为 `EXPERIMENTAL`。
- 从目标 revision 的 identity-bound tree 重建出 `...67328db2...499a8`；PROPOSED→OPEN 与 OPEN→CLOSED 均只改变目标 ledger。独立 CLOSED 探针得到退出码 1 / `LIVE_CERTIFICATION_AUTHORIZATION_NOT_OPEN`，三份证据 hash 不变且无 Provider 调用迹象。
- state/summary/guard 全量扫描中，API key、Bearer、provider request ID、prompt、Candidate、图片/base64 与原始 payload 均为 0 命中。

诚实保留的证据边界：这些是本机 payload-free A2 文件，不是外部签名/A3；没有逐批 operator log，批量上限由 evaluation-identity 绑定的 harness、lease 与 state 证明；token 来自 Provider usage 回传，本轮只读审计验证内部一致性与费用公式，没有重新调用 Provider 验真。
