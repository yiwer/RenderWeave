# P5 / T5-10 checkpoint — IMAGE_ONLY diagnostic evaluation plan

- 日期：2026-08-09
- 分支：`phase/p5-image-only-attribution-v1`
- 基线：`2d5d8af`
- AC：AC-016、AC-020、AC-021
- 生命周期：`prelive_independently_reviewed`（精确 J1 待批准）
- 外部副作用：0 Provider attempt、0 token、¥0；ledger 为 `PROPOSED`

## 目标与边界

- 复用字节不变的 Grounded Profile，对 20 个仓库合成 IMAGE_ONLY case 收集 payload-free problem taxonomy。
- 不修改 Prompt/Profile/repair/validator/quality threshold，不新增产品 Profile、OpenAPI 或 UI。
- 诊断结果永远不是 AC-021 certification；真实调用前必须完成精确 identity、pre-live A1/A2 与当次 J1。

## 实现

- 新授权版本固定 `IMAGE_ONLY_DIAGNOSTIC` purpose、单一 Grounded Profile 和 20-case slice；版本已由 journal guard/state 绑定。
- 代码级硬门固定最多 60 attempts、¥2、每批 5 case、最长 4 小时；普通 gate 清除 key/selectors 且 paid test 默认 skipped。
- report 1.3 显式保存 purpose、`certificationEligible=false` 与 20 个授权 assignment；policy 对该 slice 只返回 `INCOMPLETE/DIAGNOSTIC_ONLY`。
- authorization loader 显式拒绝重复字段、未知字段、标量/浮点强制转换与尾随根值；ledger 自身不能把 ¥2 上限扩大到通用 Plus 上限。

## 当前验证

- 红基线：4 个预期编译失败证明诊断版本、purpose 与 eligibility 尚不存在；后续 `¥2.000001` 负例再次按预期失败，证明原实现可被 ledger 放宽。
- 定向绿：authorization 11/11、journal 14/14；paid harness 1 个按默认关闭 skipped。
- clean server A1：`.sdlc/evidence/20260809-182622-server/metadata.json`，revision `1663120`、dirty=false；Schema 20、Validation 13、Inference 90、App 115，0 failure，2 skipped。
- 独立 A2：重建 473 个 tracked evaluation input 的 identity，核对 version/corpus/profile/slice/budget/journal/report；0 Blocker、0 High、0 Medium。
- 精确 PROPOSED harness 负探针：退出码 1，根因 `LIVE_CERTIFICATION_AUTHORIZATION_NOT_OPEN`；目标 evidence 目录执行前后均不存在。
- 未读取 API key、未创建诊断 journal evidence、未调用 Provider；累计 0 attempt、0 token、¥0。

## 待完成门禁

- checkpoint 与 ledger 在同一最终 PROPOSED 节点重锚定；ledger 中的 evaluation identity 是权威值。
- 用户须对该 exact identity、Profile、20-case synthetic slice、60 attempts、¥2、5 case/批与 4 小时窗口给出新的精确 J1；批准前保持 `PROPOSED`。

## 恢复点

当前恢复点为本分支最新 pre-live 节点；基线 `2d5d8af` 可整体回退。本节点仅代码、测试、ADR、checkpoint 与 PROPOSED ledger，数据库和外部 Provider 均无副作用。
