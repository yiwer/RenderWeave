# P5 / T5-10 checkpoint — IMAGE_ONLY diagnostic live attribution

- 日期：2026-08-09
- 分支：`phase/p5-image-only-attribution-v1`
- 基线：`2d5d8af`
- AC：AC-016、AC-020、AC-021
- 生命周期：`live_independently_reviewed`（J1 + A1 + 独立 A2 PASS；authorization `CLOSED`）
- 外部副作用：60 Provider attempts / 197,321 tokens / ¥0.642106；仅使用仓库 synthetic corpus，未使用真实业务数据

## 目标与边界

- 复用字节不变的 Grounded Profile，对 20 个仓库合成 IMAGE_ONLY case 收集 payload-free problem taxonomy。
- 不修改 Prompt/Profile/repair/validator/quality threshold，不新增产品 Profile、OpenAPI 或 UI。
- 诊断结果永远不是 AC-021 certification；report 强制 `certificationEligible=false` 与 `INCOMPLETE/DIAGNOSTIC_ONLY`。

## 实现与 pre-live 验证

- 授权版本固定单一 Grounded Profile 与 20-case IMAGE_ONLY slice；journal guard/state 绑定 version、corpus、identity、Profile 与预算。
- 代码级硬门固定最多 60 attempts、¥2、每批 5 case、最长 4 小时；authorization loader 显式拒绝重复/未知/尾随字段与数值强制转换。
- 最终 pre-live clean server A1：`.sdlc/evidence/20260809-183022-server/metadata.json`（revision `25a7e61`）；独立 A2 为 0 Blocker / 0 High / 0 Medium。
- PROPOSED harness 负探针以 `LIVE_CERTIFICATION_AUTHORIZATION_NOT_OPEN` 拒绝，目标 evidence 目录前后均不存在。

## J1、Git 生命周期与执行

- 用户 `yiwer` 对 exact identity `renderweave-repository-tree-sha256/1:dae53e807c08b2b0b59b07329fe1af69d5862d534b4eb2e71eafa9deaefdb17f` 给出 J1。
- 精确信封：`qwen3.7-plus-2026-05-26` / `dashscope-qwen37-plus-20260526-grounded-v1`、20 个 repository-synthetic IMAGE_ONLY case、≤60 attempts、≤¥2、≤1,000,000 total tokens、≤5 case/批、4 小时、payload-free、diagnostic-only。
- Git 生命周期：`25a7e61`（PROPOSED）→ `deedff7`（OPEN）→ `e538c4b`（CLOSED）；OPEN 窗口 `2026-08-09T14:06:58Z–18:06:58Z`。
- 实际 Provider 窗口 `2026-08-09T14:08:57.3466254Z–14:29:21.5515091Z`，约 20.403 分钟。每个 case 都在 repair budget exhausted 后结束当批，因此形成 20 个串行恢复批次，每批实际 1 case。

## Live 结果

- 20 个 assignment/case/run 全部唯一；DEV 15、HOLDOUT 5；20 个 execution 全为 `COMPLETED`。
- 60 个唯一 reservation 全部 `SETTLED` 且逐项 `actual ≤ reserved`；20 STRUCTURE + 40 REPAIR，ordinal `0/1/2` 各 20 次。
- 60 attempts 全为 `REJECTED/LIVE_OUTPUT_REJECTED`；20 个 case 全为 `FAILED/LIVE_REPAIR_BUDGET_EXHAUSTED`。
- 唯一 taxonomy 为 `CANDIDATE_DECODE_VALUE_INVALID=60`：首轮 STRUCTURE 20/20、两轮 REPAIR 40/40 均在 strict Candidate 的值级解码边界被拒绝。
- 输入 156,077、输出 41,244，合计 197,321 tokens；按 pinned Plus ¥2/¥8 每百万 token 阶梯逐条重算为 642,106 micros CNY（¥0.642106），与 attempt、reservation、state、summary 完全一致。
- 历史 reservation 简单求和不是预算暴露；严格按 reserve/settle 时序重建的最大暴露为 713,412 micros CNY，最终 outstanding 为 0。
- 质量结果为 0/20 pass；bundle contract、entity/field/type/edge/evidence/DAG 指标均为 0。诊断计数为 missing entity 24、missing field 28、supported type mismatch 26、missing edge 4；critical hallucination 与 blocker 都为 0。

### 诊断结论

当前失败发生在 JSON 已进入 strict codec 后的值级合同绑定阶段（`ValueInstantiationException` 或 `InvalidFormatException` 分类），早于 CandidateValidator 与图片语义质量评分。两轮 REPAIR 仍 40/40 重复同一类别，说明系统性不兼容位于输出枚举/值构造合同，而不是通过降低质量阈值可以解决的问题。

下一步应先离线把 `CANDIDATE_DECODE_VALUE_INVALID` 拆成 payload-free 的枚举值、构造器不变量与有限 contract-slot 类别，再据此调整不可变 Prompt/Profile。不得读取历史原始输出、降低 strict contract 或重开本账本；任何新 live 复验都要新的 identity、PROPOSED ledger 与 J1。

## 证据、关闭探针与最终门禁

- Payload-free evidence：`.sdlc/evidence/p5-image-only-diagnostic-20260809/`。
- `state.json` SHA-256：`C8E94BF670C96DA86225E2C19148476CDACA4B8D4AD9A92A1E7E12E0CCFDAE36`。
- `summary.json` SHA-256：`783B67BE8F698814C63A8033370283A9C410BA108D0D70E1AD581D52AD453868`。
- `state.guard.json` SHA-256：`AC54D69C86595CCC0E1B9B6965E28C5923BED0F5B9FE6A97E30EF89F3CAA844E`。
- CLOSED harness 负探针：exit 1 / `LIVE_CERTIFICATION_AUTHORIZATION_NOT_OPEN`，三份 hash 前后不变。该 probe transcript 未单独持久化，只按 A0/self-reported 处理；最终 hashes 已由独立 reviewer 只读核对。
- 泄露扫描覆盖 95,015 个 evidence 字符：API key、Bearer、Provider request id、Prompt、Candidate JSON、图片、RootDocument、base64 均为 0 命中。
- CLOSED 后首次 server A1 `.sdlc/evidence/20260809-223256-server/metadata.json` 诚实为红：测试构造只匹配字面 `PROPOSED`，在 CLOSED ledger 上变成 no-op。`2eff540` 仅将该负例改为生命周期中立；随后 clean server A1 `.sdlc/evidence/20260809-223530-server/metadata.json` 全绿（Schema 20、Validation 13、Inference 90、App 115，2 paid skipped）。
- 独立 A2 严格锚定 live revision `e538c4b`，重建 assignment、reservation、费用、metrics、policy、泄露面与 lifecycle，结论 PASS：0 Blocker / 0 High / 0 Medium。post-live branch health 单独锚定 `2eff540`。

## 恢复边界

- 源码可分别 revert 独立节点；当前 ledger 已 CLOSED，不能继承或重开。
- Testcontainers 数据库随每批销毁；持久恢复来源是 payload-free journal，不存在业务数据库或真实业务数据写入。
- 已发生的 Provider 调用、token 与费用不可撤销；补偿路径是 CLOSED 阻断后续调用、保留预算账本并对结果做只读审计。
- Evidence 是本地未签名文件，不是 A3；未来任何 live 复验必须从新 identity 与新 J1 开始。
