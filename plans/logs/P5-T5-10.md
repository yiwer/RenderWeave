# P5 / T5-10 checkpoint — IMAGE_ONLY diagnostic evaluation plan

- 日期：2026-08-09
- 分支：`phase/p5-image-only-attribution-v1`
- 基线：`2d5d8af`
- AC：AC-016、AC-020、AC-021
- 生命周期：`implementation_complete`（pre-live A1/A2 与 identity freeze 待完成）
- 外部副作用：0 Provider attempt、0 token、¥0；ledger 为 `PROPOSED`

## 目标与边界

- 复用字节不变的 Grounded Profile，对 20 个仓库合成 IMAGE_ONLY case 收集 payload-free problem taxonomy。
- 不修改 Prompt/Profile/repair/validator/quality threshold，不新增产品 Profile、OpenAPI 或 UI。
- 诊断结果永远不是 AC-021 certification；真实调用前必须完成精确 identity、pre-live A1/A2 与当次 J1。

## 实现

- 新授权版本固定 `IMAGE_ONLY_DIAGNOSTIC` purpose、单一 Grounded Profile 和 20-case slice；版本已由 journal guard/state 绑定。
- 设计上最多 60 attempts、¥2、每批 5 case、最长 4 小时；普通 gate 清除 key/selectors 且 paid test 默认 skipped。
- report 1.3 显式保存 purpose、`certificationEligible=false` 与 20 个授权 assignment；policy 对该 slice 只返回 `INCOMPLETE/DIAGNOSTIC_ONLY`。

## 当前验证

- 红基线：4 个预期编译失败，证明诊断版本、purpose 与 eligibility 尚不存在。
- 绿：authorization/journal 23/23；额外 paid harness 1 个按默认关闭 skipped。
- 未读取 API key、未创建 journal evidence、未调用 Provider。

## 待完成门禁

- 完成仓库 ledger contract test、server clean A1 与独立 A2。
- 在最终 tracked tree 上计算 evaluation identity，仅更新选定 ledger 并提交 PROPOSED 节点。
- 执行 PROPOSED 负探针，确认网络、费用和 evidence 均为零。

## 恢复点

当前源码恢复点为 `2d5d8af`；本节点仅代码、测试、ADR 与 PROPOSED ledger，数据库和外部 Provider 均无副作用。
