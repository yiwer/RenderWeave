# TV1-T40：definite Grid singleton AUTO fixed-child contribution

- 结果：完成
- 生命周期状态：`automated_verified`
- 验证时 revision：`28e862d` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\\Yiwer\\code\\RenderWeave-template-v1-implementation`
- 外部副作用：Provider attempts=0、API Key reads=0、open authorization=0、paid external calls=0；未发送真实
  数据；只运行仓库 gate、offline Cargo/Python 与 network-none Linux UDS replay；未 push/tag/PR

## 产品增量

1. workspace-internal `renderweave-renderer-layout` 的唯一 arrange 入口继续消费 T23/T25 同一 admitted/preflight
   tree；在 T39 singleton FRACTION Grid 闭包上，每个 definite Grid 轴新增至多一个资源无关 AUTO，不新增
   parser、crate、route、Profile 或 daemon success path。
2. 只有 span 覆盖 AUTO 的 direct GRID child 参与该轴求解；该轴必须为 FIXED，contribution 固定为
   `max(0,(fixedSize+leadingMargin)+trailingMargin)`。约束按 `(spanLength,startIndex,materializedOrder)` 稳定排序，
   先扣跨度内已结算 track sizes 与内部 gaps，FRACTION 在 AUTO 阶段为零，唯一 AUTO 单调接收正 deficit。
3. 每轴严格 FIXED → AUTO → FRACTION，且 columns-first。AUTO 结算后 singleton FRACTION 才取得
   `max(0,available-fixed-auto-allDeclaredAdjacentGaps)`；track origins 仍按 authored order，零尺寸 AUTO/FRACTION
   均不吞掉相邻 gap。
4. 多 AUTO 仍在容器 occurrence 返回 `GRID_AUTO_TRACK`；HUG contribution 在 AUTO 阶段精确定位 child 并返回
   `HUG_CONTENT`；多 FRACTION 仍返回 `GRID_FRACTION_TRACK`。cell/span、signed-margin arrange、FIXED/FILL、
   min/max、alignment、overlap、递归与全有或全无输出保持不变。
5. 一般 intrinsic/HUG、multiple AUTO/FRACTION、multiple Stack FILL、Group/compositionViewport、Text/Image、
   resource fetch/decode/shaping、world scene、paint/raster/JPEG、daemon RESULT、Profile registration 与公开
   render/preview 均未实现。

## TDD、审查与证据

- RED：先把 vector/verifier identity 升级为 `/6`，把空 AUTO 与 FRACTION-column/AUTO-row 转为 laid-out，并加入
  signed-margin multi-track AUTO、multiple-AUTO stage 与 HUG contribution cases。Rust primary 首先在空 AUTO
  返回既有 `GRID_AUTO_TRACK`，且 HUG case 停在 Grid occurrence；Python independent 同样在空 AUTO 拒绝。
- GREEN：Rust 与独立 Python 分别实现完整 authored track scan、singleton AUTO stable constraints 与后置
  singleton FRACTION；最终共同通过 33 laid-out + 12 unsupported，45/45、137 checks。
- focused Rust exact vectors 3/3、Python stdlib independent replay、workspace fmt、clippy `-D warnings`、workspace
  tests、py_compile、PowerShell gate、JSON inventory 与 `git diff --check` 均通过。
- Render gate 1.5 在 Windows Rust 与 `rust:1.89-slim` network-none Linux UDS 上均通过，Linux source mount
  read-only、actual UDS round trip=true；physical host certification 仍为 false。
- Server 最终 BUILD SUCCESS；8/8 reactor，Application 344 tests、0 failure、0 error、15 controlled skips。测试
  上下文销毁后 scheduler/Hikari pool 打印旧 Testcontainer 端口连接拒绝并触发 Surefire 30 秒强制 fork 退出，
  但未形成 test/gate failure，且与本票 renderer-only diff 无关；如实保留在原始 server log。
- Java/OpenAPI/generated SDK/migration/API version/Web/route 零产品差异；API contract 保持 `0.15.0`，runtime
  migration count 保持 25。

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1/A2 | Rust primary + Python independent 45/45、137 checks；fmt/clippy/test/py_compile/diff-check 通过 | 当前命令输出 |
| Render | A1；exact replay A2 | gate 1.5；Java 26；Rust Windows + Linux network-none UDS；Python 110+14+32+45+16 | `.sdlc/evidence/20260821-230451-render/` |
| Server | A1 | 8/8 reactor；App 344 tests、0 failure、15 controlled skips | `.sdlc/evidence/20260821-230527-server/` |
| Affected Fast | A1 | repository diff、八模块 package 与 Node 24 Web typecheck 全过 | `.sdlc/evidence/20260821-232407-fast/` |
| Final Fast | A1 | resolution governance 后执行，exact 目录只在 commit handoff 报告 | `.sdlc/evidence/<final-passing-fast>/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在 commit handoff 报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |

## 合同 identity 与保证上限

- definite vector/verifier identity 为 `/6`，fixture identity 为 `/3`；vector SHA-256
  `b54a2ac0795be29c7ac22764f7765cca52273b74af33de715e1dbeb61e9aac29`，fixture SHA-256
  `a11475bcebad7e1c35cb0acd7018419d94afcb4b37d7f1df7346a055ad1df669`。
- independent layout implementation 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_SINGLE_FRACTION_SINGLE_AUTO_GRID_BOX_KERNEL`；
  render summary 的 `layoutKernel` 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_SINGLE_FRACTION_SINGLE_AUTO_GRID_BOX_AUTOMATED_VERIFIED_UNWIRED`。
- Profile 仍 NOT_REGISTERED、certification NOT_CERTIFIED、world scene/raster ABSENT、daemon output UNWIRED、
  Renderer READY=false。
- T40 不证明一般 intrinsic/HUG、multiple AUTO/FRACTION、multiple Stack FILL water filling、完整 Layout Profile、
  resource/shaping、world transform/scene、pixel/JPEG、daemon RESULT、physical Linux certification、formal
  Case/Oracle、A3、新 J1、E6 或 Template/Renderer READY。
- rollback 边界为本票单一 verified commit；无 migration 或外部持久状态需逆转。未经授权不 push/tag/PR。
