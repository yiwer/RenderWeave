# TV1-T47：Command-bound RenderResource lease admission

- 结果：完成
- 生命周期状态：`automated_verified`
- 起始 revision：`d32512fc0495feae9f7ed805c1bacdcb5453cef3`
- 验证时 revision：`d32512f` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\\Yiwer\\code\\RenderWeave-template-v1-implementation`
- single-writer：Codex `/root`
- 外部副作用：禁止 provider、API Key、真实数据、付费调用与 push/tag/PR

## 决策与边界

1. T46 verified commit 后重读 Ticket 13/16/19 与 Rust protocol/document/daemon seam。Command 的 exact
   millisecond deadline、typed resource 的 epoch-second expiry 与 Ticket 19 minimum-inclusive 5000ms oracle 已
   冻结，可在没有网络、Clock、Resolver、Profile 或新产品语义时形成纯 admission invariant。
2. document deep module 按 manifest order 在 `i128` 域比较
   `expiresAtEpochSecond * 1000 >= deadlineEpochMillis + 5000`，从而同时保证 5000 精确接受、4999 拒绝和
   `u64` 最大 expiry 不溢出；失败只携带首个 opaque resourceId。
3. daemon 在 document admission 后、layout/fetch/raster/RESULT 前形成并缓存
   `RESOURCE_LEASE_EXPIRED/COMMAND_ADMISSION`，exact Command replay 返回相同 canonical problem。canonical
   HTTPS/app-origin、actual fetch/attempt-time checks、bytes/hash/media/decode/cache、scene/raster/RESULT/Profile
   继续 fail closed。

## TDD、审查与证据

- [x] RED：shared vector `/3` 先使 Rust primary 与 Python independent verifier 因 identity 漂移失败
- [x] GREEN：Rust pure lease admission、canonical resource problem、daemon first-error/exact replay 与 Python mirror
- [x] focused fmt/clippy `-D warnings`/workspace test/Java contract/py_compile/JSON inventory/diff-check
- [x] `render`、受影响 `server`/`fast`
- [ ] 最终 `fast`、最终 `full`
- [ ] verified local commit 与 clean worktree

## 合同 identity 与保证上限

- RenderDocument/resource shared vector 与 Python verifier identity 为 `/3`；14 document + 42 resource scalar/
  descriptor + 19 aggregate + 8 lease = 83/83 cases、106 checks；vector SHA-256
  `ba0680eb5506062b887674f513ad4f9026d56a974c5b541f28ff6098c4c8de3a`，all-kinds fixture SHA-256 保持
  `1b83a605c13837b0fa6d3a3cbf5e84fb97c71116ba8a81942cf97a3d7df9b031`。
- Profile 保持 NOT_REGISTERED、certification NOT_CERTIFIED、resource bytes UNFETCHED、world scene/raster ABSENT、
  daemon output UNWIRED；无 actual fetch、A3/J1/READY。
- rollback 边界为本票单一 verified commit；无 migration 或外部持久状态需逆转。

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1/A2 | Rust primary + Python independent 83/83、106 checks；fmt/clippy/test/Java contract/py_compile/JSON inventory/diff-check 通过 | 当前命令输出 |
| Render | A1；exact replay A2 | gate 1.8；Java 26；Rust Windows + Linux network-none UDS；Python process 110 + document 83 + preflight 32 + layout 64 + PNG 16 | `.sdlc/evidence/20260822-075544-render/` |
| Server | A1 | 8/8 reactor；Inference 361、Template 78、Asset 90、Rendering 109、App 344，0 failure/error | `.sdlc/evidence/20260822-075614-server/` |
| Affected Fast | A1 | repository diff、server package 与 Node 24 Web typecheck 全过 | `.sdlc/evidence/20260822-081600-fast/` |
| Final Fast | A1 | resolution governance 后执行，exact 目录只在 commit handoff 报告 | `.sdlc/evidence/<final-passing-fast>/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在 commit handoff 报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |
