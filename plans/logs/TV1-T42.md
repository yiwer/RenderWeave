# TV1-T42：definite empty-container HUG intrinsic

- 结果：完成
- 生命周期状态：`automated_verified`
- 起始 revision：`40371c3ccd9c0d38c02729e9db5ef98efe2461c9`
- 验证时 revision：`40371c3` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\\Yiwer\\code\\RenderWeave-template-v1-implementation`
- single-writer：Codex `/root`
- 外部副作用：禁止 provider、API Key、真实数据、付费调用与 push/tag/PR

## 决策与边界

1. T41 verified commit 后重读原始 Ticket 10/16/19 与当前实现。跨多 AUTO、multiple FRACTION 与 multiple
   Stack FILL 仍依赖未物化 residual tolerance；非空 Group/Frame HUG 需要 child transform conformance；
   Text/Image 需要资源准备。空容器 HUG 的结果则由冻结规格直接给出，不需要这些前置。
2. 本票只深化 `layout_definite_resource_free`：空 Frame/Stack 以 stroke+padding 得到自然尺寸；空 Grid 以
   FIXED/AUTO-zero tracks+gaps+stroke+padding 得到自然尺寸；Frame/Stack/Grid 再应用 HUG min/max；空 Group 遵守
   T25 已冻结的 min/max 禁令并退化为零。
3. TDD 先升级 shared vector/verifier `/8` 并捕获 Rust/Python RED；fixture bytes 未变时保持 `/3`。

## TDD、审查与证据

- [x] RED vectors：空 Frame/Stack/Grid 在实际 occurrence 返回旧 `HUG_CONTENT`，空 Group 返回旧 `GROUP`；
  Rust primary 与 Python independent replay 同时失败
- [x] GREEN：Rust primary 与 Python independent mirror 均完成空容器 HUG intrinsic；共同通过
  40 laid-out + 13 unsupported，53/53、160 checks
- [x] focused Rust exact vectors 3/3、Python stdlib independent replay、workspace fmt、clippy
  `-D warnings`、workspace tests、py_compile、JSON inventory 与 `git diff --check`
- [x] `render`：Windows Rust 与 `rust:1.89-slim` network-none Linux UDS 均通过，Profile NOT_REGISTERED、
  certification NOT_CERTIFIED、raster ABSENT、daemon output UNWIRED
- [x] `server`：8/8 reactor BUILD SUCCESS；Application 344 tests、0 failure、0 error、15 controlled skips
- [x] 受影响 `fast`：repository diff、八模块 package 与 Node 24 Web typecheck 全过
- [ ] governance-final `fast`（exact 目录只在 commit handoff 报告）
- [ ] final `full`（exact 目录只在 commit handoff 报告）
- [ ] verified local commit 与 clean worktree

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1/A2 | Rust primary + Python independent 53/53、160 checks；fmt/clippy/test/py_compile/diff-check 通过 | 当前命令输出 |
| Render | A1；exact replay A2 | gate 1.6；Java 26；Rust Windows + Linux network-none UDS；definite replay 40+13 | `.sdlc/evidence/20260822-012213-render/` |
| Server | A1 | 8/8 reactor；App 344 tests、0 failure、15 controlled skips | `.sdlc/evidence/20260822-012240-server/` |
| Affected Fast | A1 | repository diff、八模块 package 与 Node 24 Web typecheck 全过 | `.sdlc/evidence/20260822-014339-fast/` |
| Final Fast | A1 | resolution governance 后执行，exact 目录只在 commit handoff 报告 | `.sdlc/evidence/<final-passing-fast>/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在 commit handoff 报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |

## 合同 identity 与保证上限

- definite vector/verifier identity 为 `/8`，fixture identity 为 `/3`；vector SHA-256
  `25e0a8d2ba97bcef3a3fb03a70fc140093b80c967ae64658b129c5d28a8decfb`，fixture SHA-256
  `a11475bcebad7e1c35cb0acd7018419d94afcb4b37d7f1df7346a055ad1df669`。
- independent layout implementation 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_SINGLE_FRACTION_INDEPENDENT_MULTI_AUTO_GRID_EMPTY_INTRINSIC_CONTAINER_BOX_KERNEL`；
  render summary 的 `layoutKernel` 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_SINGLE_FRACTION_INDEPENDENT_MULTI_AUTO_GRID_EMPTY_INTRINSIC_CONTAINER_BOX_AUTOMATED_VERIFIED_UNWIRED`。
- Profile 仍 NOT_REGISTERED、certification NOT_CERTIFIED、world scene/raster ABSENT、daemon output UNWIRED、
  Renderer READY=false。

## 保证上限

- 一般 HUG/intrinsic、非空容器、transform union、Text/Image/Vector measurement、multiple FILL/FRACTION、跨
  多 AUTO 平均、resource/world scene/raster/JPEG/daemon RESULT/Profile/E6 均保持 fail closed。
- Profile 仍 NOT_REGISTERED、certification NOT_CERTIFIED、world scene/raster ABSENT、daemon output UNWIRED；
  无 A3/J1/READY、provider/API Key/真实数据/付费调用或 push/tag/PR。
- rollback 边界为本票单一 verified commit；无 migration 或外部持久状态需逆转。未经授权不 push/tag/PR。
