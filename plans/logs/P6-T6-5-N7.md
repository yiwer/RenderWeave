# P6-T6-5 N7 checkpoint：三模型 reachability、bounded rewind 与 owner-bound hierarchy

- 结果：N7 持续推进，final eval 与最终验收未完成
- 生命周期状态：`in_progress`；二十三份 Provider-backed reachability live evidence 为 A2，v15/v16/v17 verifier、v18 detailed
  repair taxonomy、v19 exact-duplicate support-ID normalization 与 v20 unique evidence-owned relationship region
  normalization、v21 unique connected relationship region normalization 与 v22 unique exact-region GROUP-owner
  support normalization、v23 support-owner hybrid observation、v24 bounded observation normalization、v25
  leaf-evidence OBSERVE verifier、v26 unique enclosing-connected GROUP-owner 与 v27 unique source-ancestor
  GROUP-owner 增量为 clean A1；v27 Flash/Plus/Max single-case evidence 为 A2，但质量未达门
- 分支：`phase/p6-visual-recognition-vnext`
- 当前 CLOSED lifecycle revision：`95be8fa`；当前实现 revision：`3a56af9`；预算事务修复 revision：`5ada0fa`；当前治理 revision：`2b23617`
- 关联：ADR-0028、ADR-0029、AC-VR-001..010
- 终态：Flash、Plus、Max 三份 visual ledger 均 `CLOSED`；Profile 全部隐藏 `EXPERIMENTAL`；Flash v27b
  仍未通过 OBSERVE，Plus/Max v27 已完成三阶段但 slot/binding 0 matched、质量未达门；v27 新 telemetry
  三模型均未命中，final 20/60、最终 verifier/full 与业务/视觉 J1 仍未完成

## Goal 与治理再锚定

编排 Goal `019fec8e-a851-7952-b49b-8be76a281a57` 恢复后因 turn interrupt 当前显示 `paused`；用户已明确继续
同一 objective，没有创建 replacement Goal。2026-08-11 用户首个 J1 delta 将 Flash 精确替换为
`qwen3.7-flash-2026-07-15`，Plus/Max model ID 不变，并给三个稳定预算
槽位各追加 500,000 exposed tokens；用户随后明确允许 `qwen3.7-plus` 调用，并再次给每个稳定槽位追加
500,000。当前累计 cap 为 1,500,000；单 authorization 500,000、attempt、CNY、时限与 batch 边界不变。

`252dc00` 新增 pinned Flash immutable capability/Profile、Goal guard v2 与旧/新 Flash alias 聚合；v1 guard
只在锁内验证历史 state 后原子迁移，reservations 不删除、不改写、不清零。`0d7b73c` 让 live runner 的费用
断言也使用稳定槽位。历史 v1 evidence 仍可独立重放，Authorization 自身仍受 500,000-token 上限约束。
`2b23617` 将当前 guard 升为 v3；只接受 exact v1/v2 迁移，先按旧 cap 验证 state，再原子写 v3。独立 verifier
同时接受与各自 500k/1M/1.5M cap 精确绑定的 v1/v2/v3 历史 evidence。

## 单 case reachability smoke

十二次都只使用仓库合成 `transit-board-v3`，每次最多 5 attempts、batch 1，并执行
PROPOSED→`NOT_OPEN` 负探针→OPEN→CLOSED。负探针未创建 target evidence，Goal state/hash 与 reservations
不变；live 包装器均只运行一份，ledger 在读取 evidence 前已 CLOSED。

| 授权 / Profile | identity / snapshot | attempts | exposed tokens | 最远阶段 | 终态 | A2 |
|---|---|---:|---:|---|---|---|
| `visual-v13-pinned-flash-smoke-qwen37-flash-transit-20260811` / pinned Flash product-v13 | `…bea8619` / `11bc9a…0319` | 5 | 35,339 | OBSERVE | `VISUAL_GROUNDING_PARENT_KIND_INVALID` | PASS |
| `visual-n7-reachability-qwen37-plus-transit-20260811` / Plus product-v12 | `…bea8619` / `3ffbcb…bca7` | 5 | 19,810 actual；44,693 exposed | HIERARCHY | `DASHSCOPE_HTTP_400` | PASS |
| `visual-n7-hierarchy-v14-qwen37-plus-transit-20260811` / Plus product-v14 | `…f4985` / `1d28f9…b846` | 5 | 23,620 | HIERARCHY | `VISUAL_HIERARCHY_V2_RELATIONSHIP_SUPPORT_IDS_INVALID` | PASS |
| `visual-n7-observation-rewind-v15-qwen37-plus-transit-20260811` / Plus product-v15 | `…69add7` / `09e191…cf639` | 5 | 25,768 | HIERARCHY | `VISUAL_HIERARCHY_V2_SUPPORT_CARDINALITY_MISMATCH` | PASS |
| `visual-n7-cardinality-v16-qwen37-plus-transit-20260811` / Plus product-v16 | `…1a7345d` / `d78b1b…9de5e` | 5 | 27,482 | HIERARCHY | `VISUAL_HIERARCHY_V2_SUPPORT_GROUP_REUSED` | PASS |
| `visual-n7-region-owner-v17-qwen37-plus-transit-20260811` / Plus product-v17 | `…abb8bb6` / `0f4a13…0c385` | 5 | 35,231 | HIERARCHY | `VISUAL_HIERARCHY_V2_SUPPORT_NOT_GROUP` | PASS |
| `visual-n7-region-diagnostics-v18-qwen37-plus-transit-20260811` / Plus product-v18 | `…6a9059` / `d38696…49416` | 5 | 25,194 | HIERARCHY | `VISUAL_HIERARCHY_V2_RELATIONSHIP_SUPPORT_IDS_INVALID` | PASS |
| `visual-n7-support-normalization-v19-qwen37-plus-transit-20260811` / Plus product-v19 | `…2416ab` / `c6df27…5b41d` | 5 | 31,059 | HIERARCHY | `VISUAL_HIERARCHY_V2_RELATIONSHIP_REGION_CARDINALITY_INVALID` | PASS |
| `visual-n7-region-normalization-v20-qwen37-plus-transit-20260811` / Plus product-v20 | `…dbd4` / `f8423a…ac11` | 5 | 30,337 | HIERARCHY | `VISUAL_HIERARCHY_V2_RELATIONSHIP_SUPPORT_IDS_EMPTY` | PASS |
| `visual-n7-connection-normalization-v21-qwen37-plus-transit-20260811` / Plus product-v21 | `…899ad` / `9e2deb…0a40f` | 5 | 27,525 | HIERARCHY | `VISUAL_HIERARCHY_V2_SUPPORT_NOT_GROUP` | PASS |
| `visual-n7-support-owner-v22-qwen37-plus-transit-20260811` / Plus product-v22 | `…dedec0` / `71af8d…95e18` | 5 | 26,943 | ELEMENT_BINDING | `LIVE_VISUAL_BINDINGS_V2_ACCEPTED` | PASS |
| `visual-n7-max-v22-qwen38-max-transit-20260811` / Max product-v22 | `…855f974` / `74fe60…24e6d` | 3 | 14,481 | ELEMENT_BINDING | `LIVE_VISUAL_BINDINGS_V2_ACCEPTED` | PASS |

独立 verifier 对 pinned Flash 重建出 1 completed、0 abandoned、5 provider attempts、11,707 input + 23,632
output tokens、161,450 ms、428,373 Flash-slot Goal tokens、¥0.190286、payload scan PASS。五次 attempt 均停在
OBSERVE，错误在 region kind 与 parent kind 之间切换。

Plus 独立 verifier 重建出 1 completed、0 abandoned、5 provider attempts、11,849 input + 7,961 output tokens、
140,569 ms、530,579 Plus-slot Goal tokens、¥2.227000、payload scan PASS。前两次 OBSERVE 因 parent kind
拒绝，第三次 OBSERVE accepted，第四次 HIERARCHY 因 `VISUAL_HIERARCHY_V2_ENTITY_INVALID` 拒绝，第五次
HIERARCHY 返回 `DASHSCOPE_HTTP_400` 且无 usage。对应 Goal reservation 保持 RESERVED 并按保守上界计入，
没有释放或并发重跑。

Plus v14 独立 verifier 重建出 1 completed、0 abandoned、5 provider attempts、18,992 input + 4,628 output tokens、
84,107 ms、554,199 Plus-slot Goal tokens、¥2.302008、payload scan PASS。首个 OBSERVE accepted，但产出 9 SLOT、
0 GROUP；四个 HIERARCHY responses 依次因 support element unknown、parent count invalid、support element unknown、
relationship support IDs invalid 被拒绝，仍未进入 ELEMENT_BINDING。五个新 reservation 全部 SETTLED；执行前后
只存在一个 wrapper，ledger 在读取 report 前已 CLOSED。

Plus v15 独立 verifier 重建出 1 completed、0 abandoned、5 provider attempts、15,823 input + 9,945 output tokens、
179,859 ms、579,967 Plus-slot Goal tokens、¥2.413214、payload scan PASS。前两次 OBSERVE 分别因 element evidence
outside region 与 parent kind 被拒绝，第三次 accepted 并产出 8 SLOT、1 GROUP；0-GROUP rewind 条件因此没有
触发。后两次 HIERARCHY 都因 support cardinality mismatch 被拒绝，仍未进入 ELEMENT_BINDING。五个新增
reservation 全部 SETTLED，且 wrapper 结束后无遗留进程。

Plus v16 独立 verifier 重建出 1 completed、0 abandoned、5 provider attempts、19,201 input + 8,281 output
tokens、147,141 ms、607,449 Plus-slot Goal tokens、¥2.517864、payload scan PASS。首次 OBSERVE 因 element
evidence outside region 被拒，第二次 accepted 并产出 10 SLOT、1 GROUP；后三次 HIERARCHY 依次为 support
group reused、relationship support IDs invalid、support group reused，仍未进入 ELEMENT_BINDING。五个新
reservation 全部 SETTLED，wrapper exit 0 后先 CLOSED 才读取 evidence，且无遗留进程。

Plus v17 独立 verifier 重建出 1 completed、0 abandoned、5 provider attempts、27,498 input + 7,733 output
tokens、142,447 ms、642,680 Plus-slot Goal tokens、¥2.634724、payload scan PASS。OBSERVE 首次 accepted 并产出
20 SLOT、1 GROUP；四次 HIERARCHY 依次为三次 region ownership invalid 与一次 support not group，exact-owner
rewind 没有触发，仍未进入 ELEMENT_BINDING。五个新 reservation 全部 SETTLED，wrapper exit 0 后先 CLOSED 才
读取 evidence，且无遗留进程。

Plus v18 独立 verifier 重建出 1 completed、0 abandoned、5 provider attempts、20,274 input + 4,920 output
tokens、89,402 ms、667,874 Plus-slot Goal tokens、¥2.714632、payload scan PASS。OBSERVE 首次 accepted 并产出
9 SLOT/0 GROUP；四次 HIERARCHY 均为 relationship support IDs invalid，仍未进入 ELEMENT_BINDING。五个新
reservation 全部 SETTLED，wrapper exit 0 后先 CLOSED 才读取 evidence，且无遗留进程。

Plus v19 独立 verifier 重建出 1 completed、0 abandoned、5 provider attempts、24,956 input + 6,103 output
tokens、109,700 ms、698,933 Plus-slot Goal tokens、¥2.813368、payload scan PASS。OBSERVE 首次 accepted 并产出
13 SLOT/1 GROUP；四次 HIERARCHY 均为 relationship region/cardinality invalid，仍未进入 ELEMENT_BINDING。
五个新 reservation 全部 SETTLED，唯一 wrapper exit 0 后先 CLOSED 才读取 evidence，且无遗留进程。

Plus v20 独立 verifier 重建出 1 completed、0 abandoned、5 provider attempts、24,251 input + 6,086 output
tokens、109,414 ms、729,270 Plus-slot Goal tokens、¥2.910558、payload scan PASS。OBSERVE 首次 accepted 并产出
12 SLOT/1 GROUP；四次 HIERARCHY 依次为 support not group、两次 relationship region connection invalid、
support IDs empty，仍未进入 ELEMENT_BINDING，也未命中 region normalization telemetry。五个新 reservation
全部 SETTLED，唯一 wrapper exit 0 后先 CLOSED 才读取 evidence，且无遗留进程。

Plus v22 独立 verifier 重建出 1 completed、0 abandoned、5 provider attempts、19,659 input + 7,284 output
tokens、128,862 ms、783,738 Plus-slot Goal tokens、¥3.116058、payload scan PASS。第一个 OBSERVE 因 evidence
outside region 被拒，第二个 accepted；第一个 HIERARCHY 因 relationship region/cardinality 被拒，第二个
accepted 且只记录 cardinality-derived telemetry；随后 ELEMENT_BINDING accepted。未命中 v22 support-owner
normalization；report 仍 `complete=false`，slot/group/entity/relationship/binding 的 gold 匹配远未达标。

Max v22 独立 verifier 重建出 1 completed、0 abandoned、3 provider attempts、11,318 input + 3,163 output
tokens、61,032 ms、443,297 Max-slot Goal tokens、¥9.454404、payload scan PASS。OBSERVE、HIERARCHY 与
ELEMENT_BINDING 均一次 accepted、无 problem code/repair，但实际为 11 SLOT/0 GROUP、1 entity/0 relationship、
11 bindings；只有 1 slot/1 entity 匹配，bindings 0/10、tree edit 30/32，未命中 v22 normalization。唯一
Provider wrapper 的测试主体完成后，外层 PowerShell 因 Mockito stderr warning 未返回 Maven 摘要；检查无存活
子进程、evidence lease 已完成后立即 CLOSED，未重跑。

因此 Plus/Max 均证明 OBSERVE→HIERARCHY→BINDING live 可达，但两者质量都不足以进入 final eval；十二份 A2
只证明授权、预算、journal/report 与 payload 边界闭环，不证明模型质量或 final policy。

## Hierarchy v14 bounded repair

在不读取 Provider 原文的前提下，本地 contract 诊断确认：旧 `VISUAL_HIERARCHY_V2_ENTITY_INVALID` 混合了
entity ID、schema key、display name 与 supporting IDs；crop selector 又为任何 hierarchy/binding 结构码追加
视觉 crop。无法从安全 telemetry 证明 HTTP 400 的供应商原因，因此只修复本地可证明的问题，不作外部归因。

`98ba3d0` 完成：

1. entity/relationship 字段级固定 taxonomy，不保存异常 message、动态 ID 或模型值；既有 topology/support/
   semantic verifier 继续 fail-closed。
2. 只有 `VISUAL_SEMANTIC_HIERARCHY_*` 与 `VISUAL_SEMANTIC_BINDING_*` 才从已验证 checkpoint 派生 crop；
   纯结构重试只携带固定问题码与已验证 plan，避免无依据扩张图片请求。
3. immutable hierarchy prompt v5 与 pinned Flash、Plus、Max product-v14 Profile 固化相同合同；只新增版本，
   不改写历史 Profile/Prompt/snapshot，Max Profile 的存在不构成调用授权。
4. worker/real-PG 集成验证 OBSERVE checkpoint 保留、HIERARCHY 原地重试、精确 code 累积且 task JSON 无
   `TARGETED_CROP`；语义 hierarchy/binding crop 回归保持绿色。

## V15 bounded OBSERVE rewind

v14 的安全 telemetry 证明 HIERARCHY 已提出 relationship，但已验证 inventory 为 0 GROUP；继续原地重试
HIERARCHY 不可能创建上游元素。`195894b` 完成以下有界延伸：

1. verifier 仅在“relationship 非空 + inventory 0 GROUP”时输出
   `VISUAL_SEMANTIC_OBSERVE_RELATIONSHIP_GROUP_MISSING`，`earliestStage=OBSERVE`；零 relationship 的合法扁平
   hierarchy 继续通过。
2. store 只允许 REJECTED HIERARCHY + 该精确单码事务性回到 NORMALIZE checkpoint；保留 provider call/repair
   计数，清空 element/grounding/hierarchy/binding plan 与 Candidate。通用 `InferenceStage` 仍禁止回退。
3. worker 仅在调用预算仍有余额时恢复 OBSERVE；新 task 携带固定问题码，不携带 prior plan 或
   `TARGETED_CROP`。crash/lease-expiry 后的第二 worker 可从该 checkpoint 继续。
4. immutable element prompt v7 与三模型 product-v15 Profile 固化同一通用合同；UI/E2E 把该码映射为 OBSERVE
   重复区域问题，不展示模型文本、局部 ID、坐标、图片或 Candidate。Max Profile 资源不构成调用授权。

## V16 evidence-derived relationship cardinality

v15 已得到 1 个 GROUP，却连续两次因 model-owned relationship cardinality 与该 GROUP multiplicity 冲突而
停止。相同 prompt 已明确要求二者一致，因此不再重复同一 live 假设。`bb15096` 完成以下版本化收敛：

1. pipeline 4.3 在 relationship 恰好引用一个已知 GROUP 时，从该 GROUP 已验证的 multiplicity 确定内部
   cardinality；模型继续负责 relationship 的 endpoint、field key、region 与 support，不由本地补造层级。
2. 多于一个支撑、未知元素或非 GROUP 支撑分别用固定码 fail-closed；旧 pipeline 4.1/v15 入口仍使用模型声明并
   拒绝 mismatch，历史 Profile/snapshot/journal 可重放。
3. semantic verifier 继续只拒绝；确定性 codec 只消除可由上游证据唯一推导的冗余字段。成功 HIERARCHY attempt
   记录 `VISUAL_HIERARCHY_RELATIONSHIP_CARDINALITY_DERIVED` 次数，UI 显示受控说明而不显示模型值或局部 ID。
4. 三模型 immutable product-v16 Profile 与独立 evidence verifier 的 4.3 snapshot 路由已冻结；Max Profile
   资源仍不构成调用授权。

合同/Profile 定向测试 12/12、真实 PostgreSQL 脚本 provider 1/1，后者在一个模型 cardinality 冲突下由三个
唯一 GROUP 证据继续完成 OBSERVE→HIERARCHY→BINDING 与 Candidate materialization。该结果是 A1 离线可达性，
尚不是 live 三阶段证据；本增量没有 Provider 调用，三个 Goal 累计与下表保持不变。

## V17 exact relationship-region GROUP owner rewind

v16 live 的一个 observed GROUP 被多条 relationship 复用。最初的“relationship 数量大于 distinct GROUP 数量”
回退假设在实现前被否决：数量差既可能是 OBSERVE 漏 GROUP，也可能是 HIERARCHY 多造 relationship，不能唯一
定位最早失败阶段。`31a8c6f` 因此只接受更窄的证据闭环：

1. pipeline 4.4 先验证 hierarchy 的 entity/relationship region ownership；只有 relationship 的 exact region 是
   已验证 GROUP/REPEATED_GROUP 且没有任何 observed GROUP element owner 时，输出
   `VISUAL_SEMANTIC_OBSERVE_RELATIONSHIP_REGION_GROUP_MISSING` 并回到 OBSERVE。
2. relationship region 已有 owner 时，GROUP reuse、support IDs、endpoint 或多余 relationship 仍在 HIERARCHY
   原地修复；不以 count 代替证据，不复制/合成 GROUP，不把 hierarchy 内容写回 observation。
3. store 仅允许精确单码的事务 rewind；checkpoint 保留 provider-call 计数并清除 element/grounding/hierarchy/
   binding/Candidate。恢复 task 只携带固定问题码，不携带 prior plan 或 `TARGETED_CROP`。
4. immutable element prompt v8 与三模型 product-v17 Profile、独立 verifier 4.4 路由、payload-free monitor/review
   UI 已冻结；旧 v16 诊断顺序与 snapshot 保持不变，Max Profile 仍不构成调用授权。

合同/Profile 29/29、independent verifier 2/2、真实 PostgreSQL rewind/recovery 1/1 均通过；后者在 lease-expiry
后从修复后的 OBSERVE 继续到 HIERARCHY、BINDING 与 Candidate。这是 A1 离线合同可达性，本节点没有 Provider
调用，Goal 累计不变，三份 ledger 仍 CLOSED。

## V18 detailed hierarchy region repair taxonomy

v17 live 连续三次只给出 generic region ownership code，安全 telemetry 无法区分错误字段。`4d2cc46` 新增
pipeline 4.5/product-v18，仅在新 Profile 中启用六类固定码：entity region IDs、relationship region ID、unknown
region reference、root ownership、relationship parent/child connection、relationship region/cardinality。v17 与更早
Profile 保持 legacy generic，历史 snapshot 不变。

hierarchy prompt v6 还为 support-not-group/group-reused 提供精确结构修复；所有 ID 仍必须来自已验证 plan，代码
不自动删边、补 GROUP 或猜结构。新结构码留在 HIERARCHY、不触发 crop；real-PG lease-expiry 纵切证明成功
OBSERVE checkpoint 保留，修复后只重做 HIERARCHY 并继续 BINDING/Candidate。合同/Profile/prompt 31/31、
independent verifier 2/2、real-PG 1/1 与 server/web/E2E/clean-fast A1 均通过；实现期间没有 Provider 调用。

Plus v18 live 没有命中上述 region code：OBSERVE 首次接受后，四轮 HIERARCHY 全部稳定报告 relationship
support IDs invalid。这证明 detailed taxonomy 的兼容边界有效，但并未提高三阶段 reachability；下一增量必须只
围绕 support ID 来源与 owner 约束，不得借机补 GROUP、删 relationship 或猜结构。

## V19 bounded relationship support-ID normalization

`214fff9` 新增 pipeline 4.6/product-v19，旧 Profile 保持 strict。新合同先把 null、empty、limit 与 invalid ID
拆成固定码；仅当列表中的每项都是已验证 ID 时，才稳定去除同一 ID 的精确重复。去重后存在多个不同 ID 仍由
support-count 合同拒绝；代码不在候选间选择、不发明 GROUP、不删 relationship、不猜层级。成功去重记录固定
normalization telemetry，结构码不触发 crop，checkpoint 保留 OBSERVE/grounding。

合同/Profile/prompt 33/33、independent verifier 2/2、real-PG lease-expiry 1/1 与 server/web/E2E/clean-fast A1
均通过。真实 PostgreSQL 纵切证明 exact duplicate 归一化后 HIERARCHY 可接受并进入 BINDING，崩溃恢复只重做
BINDING；这仍是本地合成输入的合同证据，不是 Provider reachability。实现期间 Provider attempts=0、三份
ledger CLOSED。后续受控 live 见本 checkpoint；相同 product-v19 不再重复。

## V20 unique evidence-owned relationship region normalization

v19 live 的 cardinality 已由唯一支撑 GROUP 的 multiplicity 派生，但 relationship 连续引用不兼容 region。
`391bd52` 新增 pipeline 4.7/product-v20，旧 Profile 保持 strict。新 policy 仅在当前 region 已知但 cardinality
不兼容时，从该唯一有效支撑 GROUP 的已验证 owned regions 中过滤 compatible region：恰有一个才确定性替换；
零个输出 `VISUAL_SEMANTIC_GROUP_REGION_INVALID` 并将最早失败阶段路由到 OBSERVE；多个不择一，继续由既有
`VISUAL_HIERARCHY_V2_RELATIONSHIP_REGION_CARDINALITY_INVALID` 拒绝。未知 region、不同 GROUP、endpoint、
topology、entity/relationship 数量均不被改写。

成功归一化只写 payload-free `VISUAL_HIERARCHY_RELATIONSHIP_REGION_NORMALIZED`，结构码保持 no-crop，已接受
OBSERVE/grounding checkpoint 保留。product-v20 复用 elements-v8/hierarchy-v7/bindings-v3 immutable prompts，
三模型 Profile 均隐藏 `EXPERIMENTAL`。inference 定向 34/34、independent verifier 2/2、real-PG 1/1 与
server/web/E2E/clean-fast A1 全绿；实现期间 Provider attempts=0、三份 ledger CLOSED。这只证明 repository
synthetic 合同可达，不构成 live 三阶段或 Max 授权入口。

Plus v20 随后按 `7afda44` PROPOSED → `191cf63` OPEN → `85b2000` CLOSED 完成受控 lifecycle。负探针精确
NOT_OPEN，Goal/guard、270 reservations 与 target evidence 不变；唯一 wrapper exit 0、119,361 ms，先 CLOSED
再由独立 verifier A2 重建。live 未命中该 normalization，重复的 connection fixed code 把下一本地假设收窄到：
只有唯一支撑 GROUP 的已验证 owned region 同时满足 cardinality 与 parent/child entity-region connection 时，
才允许确定性收敛；不得以 entity/relationship 数量、距离或任意候选排序代替唯一证据。

## V21 unique connected relationship region normalization

`dda763c` 新增 pipeline 4.8/product-v21，旧 Profile 保持原语义。新 policy 只在当前 relationship region 已知且
cardinality 或 parent/child connection 不成立时，从唯一有效支撑 GROUP 的已验证 owned regions 中筛选同时满足
两项合同的候选：恰有一个才确定性替换；零个或多个不择一，继续由既有 detailed HIERARCHY fixed code 拒绝；
如果不存在任何 cardinality-compatible owned region，仍输出 `VISUAL_SEMANTIC_GROUP_REGION_INVALID` 并回到
OBSERVE。未知 region、不同 GROUP、距离/顺序排名、endpoint/topology 与结构增删均不在修复边界内。

成功归一化复用 payload-free `VISUAL_HIERARCHY_RELATIONSHIP_REGION_NORMALIZED`；accepted OBSERVE checkpoint、
stage-local BINDING recovery 与 no-crop 结构诊断保持不变。product-v21 复用 elements-v8/hierarchy-v7/bindings-v3
immutable prompts，三模型 Profile 均隐藏 `EXPERIMENTAL`。local contract/Profile 35/35、independent verifier 2/2、
real-PG lease-expiry 1/1 与 server/web/E2E/clean-fast A1 全绿；实现期间 Provider attempts=0、三份 ledger CLOSED。
该 local A1 当时只允许在重新计算 evaluation identity 与 Plus product-v21 snapshot 后，以独立精确 J1 ledger
做单 case；本地 A1 不构成 Max 入口。

Plus product-v21 随后按 `405fa9e` PROPOSED → `d793c92` OPEN → `02872c5` CLOSED 完成受控 lifecycle。负探针
精确 NOT_OPEN，Goal/guard 哈希、275 reservations 与 target evidence 不变；唯一 wrapper exit 0、174,353 ms，
先 CLOSED 再由独立 verifier A2 重建。前两次 OBSERVE 因 parent-kind/element fixed code 被拒，第三次 accepted；
两次 HIERARCHY 均为 support-not-group，未进入 ELEMENT_BINDING，也未命中 region normalization telemetry。
相同 product-v21 不再重复；下一本地诊断只允许检查 exact relationship-region 的唯一 GROUP owner，不得用距离、
顺序、模型原文或跨 GROUP 猜测 support ID。

## V22 unique exact-region GROUP-owner support normalization

`edc0c28` 新增 pipeline 4.9/product-v22，4.8 与更早 Profile 保持原语义。新 policy 只在 relationship support
经逐项验证和 exact dedupe 后恰有一个已知非 GROUP element 时继续；原始 relationship region 还必须是已验证
GROUP/REPEATED_GROUP，并且 inventory 中恰有一个 observed GROUP 精确拥有该 region。只有此时 codec 才用该
owner 替换 support ID，再从其 multiplicity 派生 cardinality。

未知 support 保留 unknown fixed code；非容器/未知/非法 region、零 owner 或多个 owner 都不归一化，并继续
fail-closed。实现不跨 GROUP/region，不用距离、reading order、模型文本或候选排名，不新增/删除 element、entity、
relationship，也不修改 endpoint、field key、topology 或 Candidate。成功只记录 payload-free
`VISUAL_HIERARCHY_RELATIONSHIP_SUPPORT_OWNER_NORMALIZED`；v19 exact dedupe 与 v21 connection-aware region
normalization 合同继续有效。

合同/Profile/prompt 定向 36/36、independent verifier 2/2、real-PG lease-expiry 1/1 与 server/web/E2E/
clean-fast A1 全绿；真实 PostgreSQL 纵切证明归一化后进入 ELEMENT_BINDING，lease recovery 只重做 BINDING。
实现与门控期间 Provider attempts=0、三份 ledger CLOSED，Goal 累计表不变。product-v22 保持隐藏
`EXPERIMENTAL`；在随后的 live 前，该 local A1 只允许在重新计算 evaluation identity/Profile snapshot，并
确认精确 J1、额度与时限仍有效后执行 Plus v22 单 case，当时不构成 Max 入口。

Plus product-v22 随后按 `6f65516` PROPOSED → `2d396e7` OPEN → `4f86456` CLOSED 完成受控 lifecycle。
PROPOSED 负探针精确 NOT_OPEN，Goal state/guard 哈希、280 reservations 与 target evidence 不变；唯一 wrapper
exit 0、138,611 ms，先 CLOSED 再由独立 verifier A2 重建。第二次 OBSERVE、第二个 HIERARCHY 计划与
ELEMENT_BINDING 均 accepted，首次实证三阶段可达；成功 HIERARCHY 只命中 cardinality-derived telemetry，
未命中 support-owner normalization。report `complete=false` 且 stage-gold 质量未达门，因此 Profile 不晋级。
Max 的阶段前置条件现已成立，但其单 case 仍须重新计算 identity/Profile snapshot，并使用新的精确 J1 ledger。

Max product-v22 随后按 `e0b1d67` PROPOSED → `740d28f` OPEN → `99efc6b` CLOSED 完成受控 lifecycle。
PROPOSED 负探针精确 NOT_OPEN，Goal/guard 哈希、285 reservations 与 target evidence 不变。唯一 Provider
wrapper 的测试主体写出 3 个 SETTLED attempts 后完成；外层 PowerShell 的 Mockito stderr warning 使摘要不可用，
恢复检查确认无存活子进程且 evidence 已完成，立即 CLOSED 且没有重跑。独立 verifier A2 PASS，三个阶段均一次
accepted，但 0 GROUP/relationship、0/10 binding match 与 tree edit 30/32 明确未达质量门；v22 normalization
未命中，Profile 不晋级。相同 Max v22 不再重复。

## v23 support-owner hybrid observation

Max v22 在 OBSERVE/HIERARCHY 同时输出 0 GROUP/relationship 时，现有 verifier 没有 evidence-bounded 方法补造
重复结构。`e13bf0c` 新增 pipeline 4.10/product-v23，把精确绑定的本地 Document Vision observation 加到 4.9
policy 上：evidence-derived cardinality、owner/region prerequisite、fixed-code repair、support dedupe、connection-aware
region normalization 与 unique exact-region GROUP-owner replacement 全部保持不变。三个稳定模型各有 immutable
Profile，产品 selector 不变。

每次 drain 只预处理一次，三个 Provider stage 共享 observation；OCR text、line ID、bbox 仅是 untrusted secondary
evidence，不直接创建结构且不落入 checkpoint/Candidate/problem/attempt/journal/report。real-PG scripted-provider
纵切证明 support-owner normalization 仍命中、三阶段共享且 sentinel/line ID 不落盘；独立 Python verifier 接受并
重算 4.10 snapshot。该结果是 A1 合同证据，不是模型质量证据。

Flash v23 随后按 `0c1506f` PROPOSED → `9652837` OPEN → `1185890` CLOSED 完成同一 synthetic case。
负探针精确 NOT_OPEN，Goal/guard、288 reservations 与 target evidence 零变化；OPEN 前 Java/Python identity
共同得到 `renderweave-visual-evaluation-tree-sha256/1:d6f2be493cfcbfc5f6f1232b75067ba31e3322765529126fba24b5a84983b2be`，
Profile snapshot 为 `9053885261116ded0de3cc04d2e7ebe01f130fa78748c95b3f2da81c6782d102`。精确
Document Vision canary `.sdlc/evidence/20260811-080747-document-vision` PASS 后才进入 Provider。

唯一 wrapper 写出 5 个 SETTLED attempts；外层 PowerShell 因 Mockito stderr warning 成为
`NativeCommandError`。恢复检查确认无存活 Java/Maven/OCR 子进程、evidence 已完成、无 RESERVED/BREACHED 后
立即 CLOSED，未重跑。独立 verifier A2 PASS：20,110 input + 24,522 output tokens、178,163 ms、¥0.023643、
0 abandoned、payload scan PASS。五次均停在 OBSERVE：三次 region-kind enum、两次 parent-kind；实际
slot/group/entity/relationship/binding 全为 0。v23 不晋级且相同 Profile 不再重复。

## v24 bounded observation normalization

v23 只允许读取固定码，不能读取模型原文来挑选修复。`061101f` 新增 pipeline 4.11/product-v24：仅将合同已
明确列出的 `DOCUMENT→ROOT`、`CONTAINER→GROUP` 与允许 region enum 的大小写差异归一化；ITEM 只有在
同 artifact、bbox 包含、`repeatGroupId` 精确匹配的 REPEATED_GROUP 候选恰好一个时才改 parent。parent 变化
后只按验证后的 top/left/id 重算受影响 sibling readingOrder。未知 alias、零/多候选、未知 parent、拓扑和
element/entity/relationship 增删继续 fail-closed。

成功只记录 `VISUAL_GROUNDING_REGION_KIND_NORMALIZED`、`VISUAL_GROUNDING_ITEM_PARENT_NORMALIZED`、
`VISUAL_GROUNDING_READING_ORDER_NORMALIZED` 三个 payload-free count；被替换值、模型响应、OCR、图片与完整
请求均不落盘。4.11 完整继承 v23 ephemeral observation、v22 hierarchy policy、deterministic materializer、
earliest-stage recovery 与人工审核边界。三个 v24 Profile 都隐藏 `EXPERIMENTAL`，产品 selector 不变。

合同/Profile 20/20、real-PG + independent verifier 2/2 通过；脚本纵切命中 3 kind、1 unique parent、1
readingOrder 以及后续 support-owner telemetry，并完成三阶段，OCR sentinel 未落盘。clean server
`.sdlc/evidence/20260811-082418-server` 为 193 tests、6 gated skip A1 PASS；Provider attempts=0、三份 ledger
CLOSED。该 local 节点本身不构成 final eval 或晋级；随后 live 结果如下。

## Flash v24 bounded observation smoke

Flash v24 使用 identity `…ef0a049`、snapshot `d4c2f0…a085`，按 `9d2dfa3` PROPOSED → `ded9e78` OPEN →
`ac17e0e` CLOSED 完成单 case。PROPOSED 负探针精确命中 NOT_OPEN，Goal state/guard 哈希、293 reservations 与
target evidence 均不变。首次 Document Vision canary 因只设置产品变量、缺少 canary 专用变量而在 adapter 启动前
fail-closed，证据 `20260811-084242-document-vision`；确认无进程/lease/Provider 后以精确合同重跑，
`20260811-084304-document-vision` PASS、19 OCR lines。唯一 live wrapper exit 0、228.589 秒，先 CLOSED 再读取
evidence，未并发重跑。

独立 verifier A2 PASS：1 completed、0 abandoned、5 attempts、20,121 input + 24,819 output tokens、216,004 ms、
¥0.023884 增量、payload scan PASS。四次为 `VISUAL_GROUNDING_JSON_ENUM_INVALID_REGION_KIND`，一次为
`VISUAL_GROUNDING_SIBLING_OVERLAP`；五次全在 OBSERVE，三类 v24 normalization telemetry 均为 0，实际
slot/group/entity/relationship/binding 仍全为 0。该结果证明 v24 的离线 bounded 分支未被本次 Flash 输出命中，
不证明分支无效；相同 Flash v24 假设不再重复，Profile 不晋级。

## Plus v24 hybrid smoke

Plus v24 使用 identity `…9b077fb`、snapshot `fbf24e…deba0`，按 `3598c12` PROPOSED → `963d1e6` OPEN →
`4747947` CLOSED 完成单 case。负探针精确 NOT_OPEN，Goal state/guard 哈希、298 reservations 与 target evidence
均不变；Document Vision executable/adapter/models 与刚通过的 canary 输入未变，按 RULE-VAL-001 复用
`20260811-084304-document-vision`。唯一 wrapper exit 0、102.811 秒，先 CLOSED 后读取 evidence。

独立 verifier A2 PASS：1 completed、0 abandoned、5 SETTLED attempts、29,666 input + 4,777 output、90,132 ms、
¥0.097548 增量、payload scan PASS。OBSERVE 首次 accepted；两次 HIERARCHY 分别以
`VISUAL_HIERARCHY_V2_RELATIONSHIP_SUPPORT_IDS_EMPTY`、
`VISUAL_HIERARCHY_V2_RELATIONSHIP_REGION_CARDINALITY_INVALID` 拒绝，第三次带
`VISUAL_HIERARCHY_RELATIONSHIP_CARDINALITY_DERIVED=1` accepted；ELEMENT_BINDING accepted。最终仅 group 1/3、
entity 2/4、relationship 1/3 命中，slot 0/10、binding 0/10、field 1/13，另有 9 critical hallucinations、
12 blockers。三阶段合同可达，但质量门远未通过；v24 observation normalization telemetry 仍未命中，Profile
不晋级。

## Max v24 hybrid smoke

Max v24 使用 identity `…4f5f41`、snapshot `83c5c9…fceb9`，按 `a04691e` PROPOSED → `b8ac358` OPEN →
`57b1502` CLOSED 完成单 case。负探针精确 NOT_OPEN，Goal state/guard 哈希、303 reservations 与 target evidence
均不变；Document Vision executable/adapter/models 与已通过的 canary 输入未变，复用
`20260811-084304-document-vision`。唯一 wrapper exit 0、92.715 秒，先 CLOSED 后读取 evidence。

独立 verifier A2 PASS：1 completed、0 abandoned、3 SETTLED attempts、17,500 input + 4,219 output、79,835 ms、
¥0.361884 增量、payload scan PASS。OBSERVE、HIERARCHY、ELEMENT_BINDING 均首次 accepted，只有
`VISUAL_HIERARCHY_RELATIONSHIP_CARDINALITY_DERIVED=1` telemetry 命中；v24 observation normalization 未命中。
最终 slot 0/10、group 0/3、entity 1/4、relationship 0/3、binding 0/10、field 0/13，另有 16 critical
hallucinations、17 blockers。三阶段可达，但语义质量比 Plus v24 更差，Profile 不晋级。

## Goal 累计与剩余

| 槽位 | attempts | exposed tokens | 剩余 tokens | Goal cost | RESERVED |
|---|---:|---:|---:|---:|---:|
| Flash（旧 alias + pinned） | 95 / 180 | 598,343 / 1,500,000 | 901,657 | ¥0.280418 / ¥0.40 | 0 |
| Plus | 150 / 180 | 883,569 / 1,500,000 | 616,431 | ¥3.436302 / ¥4.00 | 5 |
| Max | 79 / 180 | 465,016 / 1,500,000 | 1,034,984 | ¥9.816288 / ¥18.00 | 0 |

Goal guard 共 324 reservations：319 SETTLED、5 RESERVED。Plus 的 5 个 RESERVED 均为此前保守预留；v26
Flash 的 5 个与 Plus 的 4 个新 reservation 全部 SETTLED，Max v26 未调用。既有 RESERVED 继续计费/计 token
上界，不做人工回收。

## 门控与恢复证据

- `252dc00` 前的 targeted inference/app 测试分别 14/14、10/10；server A1
  `.sdlc/evidence/20260811-025003-server`，exact-clean fast A1
  `.sdlc/evidence/20260811-025344-fast`。
- `0d7b73c` targeted 11/11；exact-clean fast A1 `.sdlc/evidence/20260811-025509-fast`。
- v14 repair targeted inference 34/34、worker real-PG 1/1；dirty server A1
  `.sdlc/evidence/20260811-031747-server`。首次 dirty fast 只因 Windows 混合换行在 repository-diff 失败，
  保留 `.sdlc/evidence/20260811-031923-fast`；机械统一 LF 后 dirty fast
  `.sdlc/evidence/20260811-031951-fast` 通过。
- `98ba3d0` exact-clean fast `.sdlc/evidence/20260811-032017-fast` 与 exact-clean server
  `.sdlc/evidence/20260811-032215-server` 均为 A1 PASS；server 汇总 374 tests、6 gated skip。
- v14 lifecycle commits：`ea4f06d` PROPOSED → `4bade19` OPEN → `edfecc9` CLOSED；负探针 exit 1 /
  `VISUAL_EVALUATION_AUTHORIZATION_NOT_OPEN`，无 evidence/journal mutation。独立 verifier 使用 tracked
  `visual-eval/v1/scenes.json` 重建 PASS。
- `195894b` targeted inference 25/25、real-PG recovery 1/1；exact-clean fast
  `.sdlc/evidence/20260811-035530-fast`、server `.sdlc/evidence/20260811-035542-server`（377 tests、6 gated
  skip）、web `.sdlc/evidence/20260811-035542-web`（73 tests + build）、E2E
  `.sdlc/evidence/20260811-035710-e2e`（18 passed、1 gated skip）均为 A1 PASS。
- v15 lifecycle commits：`f796648` PROPOSED → `0ac4ef9` OPEN → `4b57581` CLOSED；PROPOSED 负探针命中
  `VISUAL_EVALUATION_AUTHORIZATION_NOT_OPEN`，target evidence 不存在，Goal state/guard 与 245 reservations
  不变。唯一 wrapper exit 0 后 reservations 变为 250，先 CLOSED 再读取 report；独立 verifier A2 PASS。
- `4b57581` exact-clean fast `.sdlc/evidence/20260811-041140-fast` PASS。
- `bb15096` targeted codec/Profile 12/12、real-PG 1/1；server
  `.sdlc/evidence/20260811-043007-server`（379 tests、6 gated skip）、web
  `.sdlc/evidence/20260811-043128-web`（73 tests + build）、E2E
  `.sdlc/evidence/20260811-043158-e2e`（18 passed、1 gated skip）与 exact-clean fast
  `.sdlc/evidence/20260811-043334-fast` 均为 A1 PASS；三份 ledger 一直 CLOSED，Provider attempts=0。
- v16 lifecycle commits：`87662e0` PROPOSED → `f6d3a7e` OPEN → `46384c1` CLOSED；负探针 exit 1 / 精确
  `VISUAL_EVALUATION_AUTHORIZATION_NOT_OPEN`，Goal state/guard 哈希、250 reservations 与 target evidence
  均不变。唯一 wrapper exit 0 后先 CLOSED；独立 verifier A2 PASS。closed clean fast 为
  `.sdlc/evidence/20260811-044301-fast`。
- `31a8c6f` 受影响 server `.sdlc/evidence/20260811-045814-server`（383 tests、6 gated skip）、web
  `.sdlc/evidence/20260811-045937-web`（73 tests + build）、E2E `.sdlc/evidence/20260811-050010-e2e`
  （18 passed、1 gated skip、无 console/page error）与 clean fast `.sdlc/evidence/20260811-050115-fast`
  均为 A1 PASS；执行期间没有 Provider 调用。
- v17 lifecycle commits：`178bafb` PROPOSED → `8e0c31e` OPEN → `7107303` CLOSED。负探针的 Surefire 结果精确
  命中 `VISUAL_EVALUATION_AUTHORIZATION_NOT_OPEN`，Goal state/guard 哈希、255 reservations、target evidence 均
  不变；唯一 wrapper exit 0 后先 CLOSED。独立 verifier A2 PASS；closed clean fast 为
  `.sdlc/evidence/20260811-051247-fast`。
- `4d2cc46` server `.sdlc/evidence/20260811-052610-server`、web `.sdlc/evidence/20260811-052610-web`、E2E
  `.sdlc/evidence/20260811-052745-e2e` 与 clean fast `.sdlc/evidence/20260811-052853-fast` 均为 A1 PASS；
  Provider attempts=0，三份 ledger 一直 CLOSED。
- v18 lifecycle commits：`df166df` PROPOSED → `dca738c` OPEN → `2ee5691` CLOSED。负探针精确 NOT_OPEN，Goal
  state/guard 哈希、260 reservations 与 target evidence 均不变；唯一 wrapper exit 0 后先 CLOSED。独立 verifier
  A2 PASS；closed clean fast 为 `.sdlc/evidence/20260811-053811-fast`。
- `214fff9` targeted 33/33、independent verifier 2/2、real-PG 1/1；server
  `.sdlc/evidence/20260811-055056-server`、web `.sdlc/evidence/20260811-055228-web`、E2E
  `.sdlc/evidence/20260811-055259-e2e` 与提交后 clean fast `.sdlc/evidence/20260811-055541-fast` 均为 A1
  PASS；Provider attempts=0。
- v19 lifecycle commits：`09c3c16` PROPOSED → `7e1b98b` OPEN → `aa92ae2` CLOSED。负探针精确 NOT_OPEN，Goal
  state/guard 哈希、265 reservations 与 target evidence 均不变；唯一 wrapper exit 0 后先 CLOSED。独立 verifier
  A2 PASS；closed clean fast 为 `.sdlc/evidence/20260811-060633-fast`。
- `391bd52` inference 定向 34/34、independent verifier 2/2、real-PG lease-expiry 1/1；server
  `.sdlc/evidence/20260811-062224-server`、Node 24 web `.sdlc/evidence/20260811-062433-web`、E2E
  `.sdlc/evidence/20260811-062513-e2e` 与 clean fast `.sdlc/evidence/20260811-062623-fast` 均为 A1 PASS；
  Provider attempts=0、三份 ledger CLOSED。
- v20 lifecycle commits：`7afda44` PROPOSED → `191cf63` OPEN → `85b2000` CLOSED。负探针精确 NOT_OPEN，
  Goal/guard 哈希、270 reservations 与 target evidence 均不变；唯一 wrapper exit 0 后先 CLOSED。独立 verifier
  A2 PASS；closed clean fast 为 `.sdlc/evidence/20260811-063705-fast`。
- `dda763c` targeted 35/35、independent verifier 2/2、real-PG lease-expiry 1/1；server
  `.sdlc/evidence/20260811-064835-server`、Node 24 web `.sdlc/evidence/20260811-065005-web`、E2E
  `.sdlc/evidence/20260811-065038-e2e` 与 clean fast `.sdlc/evidence/20260811-065134-fast` 均为 A1 PASS；
  Provider attempts=0、三份 ledger CLOSED。
- v21 lifecycle commits：`405fa9e` PROPOSED → `d793c92` OPEN → `02872c5` CLOSED。负探针精确 NOT_OPEN，
  Goal/guard 哈希、275 reservations 与 target evidence 均不变；唯一 wrapper exit 0 后先 CLOSED。独立 verifier
  A2 PASS；closed clean fast 为 `.sdlc/evidence/20260811-070414-fast`。
- `edc0c28` targeted 36/36、independent verifier 2/2、real-PG lease-expiry 1/1；server
  `.sdlc/evidence/20260811-071714-server`（191 tests、6 gated skip）、Node 24 web
  `.sdlc/evidence/20260811-071856-web`（73 tests + type/lint/build）、E2E
  `.sdlc/evidence/20260811-071927-e2e`（18 passed、1 gated skip、无 console/page error）与 clean fast
  `.sdlc/evidence/20260811-072030-fast` 均为 A1 PASS；Provider attempts=0、三份 ledger CLOSED。
- v22 lifecycle commits：`6f65516` PROPOSED → `2d396e7` OPEN → `4f86456` CLOSED。负探针精确 NOT_OPEN，
  Goal/guard 哈希、280 reservations 与 target evidence 均不变；唯一 wrapper exit 0 后先 CLOSED。独立 verifier
  A2 PASS；closed clean fast 为 `.sdlc/evidence/20260811-073017-fast`。
- Max v22 lifecycle commits：`e0b1d67` PROPOSED → `740d28f` OPEN → `99efc6b` CLOSED。负探针精确
  NOT_OPEN，Goal/guard 哈希、285 reservations 与 target evidence 均不变。唯一 Provider wrapper 的测试主体
  完成；外壳摘要失败后检查 process/evidence lease 并立即 CLOSED，未重跑。独立 verifier A2 PASS；closed
  clean fast 为 `.sdlc/evidence/20260811-074402-fast`。
- `e13bf0c` targeted Profile/能力合同 1/1、real-PG hybrid/payload/support-owner 与独立 verifier 2/2、clean fast
  `.sdlc/evidence/20260811-075518-fast`、server `.sdlc/evidence/20260811-075612-server`（192 tests、6 gated
  skip）均为 A1 PASS；Provider attempts=0、Goal/ledger 用量不变。
- Flash v23 lifecycle `0c1506f` → `9652837` → `1185890`；Document Vision canary
  `.sdlc/evidence/20260811-080747-document-vision` 与独立 verifier A2 PASS，外壳失败后按 process/evidence
  lease 恢复且未重跑。
- `061101f` contract/Profile 20/20、real-PG + independent verifier 2/2、clean server
  `.sdlc/evidence/20260811-082418-server`（193 tests、6 gated skip）A1 PASS；Provider attempts=0。
- `2b23617` Goal guard v3 migration/tamper/independent verifier 定向 12/12 与 exact-clean fast
  `.sdlc/evidence/20260811-083559-fast` A1 PASS；293 reservations、三份 CLOSED ledger 与 Provider attempts 不变。
- Flash v24 lifecycle `9d2dfa3` → `ded9e78` → `ac17e0e`；负探针零调用，Document Vision canary
  `.sdlc/evidence/20260811-084304-document-vision`、独立 verifier A2 与 CLOSED clean fast
  `.sdlc/evidence/20260811-085018-fast` PASS。首次本地 canary 配置失败证据保留，确认零 Provider 后才修正重跑。
- Plus v24 lifecycle `3598c12` → `963d1e6` → `4747947`；负探针零调用，复用未变 Document Vision canary输入，
  独立 verifier A2 与 CLOSED clean fast `.sdlc/evidence/20260811-085833-fast` PASS。
- Max v24 lifecycle `a04691e` → `b8ac358` → `57b1502`；负探针零调用，复用未变 Document Vision canary 输入，
  独立 verifier A2 与 CLOSED clean fast `.sdlc/evidence/20260811-091152-fast` PASS。
- Flash v26b lifecycle `36a7a9e` → `ef6440f` → `b976e5f` 与 Plus v26 lifecycle `3f95ae3` → `67f33dd` →
  `31093a6` 均先负探针、后唯一 wrapper、再 CLOSED，独立 verifier A2 PASS。Flash 全部停在 OBSERVE；Plus 在
  OBSERVE accepted 后三次 HIERARCHY 拒绝，二者均未命中 v26 telemetry。Max v26 因同版本三阶段门未成立未调用。
- Flash v26 的先行预检 lifecycle `f2386b6` → `32fae98` → `2eedc61` 因隔离 worktree CRLF 导致 corpus
  identity mismatch，在 evidence/Goal mutation 与 Provider 前 fail-closed；恢复审计为零调用、零 lease、零状态变化，
  不计入 live report。
- 没有 A3。截至该历史 checkpoint 的二十份 live report 均 `complete=false`，不能代替 N7 20/60-case final eval。

## 当前结论与下一门

N7 继续保持 `in_progress`。v27 unique source-ancestor GROUP-owner 已完成 local/PG/UI A1 与三模型 single-case
A2，但新增 telemetry 未命中；Flash 仍停在 OBSERVE，Plus/Max 虽三阶段可达却没有可接受 Candidate。不因
token cap 增加而放宽 CNY/质量门，也不把三阶段可达误报为 final 质量；新的 bounded hypothesis 完成离线合同、
真实 PG 与受影响 gate 前不扩大到 20/60 case。final eval、最终 revision full、独立 final verifier 与用户业务/
视觉 J1 任一未满足时，Goal 不完成，Profile 保持 `EXPERIMENTAL`。

## v25 离线假设 checkpoint

v24 A2 的 payload-free 指标显示 actual SLOT 过量且 GROUP 缺失。`f8f09b4` / `2b6eb9c` 将这一信号收窄为
pipeline 4.12 的 leaf-evidence OBSERVE invariant：SLOT 证据不得严格包含另一元素证据；失败固定回退 OBSERVE，
不自动改 kind 或补 GROUP。60-scene stage-gold、real-PG stage-local repair、三模型 Profile/snapshot verifier 均
通过。`6cb2624` 同步 payload-free 监控/审核解释。

受影响 A1：server `.sdlc/evidence/20260811-093552-server`（196 tests、6 gated skip）、Node 24 web
`.sdlc/evidence/20260811-093552-web`（73 tests + type/lint/build）、E2E
`.sdlc/evidence/20260811-093741-e2e`（18 passed、1 gated skip）、runtime
`.sdlc/evidence/20260811-093824-runtime` 均 PASS。实现与门控 Provider attempts=0，Goal 用量和 306 reservations
不变，三份 ledger CLOSED。

该节点当时建立了新的 bounded live hypothesis，但尚无 v25 A2；后续 v25 结果见下一节。任何 v25 live 仍需 fresh
evaluation identity、精确 Profile snapshot、单次≤500,000 authorization、现有 attempt/token/CNY/time 边界、
PROPOSED→负探针→OPEN→CLOSED 和单 wrapper。未取得 final 20/60 eval、full gate、独立 final verifier 与用户
最终 J1 前，不完成 Goal、不晋级 Profile。

## Flash / Plus v25 live checkpoint

- Flash lifecycle：`a7a2a7f` PROPOSED → `a9635e4` OPEN → `edb35bc` CLOSED；fresh identity `…9c77ac2`、
  snapshot `656df9…db2`。负探针 NOT_OPEN；Document Vision 首次配置失败后确认零 Provider/进程/lease，再由
  `.sdlc/evidence/20260811-094753-document-vision` 恢复 PASS。唯一 live wrapper 211.8 秒，先 CLOSED 后读取；
  独立 verifier A2 PASS：4 attempts、16,082 input + 21,338 output、188,904 ms、payload scan PASS。全部停在
  OBSERVE，leaf-evidence rule 未命中。
- Plus lifecycle：`06cef12` PROPOSED → `34e7ab3` OPEN → `e93d1f7` CLOSED；同一 fresh tree identity、snapshot
  `ff5a1a…51a`。负探针主体命中 NOT_OPEN，外壳摘要因 `$Matches` 冲突失败；process/evidence/Goal/guard/
  reservations 恢复审计确认零副作用，未重跑。唯一 wrapper exit 0、194.231 秒，先 CLOSED 后读取；独立 verifier
  A2 PASS：5 attempts、25,433 input + 10,312 output、181,561 ms、payload scan PASS。第三次 OBSERVE accepted，
  两次 HIERARCHY 分别以 region cardinality invalid 与 support not group 拒绝，未进入 BINDING，leaf-evidence
  rule 未命中。最终 slot matched 3/10、group 1/3，其余 entity/relationship/binding matched 为 0，report
  `complete=false`。
- Flash 累计 90 attempts、555,365 tokens、¥0.258103；Plus 累计 146 attempts、853,926 tokens、¥3.346968。
  v25 没有提供新规则命中且 Plus 没有三阶段可达性，因此没有调用 Max v25；不以历史 v24 可达性盲目扩大预算。

## v26 离线 checkpoint 与当前门

`d3b0292` 将 Plus v25 的 support-not-group/region-cardinality 组合收窄为 pipeline 4.13 的唯一
enclosing-connected GROUP-owner policy：已知非 GROUP support 的全部元素证据必须被同一 container region
包围，且该 region 同时 cardinality-compatible 并连接 relationship parent/child；候选 GROUP/region 配对恰为
一个时才归一化，零/多继续 fail-closed。不读取模型原文/OCR，不用 gold、距离或排序，不改变 topology。
`5ef25bd` 同步 payload-free checkpoint telemetry 与监控/审核 UI。

合同/Profile 22/22、独立 snapshot verifier 与 real-PG 三阶段 tracer PASS；server
`.sdlc/evidence/20260811-101733-server`、Node 24 web `.sdlc/evidence/20260811-101734-web`、E2E
`.sdlc/evidence/20260811-101948-e2e`、runtime `.sdlc/evidence/20260811-102032-runtime` 均为 A1 PASS；revision
`371505b` 的隔离 clean full `.sdlc/evidence/20260811-102845-full` 为 9/9 steps、A1 PASS。该 full gate 发生在
v26 live/final eval 之前，不能冒充最终 revision gate。当前 Goal 累计为 Max 79 attempts / 465,016 tokens /
¥9.816288，Plus 150 / 883,569 / ¥3.436302，Flash 95 /
598,343 / ¥0.280418；324 reservations 中 319 SETTLED、5 个历史 Plus RESERVED，0 BREACHED；三份 ledger
CLOSED。

N7 仍为 `in_progress`。v26 Flash/Plus live A2 没有命中新 policy，也没有建立同版本三阶段可达性；Max v26 不调用。
下一节点是零 Provider 的 unique validated ancestor-GROUP owner 合同调查与 TDD。任何后续 live 仍须 fresh
identity/snapshot、单 case、单 wrapper，在调用前重新核算预算并执行 PROPOSED→负探针→OPEN→CLOSED。没有 final
20/60、最终 revision full gate、final independent verifier 与用户业务/视觉 J1，Profile 保持 `EXPERIMENTAL`，
Goal 不完成。

## v26 live checkpoint

- Flash 先行 lifecycle `f2386b6` → `32fae98` → `2eedc61` 只完成 preflight：`core.autocrlf=true` 使 corpus
  字节漂移，wrapper 以 `VISUAL_EVALUATION_CORPUS_IDENTITY_MISMATCH` 在 Provider/evidence/Goal mutation 前退出。
  process、lease、Goal/guard 与 target evidence 审计均无副作用，provider attempts=0；该生命周期不计 live A2。
- 以 LF clean worktree 重新计算 corpus SHA、evaluation identity `…530a2696` 与 Profile snapshot 后，Flash v26b
  `36a7a9e` → `ef6440f` → `b976e5f` 完成 5 attempts。独立 verifier A2 PASS：20,120 input + 22,858
  output、157,514 ms、payload scan PASS；五次均在 OBSERVE 因 enum/sibling/containment 固定码停止，实际结构全 0。
- Plus v26 `3f95ae3` → `67f33dd` → `31093a6` 完成 4 attempts。独立 verifier A2 PASS：24,635 input +
  5,008 output、91,365 ms、payload scan PASS；OBSERVE accepted，三次 HIERARCHY 在 region-cardinality 与
  support-not-group 间切换，未到 BINDING。两模型均未命中 enclosing-owner telemetry，report `complete=false`。
- Max v26 的同版本三阶段门未成立，故没有调用。最新累计：Max 79 / 465,016 / ¥9.816288；Plus 150 /
  883,569 / ¥3.436302；Flash 95 / 598,343 / ¥0.280418。共 324 reservations：319 SETTLED、5 历史 Plus
  RESERVED、0 BREACHED；三份 ledger CLOSED。
- 下一安全假设是离线检查已知非 GROUP support 的唯一 validated ancestor GROUP；只有唯一 owner/region 同时满足
  既有 cardinality、enclosure、connection 才允许归一化。禁止读取模型原文/OCR/gold、按距离/顺序排名或合成
  topology；先完成合同、真实 PostgreSQL checkpoint、telemetry 与受影响 gate，再决定是否产生新的 live 门。

## v27 source-ancestor verifier 与 pre-live 门

v26 的安全 fixed code 证明 support element 同时拥有 root 与 item 时，要求一个 GROUP 包围全部 owned regions 会
得到零候选；但原始 `relationship.regionId` 的已验证祖先链仍可能唯一指向重复容器。`676180a` 因而只在 v26
enclosing 候选严格为零时启用 source-ancestor fallback，并继续要求唯一 GROUP owner、cardinality 与 parent/child
connection。任何 ambiguity、未知引用或已有多个 v26 候选均不进入 fallback；旧 4.13 及更早 Profile 语义不变。

`e1f1a9d` / `3a56af9` 完成 product-v27 三模型 immutable Profile、worker/checkpoint telemetry、独立 verifier、
真实 PostgreSQL 三阶段 tracer 与 payload-free monitor/review UI。组合后端回归 26/26、Node 24 Web 73/73、
受影响 Playwright 1/1 通过。server 全量首轮又发现 5 参数 Provider budget reservation 的事务代理绕过竞争；
`5ada0fa` 修复入口事务并把并发用例重复 10 次。修复后 server `.sdlc/evidence/20260811-113412-server`、web
`.sdlc/evidence/20260811-113607-web`、E2E `.sdlc/evidence/20260811-113652-e2e`、runtime
`.sdlc/evidence/20260811-113726-runtime` 全部 A1 PASS；首轮红灯 `.sdlc/evidence/20260811-113055-server` 保留作
根因证据。

截至该 checkpoint 没有 Provider 调用，累计仍为 Max 79 attempts / 465,016 tokens / ¥9.816288，Plus 150 /
883,569 / ¥3.436302，Flash slot 95 / 598,343 / ¥0.280418；324 reservations 为 319 SETTLED + 5 个历史 Plus
RESERVED，0 BREACHED，三份 ledger CLOSED。下一步先在隔离 clean worktree 跑当前 revision full gate；只有随后
重新计算 evaluation identity、精确 Profile snapshot、Goal aggregate 与单模型 reservation 上界都通过，才为
pinned Flash 创建新的 PROPOSED→负探针→OPEN→CLOSED 单 case lifecycle。Plus 虽获用户允许，仍在 Flash 后按
新信号决定；Max 只有同版本三阶段门与质量/J1 条件同时成立才考虑。未满足 final eval、最终 verifier 与业务/
视觉 J1 前，N7 保持 `in_progress`、Profile 保持 `EXPERIMENTAL`。

## v27 full、三模型 live 与当前停止点

`47f622b` 在 LF detached clean worktree 完成 9/9 full gate，`workingTreeDirty=false`、A1 PASS，evidence 位于
`D:\Yiwer\code\RenderWeave-v27-full-47f622b\.sdlc\evidence\20260811-114304-full`。Java/Python 双实现随后对
clean tree 一致重算 evaluation identity `renderweave-visual-evaluation-tree-sha256/1:903f5b9f87797bcf124f08cc539982f708dc8ab5217129adcd6dc9c40960c965`；
Flash/Plus/Max snapshot 分别为 `a0e159152876073ea846aefa39987ddb10366ba0f53f1e68d5eec74711128394`、
`e0ed977931aa024266e9123d650f9749f47238368ad52796b7c516f836b6a94c`、
`8c083d086b08abff02fcde38215e7d70921dc2371e6532d0374c614af3a77624`。

Flash 首个 lifecycle `7cf9709` → `ac63bc9` → `d1c076e` 的负探针通过，但 live wrapper 漏传本地 OCR Spring
属性，以 `DOCUMENT_VISION_DISABLED` 在 Provider 前完成。独立 verifier PASS：1 completed、0 attempts、0 tokens、
Goal/guard/324 reservations 不变；它不计 Provider-backed live evidence。没有重开 ledger；而是先用
`.sdlc/evidence/20260811-115701-document-vision` 重验 RapidOCR 3.9.2、OpenVINO 2026.0.0、三份 model digest
与 19-line synthetic contract，再创建 replacement。

- Flash v27b lifecycle `6185570` → `ea9cb87` → `a473d2f`：负探针 NOT_OPEN，唯一 wrapper 185.983 秒，
  独立 verifier A2 PASS；5 SETTLED attempts、20,110 input + 22,803 output、174,008 ms。3 次 region-kind enum、
  2 次 parent-kind，全部停在 OBSERVE，结构全 0，source-ancestor telemetry 未命中。
- Plus v27 lifecycle `ccfce3b` → `7f49117` → `854d652`：唯一 wrapper 55.463 秒，A2 PASS；3 SETTLED
  attempts、14,618 input + 2,379 output、43,867 ms。OBSERVE/HIERARCHY/ELEMENT_BINDING 均 accepted，只命中
  cardinality-derived=1；最终 group 1/3、entity 2/4、relationship 1/3，slot/binding 0/10，7 blockers、
  4 critical hallucinations，新增 telemetry 未命中。
- Max v27 仅在 Plus 同版本三阶段门成立后执行：`1fa1ccf` → `705ccff` → `95be8fa`。唯一 wrapper
  130.128 秒，A2 PASS；3 SETTLED attempts、20,645 input + 6,258 output、118,138 ms。三阶段 accepted，
  cardinality-derived=2；实际 slots 21、groups 2、entities 3、relationships 2，但 slot/group/relationship/
  binding matched 为 0/0/0/0，26 blockers、27 critical hallucinations，新增 telemetry 未命中。

最终 Goal state 下三份 current evidence 再次交叉 verifier PASS、0 abandoned、payload scan PASS。累计为 Max
82 attempts / 491,919 tokens / ¥10.289316，Plus 153 / 900,566 / ¥3.484570，Flash 100 / 641,256 /
¥0.302686；335 reservations 中 330 SETTLED、5 个历史 Plus RESERVED、0 BREACHED。三份 ledger 均 CLOSED。
首次 CLOSED fast `20260811-121310-fast` 因隔离 worktree 缺 `node_modules` 环境失败，保留红证据；联接既有
pinned dependencies 后 `20260811-121335-fast` PASS。

当前停止点不是额度耗尽，而是质量与证据门：v27 专属规则没有 live 命中，Plus/Max 虽三阶段可达但没有形成
可接受 Candidate。不得用 bundle/DAG 合同 100% 掩盖匹配率、blocker 与 hallucination；本批不再调用模型，
Profile 继续 `EXPERIMENTAL`。只有形成新的 bounded、payload-free 假设并先通过离线/PG/受影响 gate，或完成
final 20/60、最终 revision full、final independent verifier 与业务/视觉 J1，才允许推进相应状态。

## evaluation identity `/2` 治理 checkpoint

`cded69e` 以零 Provider 调用实现 Git-blob canonical identity `/2`，并把 `/1` 限制为 CLOSED 历史证据只读
兼容。`/2` 的 digest 绑定 UTF-8 path bytes、regular Git mode 与 canonical blob bytes，拒绝 dirty/untracked、
assume-unchanged/skip-worktree、symlink/submodule、missing input 与不稳定双捕获。Java CLI 与独立 Python 在
exact-clean `cded69e` 一致得到 `fc46a428…b5a7bf`；后续 revision 必须重新计算，不能把该值预填到新 ledger。

clean server `.sdlc/evidence/20260811-123055-server`、fast `.sdlc/evidence/20260811-123245-fast` 均 A1 PASS。
新 verifier 使用原 `/1` 算法对 v27b Flash（5 attempts / 42,913 tokens）、Plus（3 / 16,997）与 Max
（3 / 26,903）的真实 CLOSED evidence 再回放，三者均 PASS、0 abandoned、payload scan PASS；Goal 最终累计
仍为 Flash 641,256、Plus 900,566、Max 491,919 exposed tokens，335 reservations 中 330 SETTLED、5 个历史
Plus RESERVED、0 BREACHED。

本 checkpoint 只关闭治理债务，不构成新的 bounded 质量假设，也不消耗新增授权。N7 保持 `in_progress`，三份
ledger 保持 CLOSED，product-v27 保持 `EXPERIMENTAL`；形成并离线证明下一假设前不创建新 OPEN ledger，不扩大
20/60 final eval。

## v28 离线假设与 pre-live checkpoint

新的 bounded 假设不是再优化 prompt，而是消除 entity-region ownership 与 field binding 之间的第二事实源：
同一 entity 不得同时拥有祖先/后代 region，非根 entity 不得拥有 ROOT；字段必须绑定到覆盖其证据的唯一最小
spatial entity owner。`76a0635` 以 opt-in policy 固化该合同；`a96fec1` 以 pipeline 4.15/product-v28 接入三模型
Profile、worker、checkpoint 与独立 verifier。binding 阶段发现 ambiguity 时必须返回 HIERARCHY，因为 BINDING
重试不能改变 ownership；持久层只对白名单精确单码允许该逆向恢复。

离线 evidence 已证明 legacy 兼容、fixed-code fail-closed、最早失败阶段修复与 crash-safe checkpoint：verifier/
codec 27/27、inference 180/180、真实 PostgreSQL repair tracer 2/2、独立 Profile snapshot 1/1 PASS。一条
五次 scripted stage path 从 BINDING ambiguity 回到 HIERARCHY 后到达 `REVIEW_REQUIRED`，且已验证 OBSERVE
inventory/grounding 被保留。`6a8a36f` 的 monitor/review telemetry 与针对性 E2E 不泄漏 OCR、Provider response、
图片、完整 prompt 或 Candidate；Node 24 Web gate evidence 为 `.sdlc/evidence/20260811-125512-web`。

截至本 checkpoint 没有 Provider 调用，三份 ledger 仍 CLOSED；累计 Flash 100 attempts / 641,256 tokens，Plus
153 / 900,566，Max 82 / 491,919，三槽 cap 均为 1,500,000，attempt/CNY/time 与单 authorization 500,000
边界不变。下一门是对包含治理文档的 clean revision 跑 full gate，然后重新计算 `/2` evaluation identity、三份
Profile snapshot、Goal aggregate 与时限；只有这些均通过才可优先为 Flash 创建单 case、最多 5 calls 的
PROPOSED→负探针→OPEN→CLOSED smoke。Plus 已获允许但仍按 Flash 新信号决定；Max 仅在 v28 三阶段合同可达且
质量/J1/预算/时限仍成立时考虑。final 20/60、最终 revision full、final independent verifier 与业务/视觉 J1
任一未满足，N7/Goal 均保持 `in_progress`，Profile 保持 `EXPERIMENTAL`。

## v28 full、Flash/Plus live 与 Max 停止门

`0a3b90b` clean full 9/9 A1 PASS，Document Vision `20260811-130940-document-vision` 19-line canary PASS；
fresh Java/Python identity 为 `…c669d172`，Flash/Plus/Max snapshot 分别为 `85444b…1b3026`、
`90057d…0b2`、`2470b5…9644`。

- Flash CRLF lifecycle `54b4e9d`→`7a4778d`→`61088f4` 因 checkout corpus bytes 漂移在 Provider 前关闭；
  LF replacement `701f774`→`10789d8`→`c39672f` 独立 A2 PASS，5 attempts、44,335 tokens、196,049 ms，
  五次均在 OBSERVE fail-closed。
- Plus 首个 `0f8a0dd`→`6668708`→`4215a14` 因 wrapper timeout=120 超过 Document Vision 60 秒合同，在
  Provider 前以 `DOCUMENT_VISION_TIMEOUT_INVALID` 完成；A2 PASS、0 attempts，未重开。
- Plus replacement v28b `2a69291`→`fcd3abc`→`4add041` 完成 PROPOSED 负探针与唯一 wrapper；A2 PASS，
  5 attempts、27,680 input + 8,524 output、151,360 ms、payload scan PASS。OBSERVE 第二次 accepted；
  三次 HIERARCHY 均由 `VISUAL_HIERARCHY_V2_RELATIONSHIP_REGION_CARDINALITY_INVALID` 拒绝，未进入 BINDING，
  slot/group/entity/relationship/binding matched 为 1/0/0/0/0，tree edit 20/20。

v28 未满足 accepted 三阶段，更未满足质量/J1 门，因此 Max 保持 CLOSED、未调用。Goal 最终 345 reservations：
340 SETTLED、5 个历史 Plus RESERVED、0 BREACHED；Flash 105 / 685,591 / ¥0.326091，Plus 158 / 936,770 /
¥3.608122，Max 82 / 491,919 / ¥10.289316。三槽 cap 仍为 180 attempts / 1,500,000 tokens，既有 CNY/time
边界不变。下一步必须先形成并离线验证同一 HIERARCHY fixed code 的 bounded stage-local repair/no-progress
假设；不得直接扩大 final eval。N7/Goal 保持 `in_progress`，Profile 保持 `EXPERIMENTAL`。
