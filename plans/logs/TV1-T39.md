# TV1-T39：definite Grid singleton FRACTION per axis

- 结果：完成
- 生命周期状态：`automated_verified`
- 验证时 revision：`c95ecca` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\\Yiwer\\code\\RenderWeave-template-v1-implementation`
- 外部副作用：Provider attempts=0、API Key reads=0、open authorization=0、paid external calls=0；未发送真实
  数据；只运行仓库 gate、offline Cargo/Python 与 network-none Linux UDS replay；未 push/tag/PR

## 产品增量

1. workspace-internal `renderweave-renderer-layout` 的唯一 arrange 入口继续消费 T23/T25 同一 admitted/preflight
   tree；在 T34 FIXED Grid 与 T38 singleton Stack FILL 闭包上，仅新增 definite Grid 每轴至多一个 FRACTION，
   不新增 parser、crate、route、Profile 或 daemon success path。
2. 每轴按固定 binary64 顺序累计全部 FIXED sizes 与 declared adjacent gaps；唯一 FRACTION 取得
   `max(0, available - used)`。singleton 下 `weight` 在冻结比例公式中相消，不引入 residual tolerance、FMA、
   fast-math 或中间量化。
3. track origins 仍按 authored order 物理排列；FIXED 已占满或超出 available 时 FRACTION 为正零，gap 不因零尺寸
   消失。无 FRACTION 的额外空间仍留在物理右/下端；cell/span、signed margins、FIXED/FILL、min/max、alignment、
   overlap 与 Frame/Stack/Grid 递归语义保持不变。
4. 每轴严格按 FIXED → AUTO → FRACTION 判定；同轴 AUTO 即使 authored 晚于 FRACTION，也先返回
   `GRID_AUTO_TRACK`。无 AUTO 后，两个及以上 FRACTION 返回 `GRID_FRACTION_TRACK`；columns-first 与 authored
   DFS first error 保持不变，任何失败不返回 partial layout。
5. HUG、multiple Stack FILL、Grid AUTO/multiple FRACTION、Group/compositionViewport、Text/Image、resource
   fetch/decode/shaping、world scene、paint/raster/JPEG、daemon RESULT、Profile registration 与公开 render/preview
   均未实现。

## TDD、审查与证据

- RED：先把 vector identity 升级为 `/5`，加入双轴 singleton FRACTION 与 overflow-to-zero exact expectations，
  并把 FRACTION-before-AUTO 的期望改为阶段优先的 `GRID_AUTO_TRACK`；Rust primary 与 Python independent 都先在
  首个 singleton case 返回既有 `GRID_FRACTION_TRACK`，阶段顺序 case 也先暴露旧 authored-order 返回。
- GREEN：Rust 与独立 Python mirror 均先完成整轴扫描，再按 FIXED → AUTO → FRACTION 结算；最终共同通过
  30 laid-out + 12 unsupported，42/42、128 checks。覆盖双轴 positive remainder、overflow 正零、authored origin/
  gap、AUTO-before-FRACTION 与 columns-first multiple-FRACTION。
- focused Rust exact vectors 3/3、Python stdlib independent replay、workspace fmt、clippy `-D warnings`、workspace
  tests、PowerShell gate、JSON hash/inventory 与 `git diff --check` 均通过。
- Render gate 1.5 在 Windows Rust 与 `rust:1.89-slim` network-none Linux UDS 上均通过，Linux source mount
  read-only、actual UDS round trip=true；physical host certification 仍为 false。
- Server 最终 BUILD SUCCESS；Application 344 tests、0 failure、0 error、15 controlled skips。测试上下文销毁后
  scheduler/Hikari pool 打印旧 Testcontainer 端口连接拒绝并触发 Surefire 30 秒强制 fork 退出，但未形成
  test/gate failure，且与本票 renderer-only diff 无关；如实保留在原始 server log。
- Java/OpenAPI/generated SDK/migration/API version/Web/route 零产品差异；API contract 保持 `0.15.0`，runtime
  migration count 保持 25。

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1/A2 | Rust primary + Python independent 42/42、128 checks；fmt/clippy/test/diff-check 通过 | 当前命令输出 |
| Render | A1；exact replay A2 | gate 1.5；Java 26；Rust Windows + Linux network-none UDS；Python 110+14+32+42+16 | `.sdlc/evidence/20260821-215633-render/` |
| Server | A1 | 8/8 reactor；App 344 tests、0 failure、15 controlled skips | `.sdlc/evidence/20260821-215659-server/` |
| Affected Fast | A1 | repository diff、八模块 package 与 Web typecheck 全过 | `.sdlc/evidence/20260821-221708-fast/` |
| Final Fast | A1 | resolution governance 后执行，exact 目录只在 commit handoff 报告 | `.sdlc/evidence/<final-passing-fast>/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在 commit handoff 报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |

## 合同 identity 与保证上限

- definite vector/verifier identity 为 `/5`，fixture identity 为 `/3`；vector SHA-256
  `6e2c08f42b0aa7bc237dd9ad5d24a53584a59489fa432ed75192e8ceca99edfb`，fixture SHA-256
  `a11475bcebad7e1c35cb0acd7018419d94afcb4b37d7f1df7346a055ad1df669`。
- independent layout implementation 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_SINGLE_FRACTION_GRID_BOX_KERNEL`；render
  summary 的 `layoutKernel` 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_SINGLE_MAIN_FILL_AND_FIXED_SINGLE_FRACTION_GRID_BOX_AUTOMATED_VERIFIED_UNWIRED`。
- Profile 仍 NOT_REGISTERED、certification NOT_CERTIFIED、world scene/raster ABSENT、daemon output UNWIRED、
  Renderer READY=false。
- T39 不证明 AUTO、multiple-FRACTION residual distribution/tolerance、multiple Stack FILL water filling、HUG、
  完整 Layout Profile、resource/shaping、world transform/scene、pixel/JPEG、daemon RESULT、physical Linux
  certification、formal Case/Oracle、A3、新 J1、E6 或 Template/Renderer READY。
- rollback 边界为本票单一 verified commit；无 migration 或外部持久状态需逆转。未经授权不 push/tag/PR。
