# TV1-T34：资源无关 FIXED-track definite Grid layout kernel

- 结果：完成
- 生命周期状态：`automated_verified`
- 验证时 revision：`e6e853d` + 本票 dirty diff
- 分支/worktree：`feature/template-v1` / `D:\\Yiwer\\code\\RenderWeave-template-v1-implementation`
- 外部副作用：Provider attempts=0、API Key reads=0、open authorization=0、paid external calls=0；未发送真实
  数据；只运行仓库 gate、offline Cargo/Python 与 network-none Linux UDS replay；未 push/tag/PR

## 产品增量

1. workspace-internal `renderweave-renderer-layout` 的唯一 arrange 入口继续消费 T23/T25 同一 admitted/preflight
   tree，并把 T33 的 Canvas/Frame/Stack/资源无关叶子闭包扩展到 definite Grid 与 GRID child；不新增 parser、
   crate、route、Profile 或 daemon success path。
2. Frame/Stack/Grid ContentBox 按每侧 inward stroke 后 padding 的冻结顺序逐项扣除；每次负剩余量 floor 为正零，
   origin 仍按固定加法顺序前进，不再错误返回 `DEGENERATE_CONTENT_INSET`。
3. Grid 先 columns 后 rows 求解；全 FIXED tracks 按 authored order 从 ContentBox 左/上排列，相邻 gap 保留，extra
   留在物理右/下。spanned cell 包含内部 gaps，overlap 合法且输出保持 authored preorder。
4. GRID child 支持 signed margins、两轴 FIXED/FILL、min/max 与 START/CENTER/END alignment。FILL 先对原始
   interval 取 `max(0, …)` 再 clamp，并从 interval 起点放置；Frame/Stack/Grid/八种资源无关叶子可递归组合。
5. AUTO/FRACTION/HUG 分别稳定返回 `GRID_AUTO_TRACK`、`GRID_FRACTION_TRACK`、`HUG_CONTENT`；columns-first 与
   完整 authored DFS first-error 均受向量约束。未猜测 residual tolerance，也未引入 intrinsic/resource measure。

## TDD、审查与证据

- RED：升级后的 shared vectors 先令 Rust 缺少 Grid AUTO/FRACTION closed variants，Python 仍把 degenerate
  ContentBox 判为 unsupported；两端均在实现前失败。GREEN：Rust primary 与 Python stdlib independent algorithm
  同时通过 23 laid-out + 11 unsupported，34/34、105 checks。
- 覆盖 floor-zero、fixed track/gap/span、signed margins、FIXED/FILL/min-max/alignment、overlap、nested Grid、
  negative interval FILL、AUTO/FRACTION columns-first 与 sibling/premeasure 下的 authored DFS first error。
- `cargo fmt --all`、workspace clippy `-D warnings`、workspace test、Python compile/replay、PowerShell gate parse、JSON
  inventory/hash 与 `git diff --check` 均通过；自审确认未打开 resource、world scene、raster 或 daemon output。
- Java/OpenAPI/generated SDK/migration/API version/Web/route 零差异；API contract 保持 `0.15.0`，runtime migration
  count 保持 25。

## Gate 证据

| Gate/审计 | 保证等级 | 结论 | 原始证据指针 |
|---|---|---|---|
| Focused | A1/A2 | Rust primary + Python independent 34/34、105 checks；fmt/clippy/test/diff-check 通过 | 当前命令输出 |
| Render | A1；exact replay A2 | gate 1.5；Java 26；Rust Windows + Linux network-none UDS；Python 110+14+32+34+16 | `.sdlc/evidence/20260821-164544-render/` |
| Server | A1 | 8/8 reactor；App 344 tests、0 failure、15 controlled skips | `.sdlc/evidence/20260821-164614-server/` |
| Fast | A1 | 串行 repository diff、八模块 package 与 Web typecheck 全过 | `.sdlc/evidence/20260821-170305-fast/` |
| Final full | A1；各 exact replay step 按自身合同为 A2 | 最终目录只在提交交接报告，不反写本文件 | `.sdlc/evidence/<final-passing-full>/` |

首次与 server 并发启动的 fast `.sdlc/evidence/20260821-164614-fast/` 因 server 的 Maven `clean` 删除共享
`target` 中间产物而失效；它是无效并发执行，不作为产品失败或绿色证据。串行重跑已通过。

## 合同 identity 与保证上限

- definite fixture/vector/verifier identity 均为 `/3`；vector SHA-256
  `322adeb2181b872f722e10850bfc356288c0a2755123dabd40d207661fc9a5e6`，fixture SHA-256
  `a11475bcebad7e1c35cb0acd7018419d94afcb4b37d7f1df7346a055ad1df669`。
- render summary 的 `layoutKernel` 为
  `RESOURCE_FREE_DEFINITE_ABSOLUTE_STACK_AND_FIXED_GRID_BOX_AUTOMATED_VERIFIED_UNWIRED`；Profile 仍
  NOT_REGISTERED、certification NOT_CERTIFIED、world scene/raster ABSENT、daemon output UNWIRED、Renderer
  READY=false。
- T34 不证明 Grid AUTO/FRACTION、HUG、Stack water filling、完整 Layout Profile、resource/shaping、world
  transform/scene、pixel/JPEG、daemon RESULT、physical Linux certification、formal Case/Oracle、A3、新 J1 或
  Template/Renderer READY。
- rollback 边界为本票单一 verified commit；无 migration 或外部持久状态需逆转。未经授权不 push/tag/PR。
