# Checkpoint：P1-T1-2

- 结果：完成
- AC：AC-001, AC-003, AC-023（P1 persistence subset）
- 生命周期状态：`automated_verified`
- 摘要：新增自然主键 Draft 与 append-only revision 表、完整 normalized JSONB snapshot、schema-owned persistence port 和显式 SQL adapter；创建为 revision 0，保存以 conditional update 实施 optimistic concurrency。

## 验证证据

| Gate | 保证等级 | Revision | 命令/来源 | Exit/结论 | 原始证据指针 | 验证者 captured_by | 新鲜度 |
|---|---|---|---|---|---|---|---|
| T1-2 red baseline | A1 | `UNBORN` | `mvn -B -ntp -pl renderweave-app -am test` | 1；缺少 writer/domain/persistence API | 当前会话 tool capture | Codex shell tool | exact input；临时红证据未落盘 |
| PG integration green | A1 | `UNBORN` | reactor test | 0；9 schema tests + 4 Draft PG tests + environment canary | 当前会话 tool capture | Codex shell tool | superseded by server gate |
| affected server gate | A1 | `UNBORN` | `tools/run-gate.ps1 -Gate server` | 0；全 reactor verify；并发 same-revision 仅一名 winner | `.sdlc/evidence/20260808-003059-server/metadata.json` | project gate | exact manifest |
| field identity inventory | A1 | `UNBORN` | `rg fieldId|field_id` on production Java | 无命中 | 当前会话 tool capture | Codex shell tool | exact input |

- 证据复用理由：server gate 在 T1-2 最后一次生产/测试变化后生成，包含 V002 Flyway migration 和 Testcontainers PostgreSQL 执行。
- 人工 gate：无新增产品判断。

## 重要偏差

- 第一次绿色尝试暴露 Spring 默认 CGLIB 无法代理 final/non-visible adapter，已把实现类改为可代理 public class；事务接口和模块依赖未改变。
- PostgreSQL JSONB existence operator `?` 会被 `JdbcClient` 识别为 positional placeholder；测试改用语义等价的 `jsonb_exists`，不影响产品 SQL。
- 数据库只保留 PK/FK/unique/check 等普通约束，没有按用户决策增加 Static/Draft 生命周期 trigger。
- 是否需要 spec delta：否。
- 再锚定是否发现漂移：否；表和接口只服务 Schema Draft，没有未来模块占位。

## 多 Agent

- 未启用；single writer，`atomic_claim:none`。

## 下一步

- 下一任务或恢复入口：P1/T1-3，先以 MockMvc contract tests 冻结 create/get/save 与 400/404/409 RFC 9457，再更新 OpenAPI、generated SDK 和生产 Schema Studio 路由。
- 需要用户决定：无。
