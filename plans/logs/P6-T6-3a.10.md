# P6-T6-3a.10：协作式取消反馈与原子调用记账

- 状态：`automated_verified`
- 实现 revision：`3fb66e251a837467be3ddc895be8631954a68813`
- 外部 Provider 副作用：本任务未发起新调用；只读诊断用户已执行的 run，并在部署前后核对累计计数

## 诊断

用户 run `fdbcbe45-22a5-4cd4-b096-4566cdf562a6` 的持久状态证明确认按钮已经生效：

- sequence 3 为 `CANCELLATION_REQUESTED`，run 保持 `RUNNING` 且 `cancellation_requested=true`；
- sequence 4 为 `CANCELLED`，取消请求到最终安全退出约 123 秒；
- 这是既定的协作式取消：正在进行的 DashScope HTTP 调用先返回或超时，worker 不再开始下一阶段；
- Web 旧实现忽略 `cancellationRequested`，仍显示普通运行态和“取消任务”，因此表现为按钮失效。

同时发现第二个取消竞态：该调用的 reservation 已 `SETTLED`，实际费用为 335,112 micros CNY，但旧 `checkpointAttempt` 在发现取消后回滚整个事务，导致 attempt 行缺失。费用账本仍正确，但执行日志错误地显示零调用。

## 实现闭环

1. 监控页在 `RUNNING + cancellationRequested` 时显示“取消请求已受理 / 正在取消”，解释安全检查点语义，并隐藏重复取消按钮。
2. 历史任务列表使用同一状态格式，避免另一入口继续显示普通“识别中”。
3. `recordAttempt` / `checkpointAttempt` 先锁定同一 run/lease/stage 边界；若已请求取消，在单一 PostgreSQL 事务内写入 attempt、落为 CANCELLED、清理 lease 并追加事件。
4. 新增成功响应与失败响应两类取消竞态的真实 PostgreSQL 回归，以及组件和 Playwright 浏览器回归。

## 证据

- focused PostgreSQL workflow：24 tests；PASS。
- focused Web component：6 tests；focused Playwright：1 test；PASS。
- clean Server A1：`.sdlc/evidence/20260810-170326-server`，142 tests，0 failure/error，3 gated skip。
- clean Web A1：`.sdlc/evidence/20260810-170326-web`，69 tests + lint/typecheck/build；PASS。
- clean E2E A1：`.sdlc/evidence/20260810-170433-e2e`，17 passed / 1 gated skip；PASS。
- Compose 部署前后均为 `active=0` / `attempts=15` / `reservations=12`；readiness `UP`，用户 run 仍为 `CANCELLED`，没有新增 Provider 调用。

## 残余边界

- 协作式取消不承诺强行中断已发出的 HTTP 调用；界面现在明确反馈等待过程，最多受当前阶段 timeout 约束。
- 历史受影响 run 的原始 Provider response、token 与 request id 未被保存，不能伪造或补写 attempt；其不可变 SETTLED reservation 仍是实际费用的权威记录。修复后的新 run 不再产生该缺口。
- 本节点是本地 A1 自动证据，不构成外部 A3；未由 Agent 触发真实 DashScope 调用。
