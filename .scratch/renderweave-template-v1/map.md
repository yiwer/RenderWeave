# Wayfinder Map：RenderWeave Template v1

Label: wayfinder:map

## Destination

形成一份可直接交给后续实施规划的、决策完备的 `RenderWeave Template v1` 产品与软件规格：覆盖在线 Template 与 Asset Management、JSON DesignDSL、数据/表达式、元素与布局、循环与嵌套、受控求值、RenderDSL/RenderDocument 以及有限二维图片输出；本地图不实施产品代码。

## Notes

- 当前 Schema v1 与已批准 delta 仍是权威；Template 创建时永久绑定精确 `{schemaKey, versionTag}`，不得改变既有 StaticSchema 内容或生命周期。
- Template 没有发布状态；metadata 位于 DesignDSL，每次 accepted save 追加不可变 revision；Template 拥有不可变 ownerScope，TemplateRef 只允许 same-scope 并跟随 current，DELETED 是不可恢复终态。
- TemplateReadiness/当前报告只是最终一致投影；INVALID/STALE 可编辑但不能 Render，编辑器打开和每次 Render 都权威重检最新 current 与一致闭包。
- DesignDSL JSON 是唯一作者内容事实源；编辑器是无损投影，Evaluator 是动态语义的唯一权威，RenderEngine 只消费符合 RenderDSL 的 RenderDocument。
- Evaluation严格经过closure/input/Asset admission、CapabilityState、串行惰性物化与原子seal；不定义正式MaterializedScene。RenderDocument使用独立`renderweave-render/1.0`、opaque occurrenceId与一对一RenderResource，只保留静态布局规则并由Engine形成final LaidOutScene。
- renderRequestId、evaluationFingerprint、evaluationResultDigest、renderDocumentDigest与Design contentHash严格分离；完整OccurrencePath/sourceNodeId只在请求级诊断sidecar，不进入RenderDocument或Engine。
- 外部 Connector 在 Template 边界外归一化业务数据；DesignDSL 不获得任意 SQL、HTTP、文件、脚本或凭证能力。
- Expression只可经显式input使用exact Profile拥有的封闭Clock/Random Evaluation Capabilities；同一renderRequestId/fingerprint内部恢复重放CapabilityState，新Evaluation重新取UTC snapshot/server-only nonce且不承诺跨请求重放。AssetResolver仍是独立的实际asset occurrence解析合同。
- Asset Management 首版只接纳 IMAGE/FONT，以稳定 Asset、不可变内容版本、逻辑 current、scope 内 Blob 去重、标签目录和可恢复软删除运行；被引用 Asset 仍可经精确影响确认删除。
- 资源交接固定为 `AssetRef → ResolvedAsset → RenderResource`：authored 引用只选 logical current，Rendering 按实际消费位置线性化钉死 exact 内容，RenderEngine 只接收去除 Asset 业务身份的一对一请求级 manifest 与 Renderer-only fetch lease。
- 首个语义目标是每条Renderer Command把一个有限二维根画板原子生成一张完整PNG/JPEG；多Template、输入、变体、DPI或格式由Engine外编排为独立请求。主要作者是理解 StaticSchema、字段路径和表达式的技术型/低代码模板设计者。
- `D:\Yiwer\code\hbads-design-v2` 仅作证据与反例来源，不承诺旧 DSL、API、存储或行为兼容。
- 允许一次性 UI/逻辑原型与 JSON fixtures 作为决策证据；不得进入产品路由、API、数据库或生产模块。
- 处理 grilling 票据时始终使用 `grilling` 与 `domain-modeling`；research/prototype 票据分别使用对应技能。

## Decisions so far

<!-- 仅在子票据解决后追加一行上下文指针。 -->

- [从历史系统提取 DesignDSL 语义证据](issues/01-historical-design-dsl-evidence.md) — 历史系统证明精确 Schema 上下文、集中动态语义与静态 Scene seam可行，也暴露了属性权威分散及无通用datasource/图片Renderer；原取证commit的ArrayLoop未接线已被后续HEAD修正，但最新系统仍以Java预布局、可持久化旧MaterializedScene运行，不能作为新RenderDocument复用。
- [评估受控能力下的表达式语言基础](issues/02-expression-language-foundations.md) — CEL 是首个原型候选，但类型、missing/decimal、受控能力、预算与 Java/Web 权威边界必须由 RenderWeave 自定义；JSONata/JMESPath/ANTLR 保留为对照。
- [划定 Template、Render 与 Asset 的限界上下文](issues/03-bounded-contexts-and-language.md) — 三个上下文以 Template closure snapshot、`AssetRef → ResolvedAsset → RenderResource` 和 RenderDocument 单向交接；Evaluation 消除动态组合并产生Engine-facing RenderDSL，Engine 内 LaidOutScene 再产生图片与权威预览。
- [定义可变 Template 生命周期与永久 Schema 合同](issues/04-mutable-template-lifecycle.md) — Template 以永久 Schema、线性 revision、current-only Asset/Template 依赖和终态软删除运行；依赖错误可确认保存 INVALID，但 Render 必须重检一致 current 闭包。
- [定义 Asset Management 产品边界与生命周期](issues/05-asset-management-domain.md) — Asset 以 scope 内内容去重、不可变 contentVersion、逻辑 current 与 ACTIVE/DELETED 恢复运行；AssetRef 按执行出现位置独立解析，被引用删除经 proof/token 协调后使 Template 重检。
- [定义渲染输入与词法作用域模型](issues/06-render-input-and-scope.md) — 根 Evaluation 以单一 RootDocument 和可选根级 Custom overrides 形成 AdmittedRenderInput；显式 invocation/loop domain、typed ABSENT、隔离子调用与 exact-Schema context proof 构成封闭词法作用域。
- [选择 Value Source、Binding、Mapping 与 Expression 模型](issues/07-value-binding-expression-model.md) — ValueSource/Definition 与自有 Expression 1.0 形成严格类型、显式输入、惰性求值模型；Binding 以 node-local targetPropertyRef 覆盖既有静态叶子，全局只追加 BindingPolicyCatalog 决定可绑定目标且失败绝不回退 baseline。
- [冻结 DesignDSL envelope、身份与演进规则](issues/08-designdsl-envelope-and-evolution.md) — DesignDSL 以 exact Profile、唯一 DesignRoot、client-owned UUID v4、strict JSON 与自有 canonical/hash 成为语义无损作者事实；Template revision export、raw import、显式 migration 和永久版本兼容均失败封闭，外部依赖漂移不改变 contentHash。
- [定义封闭的节点、属性与 BindingPolicy 模型](issues/09-node-property-slot-model.md) — 唯一 NodeContractCatalog 冻结 Canvas 根、15 种首批视觉 Node、mm/pt 单位、Placement/容器/Text/Image/Vector/码制合同与 ContentModel；独立只追加 BindingPolicyCatalog 只授权永久 Property Identity 的已有叶子，不引入 Slot、属性包、隐式字体或 Binding fallback。
- [定义几何与布局容器语义](issues/10-geometry-and-layout.md) — `renderweave-layout/1.0` 冻结固定物理 Canvas 内的约束自适应 measure/arrange、box/transform/clip/paint、Stack fillWeight、Grid track、Text shaping/overflow 与 Image/Vector bounds；RenderDocument 保留静态布局规则，Engine 内 LaidOutScene 不持久化，任何失败零输出。
- [定义循环与条件结构语义](issues/11-repeat-and-conditional-structure.md) — authored Repeat/Conditional 以 typed items/condition、显式 absent policy、Loop lexical frame 与确定性剪枝运行；item subtree 可显式放置 TemplateUse，itemLayout/instanceLayout 与 PACK 完成两级排布，动态结构在 RenderDocument 前完全降低为普通静态 Frame/Stack/Grid。
- [定义嵌套 Template 组合语义](issues/12-nested-template-composition.md) — structural TemplateUse以same-scope logical current、exact ContextSelector和PUBLIC Custom fills建立隔离child invocation；完整closure先冻结，实际调用按结构惰性执行，child artboard最终降低为RenderDocument静态compositionViewport并以CONTAIN/CENTER/双层clip嵌入parent host。
- [定义 Asset 引用、选择与解析合同](issues/13-asset-reference-and-resolution.md) — AssetRef 是服务端 UUID v4 的封闭 logical-current selector；完整 closure 先检查全部 authored occurrence，实际 Node property 再按稳定顺序逐 occurrence 线性化选择并形成 `ResolvedAsset → RenderResource`，exact lease、descriptor、Engine fetch、cache、错误脱敏及 selection digest 均失败封闭且零 partial output。
- [定义 Evaluation Capabilities 与请求内一致性](issues/14-evaluation-capabilities.md) — exact Expression Profile只开放显式input中的UTC_DATE/UTC_TIME与位置派生`[0,1)`Random；完整准入后建立短期幂等CapabilityState，同请求恢复重放而新请求换state，逻辑demand预算、HMAC向量、result digest、脱敏与Engine零残留均失败封闭。
- [定义 Evaluation 编译流水线与 RenderDocument 合同](issues/15-evaluator-and-render-document.md) — 权威Evaluator按固定阶段与consumer order把closure/input/capability/Asset物化并原子seal为`renderweave-render/1.0` strict静态文档；opaque occurrenceId、请求sidecar、一对一manifest、跨语言canonical/digest、四类运行身份及Engine final-geometry边界均失败封闭，文档不可公开、持久化或跨请求复用。
- [定义 RenderEngine 与图片输出合同](issues/16-renderer-and-image-output.md) — 每条closed Command为根Canvas原子生成一张PNG/JPEG；独立Layout/Renderer/Output Profile冻结deadline/cancel/registry、surface像素、资源与字体、sRGB raster、QR/Barcode、byte-exact编码、trace/transport/error及READY证据，旧busbox只能作为重建基线而不兼容。
- [原型验证在线 Template 与 Asset 创作工作流](issues/17-authoring-workflow-prototype.md) — 选择 `B · Canvas Focus` 作为桌面作者工作台基线：固定物理画布居中、左右按需面板与紧凑dock；属性以exact Property Identity投影为分组中文名称和类型化控件，BindingPolicy唯一授予bindability，同行Binding按钮明确区分未绑定、已绑定与ABSENT/ERROR，草稿画布始终不冒充权威预览。
- [定义编辑、校验、预览与恢复体验](issues/18-editor-preview-and-recovery.md) — 编辑器以Canonical baseline和显式save运行，区分Structured/Raw Repair/Compatibility三种模式；current-only权威preview、设备本地恢复、冲突覆盖、unknown save reconciliation、历史恢复、问题代次、键盘与WCAG边界均失败封闭，且不引入autosave、merge或产品实现。

## Not yet specified

- Ticket19已冻结权限/closed-world边界、175个容量cell与543个容量case严格下限、3,651条atomic requirement、12项Editor J1、Conformance case/oracle record schema、`renderweave-conformance-c14n/1.0`、十个Oracle family、六个execution class、110个exact Probe inventory、21个canonical byte/digest vectors、六个Safe baseline定义、六个generator contract、175轴/525变体capacity coverage mapping、bootstrap order、assertion级coverage与append-only supersession。Probe Profile已用143个accepted与110个rejected assertion vectors完成全局gate并可被record引用；SPEC_REGISTRY自己的baseline、generator、31-probe adapter、Node primary与Python independent executor、exact target及363条assigned requirement mapping已冻结。首批46个Case与46个Oracle按相同bytes发行；`spec-registry-bootstrap/1.7`目标保留十一份历史catalog snapshot并加入两份当前snapshot（共13份），正式JSONL由Node/Python分别重放22,560与22,468项通过。current phase仍为`CAPACITY_BOUNDARY`。175轴只静态物化为525个Case shape candidate与525个Oracle shape candidate，当前Node/Python分别通过38,246与39,401项检查，正式capacity record保持0。DOMAIN_SERVICES的4轴已冻结12份capacity fixture与1份named fixture，Node/Python分别通过138与246项；DESIGN_INPUT_EXPRESSION的65轴已冻结195+1份fixture，分别通过2,163与2,529项；RENDERING_PIPELINE的52轴已冻结156+1份fixture，分别通过1,734与2,072项；RENDERER_EXACT_OUTPUT的54轴已冻结fixture-only effective Command context、32-probe closed adapter、deterministic generator、162份capacity predicate fixture及1份named context fixture，分别通过1,800与2,211项；EDITOR_AUTOMATED在0个capacity axis边界下冻结Canvas Focus、Structured/clean/current-recheck-pending的fixture-only EditorSession context、31-probe closed adapter与1份named fixture，分别通过186与189项。Editor automation admission seam现进一步冻结supported-target matrix、immutable product build、exact browser/OS、四种environment profile、runner、active corpus、adapter与独立回放的全成或全不成接口；410条EDITOR_AUTOMATED requirement及4条J1跨family依赖已形成414条routing inventory、45组与12个automation journey seed，Node/Python静态重放27与5,385项一致通过，但repository readiness仍以九个closed blocker拒绝admission。12个journey现进一步结构化拆成108个`EDC::Jnn::nnn` planning candidate，形成138/138条seed requirement planned edge与1,284条assertion plan；其中905条为exact expectation（619条literal、286条ABSENT），379条仍待target literal/artifact，且working-copy、canonical baseline、reconciliation lock、preview generation等语义暴露出12个必须经未来新exact Probe Profile发行的candidate probe。Node/Python分别通过20与13,509项静态检查，但formal Editor Case/Oracle仍保持0，当前Probe Profile未修改。六个class的baseline/generator静态夹具现均可引用；四个承载容量轴的class仍各自只剩exact产品target、required executor manifests与产品独立回放三项阻塞，Editor则仍缺bootstrap predecessors、supported target matrix、exact product build、browser/OS及environment targets、browser automation executor、exact fixture/fault/terminal/target-literal bindings、Probe Profile revision决策、formal Case/Oracle发行、产品独立回放和独立J1。全部这些证据只证明fixture bytes、精确scalar/token/closed-formula保存、单轴输入隔离、Editor静态上下文、admission接口或candidate decomposition，不证明产品guard wiring或浏览器行为；Renderer夹具没有构造合法产品Command或RenderDocument，也没有执行resource fetch/decode、layout、shaping、raster、encode、trace、Engine、物理Linux认证或terminal output；Editor夹具、admission audit与candidate evidence均没有启动浏览器/Web服务、构建/执行产品、保存、冲突、恢复、预览、无障碍断言或J1。18个combined和其他非容量atomic records仍pending，完整自动corpus仍不可称executable。Renderer hermetic Linux build前置虽已冻结，但没有build授权或build attempt，仍未完成link、ELF、font、pixel或两台不同家族物理Linux验证；Ticket19保持open。还须按execution-class gate补齐产品target与executor后再做preissuance并append正式capacity records。未来若引入batch/variant，应作为Engine外独立编排领域另行设计。
- Editor atomic/probe current snapshot（取代上一条中“1,284条assertion plan/12个future probe proposal/Probe Profile revision决策”子段）：12个source proposal已按deep closed Interface裁决为7个原样保留与5个合并，净形成9个新probe；完整但未发行的`renderweave-conformance-probes/1.1` candidate现含119个probe、157个accepted与119个rejected vector，`editor.workingCopyDigest`明确支持Raw Repair/Compatibility Read-only的`ABSENT`，Editor candidate Adapter映射40个probe，且`recordMayReference=false`、current 1.0 Profile bytes未变。108个`EDC::Jnn::nnn`仍有1,265条assertion plan，109条binding精确指向1.1 candidate并覆盖82个candidate。Probe candidate Node/Python分别通过17与29项；1.1显式发行、formal Editor Case/Oracle、产品重放及J1均未发生。
- Editor terminal/fault current snapshot（取代上一条中“902条exact/363条pending、37份fault artifact与33个terminal binding尚未闭合”子段）：33个原pending terminal candidate已逐项裁决为exact code/stage或`NOT_REQUIRED`，pending terminal为0；37个非`NONE` fault schedule已分别冻结为closed、ordered、exact-once、SHA-256/formal-identity绑定artifact，另71个candidate绑定exact `NONE` identity。108个candidate仍有1,265条assertion plan，其中955条exact expectation（668条literal、287条ABSENT）、310条pending；Node/Python分别通过28与14,649项静态回放。该收口只冻结planning Interface，不证明产品terminal或fault injection seam；仍缺108份exact product fixture、108组target literal/artifact、108个browser/OS target、108个runner、108份独立product replay，82个candidate仍受未发行Probe Profile 1.1约束，formal Editor Case/Oracle、产品执行与J1均为0。
- SPEC_REGISTRY current target snapshot（取代上一条中`spec-registry-bootstrap/1.7`与旧replay计数）：`spec-registry-bootstrap/1.8`固定115份artifact，Node primary与Python independent分别通过22,571与22,479项重放；46个Case与46个Oracle bytes及current Probe Profile 1.0保持不变。
- SPEC_REGISTRY terminal/fault target snapshot（取代上一条`spec-registry-bootstrap/1.8`子段）：`spec-registry-bootstrap/1.9`固定155份artifact，新增terminal adjudication、fault contract/catalog及37份独立fault artifact；Node primary与Python independent分别通过22,611与22,519项重放，46个Case、46个Oracle及current Probe Profile 1.0 bytes保持不变。
- Editor input/target current snapshot（取代上一条中“955条exact/310条pending、108份exact product fixture与108组target literal/artifact尚未闭合”子段）：108个candidate各自绑定一份immutable semantic input fixture及formal input identity；310个原pending expectation中，108个terminal-vector已冻结为exact JSON target artifact，38个非空network request sequence已冻结为exact literal，共146个机械可确定target收口。当前1,265条assertion plan中1,101条为exact expectation（706条literal、108条artifact、287条ABSENT），164条仍按12种probe原因fail-closed；其中101条pending literal、63条pending artifact，107个candidate仍带target-value blocker。Node/Python分别通过34与20,360项静态回放。该收口只证明semantic fixture/target bytes和identity，不证明product fixture adapter、产品观察、browser/OS target、runner或product replay；formal Editor Case/Oracle、J1与READY仍为0。
- SPEC_REGISTRY input/target target snapshot（取代上一条`spec-registry-bootstrap/1.9`子段）：`spec-registry-bootstrap/1.10`固定375份artifact，新增input fixture contract/catalog、108份input fixture、target binding contract/catalog及108份terminal target artifact；Node primary与Python independent分别通过22,831与22,739项重放，46个Case、46个Oracle与current Probe Profile 1.0 bytes保持不变。
- Editor content-source current snapshot（取代上一条“1,143条exact/122条pending/60条内容派生/95个target-value blocker”子段）：`EditorSemanticProjection`现将Raw Repair与Compatibility Read-only两例的`editor.workingCopyDigest`绑定为exact `ABSENT`，因为两种模式都不存在可信canonical DesignDSL working copy；未发行Probe Profile 1.1只增加该`ABSENT` operator vector。新增窄封闭`EditorContentSource` contract/catalog，以`IMMUTABLE_SPEC_FIXTURE`或`ADMITTED_PRODUCT_CAPTURE`为仅有adapter，冻结35个working-copy与12个canonical-baseline第一层source slot；10个slot要求server canonical response，2个要求existing trusted baseline，47个binding全部`UNBOUND`、source artifact为0，禁止由baselineId、revisionDelta、动作名或跨execution-class artifact推导bytes/revision/contentHash/digest。当前1,265条assertion plan中1,145条exact（748 literal、108 artifact、289 ABSENT）、120条pending；pending严格分为58条内容派生项（47个第一层source slot加11个下游前置图）与62条UI观察，94个candidate仍带target-value blocker。Node/Python分别通过37与21,102项静态回放；formal JSONL仍46/46且formal Editor Case/Oracle、产品执行、J1与READY均为0。
- SPEC_REGISTRY content-source target snapshot（取代上一条`spec-registry-bootstrap/1.11`子段）：`spec-registry-bootstrap/1.12`固定379份artifact，新增content-source contract/catalog；Node primary与Python independent分别通过22,835与22,743项重放，46个Case、46个Oracle及current Probe Profile 1.0 bytes保持不变。
- Editor dirty-guard content-source snapshot（取代上一条“47个binding全部`UNBOUND`、source artifact为0、1,145条exact/120条pending/94个target-value blocker”子段）：只对`EDC::J10::012::PA011`冻结一份Editor自有`IMMUTABLE_SPEC_FIXTURE` source record及两份`renderweave-design-c14n/1.0` exact DesignDSL bytes；clean baseline与dirty working copy只在顶层authored `displayName`不同，source record以revision 0、domain-separated contentHash、两份working-copy digest、exact rejection terminal与`BYTE_IDENTICAL_TO_PRE_ACTION`规则证明该规划场景的post-action target仍为pre-action dirty bytes。其余46个source slot继续`UNBOUND`。当前1,265条assertion plan中1,146条exact（749 literal、108 artifact、289 ABSENT）、119条pending；原58条semantic content prerequisite中1条由source闭合、57条仍pending，另62条UI observation不变；93个candidate仍带target-value blocker。Node/Python分别通过37与21,141项静态重放。该fixture只证明spec-owned bytes与expected atomic-rejection state rule，不证明产品动作、fixture adapter、浏览器观察、formal Editor Case/Oracle、J1或READY。
- SPEC_REGISTRY dirty-guard target snapshot（取代上一条`spec-registry-bootstrap/1.12`子段）：`spec-registry-bootstrap/1.13`固定382份artifact，新增一份content-source record与两份canonical DesignDSL artifact；Node primary与Python independent分别通过22,838与22,746项重放，46个Case、46个Oracle及current Probe Profile 1.0 bytes保持不变。

## Out of scope

- 本地图不创建或修改 Template/Asset 产品代码、API、数据库表、路由、生产配置或实施 Phase DAG。
- 不修改 Schema v1、StaticSchema 不变量、Schema DSL、validator 或 compiled JSON Schema 语义。
- 不导入或兼容 `hbads-design-v2` 的历史 DesignDSL；只提取可验证的语义证据与失败教训。
- 不实现 CSV、Excel、数据库、HTTP 等具体 Connector，也不允许 DesignDSL 直接持有查询、凭证或任意外部访问能力。
- Workspace、实时协作及组织级权限模型不在本规格；Asset Management 所需的最小所有权边界由其领域票据决定。
- 不开放任意插件节点、运行时代码注册、HTML/CSS DOM 或通用脚本能力。
- HTML、PDF、视频、3D 等通用文档/媒体目标，以及生产 render farm、调度和部署，不在首版目的地内。
- Asset 的 SVG/动画/DOCUMENT、URL/ZIP 导入、文件夹/分享、内容变换、标准化 quarantine、硬清除/GC/保留期、scope 计费配额、全局去重与原子批处理留在 fog。
