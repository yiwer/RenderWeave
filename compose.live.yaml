services:
  api:
    environment:
      DASHSCOPE_API_KEY_FILE: /run/secrets/dashscope_api_key
      RENDERWEAVE_LIVE_AI_ENABLED: "false"
      RENDERWEAVE_LIVE_UPLOAD_ENABLED: "false"
    secrets:
      - dashscope_api_key

secrets:
  dashscope_api_key:
    environment: DASHSCOPE_API_KEY
