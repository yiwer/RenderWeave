# Integration tests drive workers explicitly unless a wiring test opts production recovery back in.
renderweave.inference.recovery-enabled=false
# Integration tests drive the replayable Template consumer explicitly; production defaults on.
renderweave.template.stale-consumer.enabled=false
